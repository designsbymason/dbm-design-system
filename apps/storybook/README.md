# storybook

Placeholder — **not** where Storybook itself lives. The real Storybook instance (Storybook 10, currently pinned at 10.5.x) is already built and in active use at `packages/components/.storybook/`, alongside the component library it documents — it's been running since Phase 3. This `apps/storybook/` folder is reserved for the future **public-hosted** deployment target only (e.g. a static-export/deploy wrapper around `pnpm build-storybook`'s output) — currently **Phase 9** in the roadmap (`guidelines/01-vision-and-goals.md` §13; corrected 2026-08-12 from "Phase 2," which was this doc's number under an earlier, pre-2026-07-18 phase numbering, and from "Storybook 8," the version originally planned before Phase 3 bumped to 10 — see `guidelines/02-tech-stack-and-structure.md` §3).
