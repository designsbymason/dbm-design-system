# FieldLabel — Storybook/component review findings

*(Migrated from `07-storybook-and-documentation-standards.md` §6 during the guidelines retrofit pass, 2026-08-31 — this file's own content is unchanged from what was there, just relocated. See `07`'s own status table for this component's current Docs-page/Finalized status.)*

FieldLabel — ✅ done (2026-08-29 — comprehensive `06-engineering-standards.md` §9 review, worked one finding at a time across 7 findings): (1) `Default` story renamed to `Playground`, every prop wired live via `args`/`argTypes` (`children`, `htmlFor`, `size`, `required`, `disabled`) — confirmed multiple real, concrete gaps live first, not assumed: `size` showed an inert "Choose option..." placeholder despite `size="md"` being the real default; `required`/`disabled` both showed inert "Set boolean" placeholders; `children` fell back to a raw "Edit children as JSON" control instead of a proper text input, since it had no explicit `argTypes` entry at all. Immediately following, at explicit direction: `children`/`size`/`required`/`disabled` reordered to content-first in both the type interface and the component's own destructuring (previously `size, required, disabled, children`) — matches `FieldError`'s/`FieldHelperText`'s own established precedent. (2) `AllSizes`'s dead Controls panel fixed (`render` ignored `args` entirely, the same bug already fixed on `CloseButton`/`FieldError`/`Checkbox`) — `size`/`children`/`htmlFor` correctly stay fixed per-instance (the whole point of the grid, and each needs a unique `htmlFor` for a valid association), `required`/`disabled` now live and shared; verified live, toggling `required` updates all 5 rendered instances simultaneously. (3) `children` had no JSDoc at all — added (`/** The label text. */`, matching `FieldError`'s/`FieldHelperText`'s own one-line style). (4) `id`/`className`/`style`/`data-testid` redeclared on `FieldLabelProps` with full JSDoc — previously only worked via native inheritance, the same gap already fixed on `FieldError`/`FieldHelperText`/`Checkbox`. (5) `htmlFor` — the single most functionally important prop on this component, despite the component's own top-level JSDoc already leaning on it — was never itself redeclared either, only inherited; added with its own JSDoc, positioned right after `children` (ahead of the visual/behavioral props) given its functional importance, not treated as a generic escape-hatch prop the way `id`/`className`/`style`/`data-testid` are. (6) Judgment call, explicitly declined rather than built: some comparable libraries' own label components support an `error` prop that tints the label red when its field is invalid — flagged with the concrete gap named, reasoned that `FieldError` already exists as a dedicated atom for that exact signal and duplicating it on the label risks two places disagreeing — **declined at explicit direction**, no error-state styling added. (7) `FieldLabel.mdx` Docs page added from scratch, full 10-section template mirroring `FieldError.mdx`/`FieldHelperText.mdx`.

  **Final review pass** (2026-08-29, same day): re-verified the full checklist end to end rather than assuming the 7 findings already covered everything, the same discipline as every prior finalized component's own final pass. No new findings — re-confirmed live: every story's own Controls panel, not just Playground's (`Required`'s toggle live-removes the asterisk; `Disabled`'s starting state and toggle both correct), theming beyond the Playground story (`Disabled` spot-checked in Purple/Dark, legible and correct), and a `tsup` build succeeds cleanly with `FieldLabel`/`FieldLabelProps`/`FieldLabelSize` correctly present in the emitted `.d.ts`. Full self-verification, whole package: `tsc` (package + `.storybook`), `eslint`, `tsup` build, both Vitest projects (`unit`: 771/771, `storybook`: 278/278), and the Foundations token-coverage check (78/78 in sync) all clean. **Finalized 2026-08-29** — per that section's own note, don't make further changes to FieldLabel (code, stories, docs, or its tokens) without asking first.


**Authorized post-finalization change (user-directed, 2026-09-14): cross-link to `FormField` added.** Now that `FormField` (item 3, molecule build order) exists and actually composes this atom, added a `FormField` card to this Docs page's own "Related components" section — a live, working preview (`<FormField label="Email address">{(fieldProps) => <Input {...fieldProps} />}</FormField>`), same as every other card on this page. No change to `FieldLabel.tsx`/`FieldLabel.types.ts`/any existing story or card.

**Three-question finalization test (`06-engineering-standards.md` §9):**
1. *Does it change the rendered/behavioral output of anything that already existed at finalization
   time?* No — purely additive: one new import, one new `RelatedCard`, appended after the existing
   ones. Every previously-existing section, prop, story, and card is untouched.
2. Not reached — question 1 already resolved to "No."
3. Not reached.

**Result: stays Finalized.** Full re-verification: `tsc` (package + `.storybook`), `eslint --max-warnings 0`, both Vitest projects (`unit`: 1221/1221 whole package, `storybook`: 414/414), `tsup` build — all clean. Live-reconfirmed: the new card renders its live preview correctly on the Docs page.

**Correction (user-reported, 2026-09-14, with screenshot): that same `FormField` card's preview was cropped.** `RelatedCard`'s preview slot is a fixed 80px-tall box built for a single compact instance (see `guidelines/component-reviews/PasswordInput.md` for the full root-cause writeup) — the composed `<FormField label="Email address">{...}</FormField>` used here was taller than that, so it was clipped top and bottom. Fixed by previewing a bare `Input` instead, matching every other card's own "single compact instance" convention; the description text alone still conveys the `FormField` relationship. Purely corrective within this same authorized change — stays Finalized, no new date.
