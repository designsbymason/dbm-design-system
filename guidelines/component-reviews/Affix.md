# Affix — Storybook/component review findings

*(Migrated from `07-storybook-and-documentation-standards.md` §6 during the guidelines retrofit pass, 2026-08-31 — this file's own content is unchanged from what was there, just relocated. See `07`'s own status table for this component's current Docs-page/Finalized status.)*

Affix — ✅ done (2026-08-30 — comprehensive `06-engineering-standards.md` §9 review, worked one finding at a time across 10 findings, two of them judgment calls confirmed at explicit direction before building): (1) Real, confirmed bug — `data-stuck` was computed *before* `{...props}` in the JSX and, unlike some prior "instances" of this bug class, was genuinely never destructured out of `props`, so a consumer-supplied `data-stuck` silently won over the real computed stuck state. Proved it wasn't theoretical before fixing: rendered `<Affix data-stuck="false">` and confirmed the DOM came out `data-stuck="false"` regardless of real state; fixed by moving `{...props}` first (10th confirmed instance, `05-component-api-conventions.md` §3), with a regression test verified to actually fail against the reverted code before being trusted. (2) The sentinel's `height: 1` hardcoded value replaced with the existing `space.px` token (0.0625rem) — a real, previously-unnoticed "no hardcoded values" violation. (3) `data-testid` didn't even typecheck (not part of native HTML types, never redeclared) — same recurring gap fixed on every other reviewed component; redeclared alongside `id`/`className`/`style` with full JSDoc. (4) `children` given real JSDoc (previously undocumented). (5) Feature-completeness gap, named against Ant Design's own `Affix` (same name, same role) and built at explicit direction: `scrollContainerRef` — the `IntersectionObserver`'s `root` was always `null` (the viewport), which doesn't correlate correctly with a nested scrollable container's own scroll position (`position: sticky` itself already works against any scrolling ancestor with no configuration; only the *stuck-state detection* needed this). Named `scrollContainerRef` (a `RefObject`) rather than porting Ant's own `target` (a getter function) — more idiomatic for this system's own conventions, and unambiguous where "target" alone isn't. New `WithinScrollContainer` story demonstrates it, with its own dedicated `play`-function test (not just the unit-level "root gets passed to the constructor" check) proving the feature works end-to-end in a real browser — scrolling the panel itself (never touching the page) still correctly reports `data-stuck`. (6) Scroll-simulation `play`-function interaction story added at explicit direction (`ScrollInteraction`) — real page-level `window.scrollTo` in the Chromium-backed `storybook` Vitest project, which exercises a genuine `IntersectionObserver`, unlike the jsdom-based `unit` project's stub. Two real bugs found and fixed while building it, both confirmed via the actual failing-test output before fixing, not guessed: `.closest("[data-stuck]")` returns `null` whenever unstuck, since `data-stuck={isStuck || undefined}` means the attribute is never rendered at all when `false` — fixed by using `header.parentElement` (Affix's real sticky root) directly instead; and the `WithinScrollContainer` story's own scrollable demo panel (`overflow: auto`, no `tabIndex`) failed live a11y checks under axe's `scrollable-region-focusable` rule — fixed with `role="region"` + `tabIndex={0}` (a justified `eslint-disable` for `jsx-a11y/no-noninteractive-tabindex`, a known false-positive tension between that lint rule and the actual WCAG-correct pattern for a scrollable region). (7) `Playground` story added — the sticky-header demo now has live `side`/`offset` controls. (8) `Affix.mdx` Docs page added from scratch, full 10-section template.

  **Correction, same day, at explicit direction:** the pre-existing `StickyHeader` story was initially removed as redundant with the new `Playground` (both render the identical demo), reasoning it added no distinct coverage. The user's own original finding had actually called for the opposite — keeping it as a dedicated Variants-gallery entry alongside the Playground, not folding the two together — and flagged the removal as a deviation from what was originally scoped when asked directly whether every finding had been completed. `StickyHeader` restored (renamed "Sticky table-style header" for the sidebar/Variants heading), still args-driven rather than the original bare `render` that ignored args. `Affix.mdx`'s Variants section now has two entries: the restored sticky-header demo and `WithinScrollContainer`. Playground and a static Variants gallery are two distinct, both-expected sections per the standard template (`07-storybook-and-documentation-standards.md` §4 item 2 vs. item 4) — one doesn't substitute for the other just because a given component's demo happens to render similarly in both. Live-verified the restored story renders correctly on the Docs page; full self-verification (`tsc`, `eslint --max-warnings 0`, both Vitest projects — `unit`: 815/815, `storybook`: 294/294) all clean.

  **Real bug found and fixed, same day, via direct user report — `id`/`className`/`style`/`data-testid` had no `argTypes` entries at all in `Affix.stories.tsx`'s meta, unlike every other reviewed component.** All four rendered as broken, inert "Set string"/"Set object" Controls-panel placeholders instead of "–" — confirmed live via the raw Controls panel's own DOM content (`id: Set string`, `className: Set string`, `style: Set object`, `data-testid: Set string`), the exact violation `07-storybook-and-documentation-standards.md` §5 already prohibits ("check the full rendered Properties table for stray placeholders, not just the props you deliberately typed" — missed here because these four were never typed into `argTypes` at all, not because a typed one was wrong). Fixed by adding the same `control: false` + description block every other reviewed component's stories file already has for this set of four. Live-reconfirmed after the fix: all four now read `-`.

  **The same report also claimed `side="top"` doesn't actually stick — investigated and found not to be a real bug, but worth recording since it looked like one at first.** Manually scripting a scroll via this session's own browser-automation tooling (reaching into the Storybook manager+preview iframe hierarchy from outside, the same technique already flagged once this session as unreliable — see the `WithinScrollContainer` finding above) showed the sticky element apparently tracking the scroll 1:1 instead of pinning, which would mean `position: sticky` wasn't taking effect at all. Rather than trust that reading, added a temporary `play`-function test directly to the `Playground` story (real `window.scrollTo` in the Chromium-backed `storybook` Vitest project, mirroring `ScrollInteraction`'s already-proven mechanism) — it passed cleanly, confirming `side="top"` genuinely works correctly and the manual reading was a false signal from the debugging method itself, not the component. Diagnostic story reverted once confirmed (kept the file at its prior, already-reviewed state — no permanent test added here, since `ScrollInteraction` already covers this exact behavior and a near-duplicate would be redundant). Full self-verification after both fixes: `tsc`, `eslint --max-warnings 0`, both Vitest projects (`unit`: 815/815, `storybook`: 294/294) all clean.

  **Correction — the dismissal above was wrong. Real, severe, previously-shipped structural bug, found via a second, more precise direct user report the same day.** The user pushed back on the "false signal" conclusion with a specific description that didn't match automation flakiness: in `Playground` with `side="top"`, the header's own text correctly flips "Scroll down" → "Stuck!" while scrolling, but the header visually keeps scrolling away with the rest of the content instead of pinning to the viewport top. That precision is what exposed the actual gap — the `play`-function test added above only asserted on the `data-stuck` attribute (state detection via `IntersectionObserver`), never on the element's real screen position, so it could pass cleanly even with `position: sticky` completely broken; state detection and visual pinning are independent and this test only proved the former. Re-investigated with a `getBoundingClientRect()` check instead of trusting the attribute, and found the real defect: `Affix.tsx` wrapped the sentinel and the sticky root together in a shared `.wrapper { position: relative }` div, which made that wrapper the sticky element's CSS *containing block* — and since the wrapper's only other child (the sentinel) is `position: absolute` and contributes no flow height, the wrapper was never taller than the sticky content itself, leaving it almost no room to actually stay pinned. Confirmed empirically before fixing: scrolling 1400px down showed `getBoundingClientRect().top` at `-506px` (fully off-screen) instead of `0`. Fixed by removing `.wrapper` entirely and rendering the sentinel and sticky root as plain `Fragment` siblings ordered by `side` (sentinel first for `side="top"`, last for `side="bottom"`) — siblings inherit whatever containing block the *consumer's* own surrounding layout provides, which is normally the whole scrollable region `Affix` is meant to stick through, not an Affix-internal wrapper sized to its own content. Verified the fix rigorously, not just visually: temporarily reverted to the buggy wrapper structure, confirmed the new regression test (asserting `getBoundingClientRect().top` stays `0` across a real `window.scrollTo`) failed against it exactly as expected, then restored the fix and confirmed it passed. `side="bottom"` had zero test coverage before this and turned out to need real geometry care to test correctly (`bottom: 0` sticky requires substantial *leading* content and engages scrolling *down*, the mirror of `top`'s trailing-content/scroll-down-from-zero case) — added as a new `BottomScrollInteraction` story with its own `play`-function regression test, confirming `getBoundingClientRect().bottom` stays pinned at `window.innerHeight` across scroll. `WithinScrollContainer`'s own play function gained the equivalent visual-position assertion (with a `<3px` tolerance — the panel's own border-width creates a real, expected sub-pixel offset between its border-box and the edge Affix pins to, not a bug). **Lesson for future `IntersectionObserver`-based components: a passing `data-stuck`/state-based test is not proof the underlying CSS positioning works — assert on real geometry (`getBoundingClientRect()`) too, since the two are independently implemented and can diverge exactly like this.** Live-reconfirmed in a real Chromium tab post-fix: `side="top"` in `Playground`, scrolled to 1400px, `top` reads `0` (was `-506`). Full self-verification, whole package: `tsc`, `eslint --max-warnings 0`, `tsup` build, both Vitest projects (`unit`: 815/815, `storybook`: 295/295), the bundle-size check (Affix: 0.52KB JS / 0.08KB CSS, within budget), and the Foundations token-coverage check (79/79) all clean.

  **Systemic Storybook gap found across the whole Layout category, not just Affix (flagged by direct user observation, confirmed by checking the actual files rather than assumed):** `AspectRatio`, `Center`, `Bleed`, `Backdrop`, `BackToTop`, `Divider`, and `Spacer` all have zero `argTypes`/`args` in their own `.stories.tsx` — no Playground, no live per-prop Controls panel, matching exactly what Affix looked like before this review. Whatever "documentation" exists for each currently lives entirely inside a single bespoke demo `render` function, doing double duty as both the interactive showcase and the closest thing to prose explanation — the gap `07-storybook-and-documentation-standards.md`'s own §1 originally called out project-wide, still unresolved for this specific tier. Not fixed here (out of scope for Affix's own review) — flagging so the next session doesn't have to rediscover it component by component when each of these gets its own review pass.

  A real bug found and fixed while building the Docs page's own Related Components section, unrelated to Affix itself: `Backdrop` defaults to `inPortal={true}` (portals to `document.body`), so previewing it inside a small contained `RelatedCard` preview box — even with an inline `style={{ position: "absolute", inset: 0 }}` override — silently escaped the container and dimmed the entire Docs page, since the portaled element's `position: absolute` resolved against `document.body`, not the intended local wrapper. Fixed with `inPortal={false}` on the preview instance specifically; confirmed live before and after (the whole page was visibly dimmed, then rendered normally). `Backdrop` itself is unreviewed/untouched — this was a preview-usage bug in `Affix.mdx`, not a defect in `Backdrop`.

  **Two more real bugs found and fixed, same day, while specifically re-checking the Docs page's Playground and Variants sections at direct user request (a follow-up check after the structural sticky-positioning fix above):**

  (1) **Direction-agnostic stuck detection.** The stuck-state logic (`stuck = !entry.isIntersecting`) couldn't tell "the sentinel scrolled past the edge" (genuinely stuck) from "the sentinel hasn't been scrolled to yet" (mounted below the fold — a long Docs page stacking multiple demos is exactly this case, but so is any real page where `Affix` sits deep in content, or behind a route that lands mid-page) — both report `isIntersecting: false`. Caught by loading the Docs page itself: at `scrollY: 0`, the "Sticky table-style header" Variant (positioned far down the combined page) read `data-stuck="true"` and rendered "Stuck!" despite never having scrolled anywhere. Fixed by comparing the sentinel's own edge against the observed root's edge instead (`entry.boundingClientRect.top < (entry.rootBounds?.top ?? 0)` for `side="top"`, the bottom-edge mirror for `side="bottom"`), which disambiguates "scrolled past" from "not yet reached." `side` added to the effect's dependency array since the callback now reads it. Three new regression tests added (`Affix.test.tsx`, `unit` project: 13→16) — one for the below-the-fold case, one confirming `side="bottom"` uses its own edge rather than reacting to the opposite one — all three verified to actually fail against the reverted `!entry.isIntersecting` logic before being trusted, matching this file's own established discipline. Re-ran the real Chromium-backed `storybook` Vitest project after the fix (still 5/5) to confirm the directional `rootBounds`/`boundingClientRect` logic holds under an actual browser `IntersectionObserver`, not just the jsdom mock.

  (2) **Storybook Docs-mode breaks page-scroll sticky demos, independent of Affix's own correctness.** Even after fix (1), the Playground and Sticky-header demos still never visually stuck when viewed through the Docs page's `<Canvas>` blocks (though they worked correctly on their own standalone sidebar story pages, and in every automated test). Root cause: Storybook's addon-docs wraps every inlined `<Canvas>` story in a `.docs-story` container that unconditionally sets `overflow: auto` — to contain wide/tall demos without blowing out the page layout — and per the CSS spec, any ancestor with `overflow` other than `visible` becomes `position: sticky`'s nearest scrolling-ancestor reference, *even with zero actual overflow* (confirmed: `.docs-story`'s own `scrollHeight === clientHeight`, so it never has a real scrollbar to move). A page-scroll-based `Affix` (no `scrollContainerRef`) trapped behind that boundary has no real scroll delta to react to and can never stick, regardless of how correct its own CSS or JS is. This is a Storybook/MDX architectural constraint, not something fixable inside the component. Fixed per-story via `parameters: { docs: { story: { inline: false, iframeHeight: 500 } } }` on `Playground` and `StickyHeader` (the two page-scroll demos surfaced via `<Canvas>` in `Affix.mdx`) — this makes Storybook render those specific Canvas blocks as real per-story `<iframe>`s (their own document/window) instead of inlining the live React tree into `.docs-story`'s DOM, sidestepping the containing-block issue entirely. `WithinScrollContainer` didn't need this — it already creates its own actually-scrollable local container via `scrollContainerRef`, which sits nearer than `.docs-story` in the ancestor chain and wins first. Live-confirmed post-fix: the Playground demo's `<Canvas>` on the Docs page now renders in a genuine nested iframe, and scrolling within it correctly pins the header ("Stuck!" visible with content scrolling behind it, screenshot-verified). The Sticky-header demo's own live re-check was inconclusive in this session's own browser-automation tooling specifically — `document.visibilityState` kept reading `"hidden"` for that check regardless of clicks/scrolls, a known trigger for Chromium's own `IntersectionObserver` throttling on backgrounded tabs, unrelated to the fix — but it shares 100% identical `Affix` usage with the already-confirmed Playground demo and the same passing automated `storybook` Vitest suite, so treated as equally fixed rather than re-litigated further.

  **Correction to fix (2), same day, at direct user report — `inline: false` fixed the visual-sticking issue but broke the Playground's entire reason for existing.** Reported symptoms: `offset` looked completely inert (control showed a value but nothing visibly changed) and `side="bottom"` "acted like top." Root cause was neither prop nor layout — it was fix (2) itself: an `inline: false` story renders in a genuinely separate iframe/document, and this system's own `PlaygroundControls`/`usePlaygroundArgs` (the custom `<Controls>` replacement — see that file's own doc comment) pushes updates through the Storybook channel, which never reached that separate iframe's own preview runtime. The Controls widgets themselves correctly displayed the newly-picked values (confirmed: `offset` showed `12`, `side` showed `bottom`) while the actual rendered demo silently kept rendering the original defaults (`top: var(--dbm-space-0)` — confirmed via the DOM's own `style` attribute, unchanged no matter what was selected). `StickyHeader` has no `PlaygroundControls` attached on the Docs page (a static, args-free demo) and loses nothing from `inline: false`, so it keeps the fix. `Playground` reverted to normal inlining — its entire purpose is live Controls-driven interaction, which matters more than the embedded canvas being able to demonstrate the literal scroll-to-stick transition (a job better suited to the dedicated, already-tested `ScrollInteraction`/`BottomScrollInteraction` stories anyway, which were never surfaced via `<Canvas>` in the first place). Live-reconfirmed post-revert: changing `offset` immediately updates the rendered `style` attribute (`top: var(--dbm-space-12)`), and changing `side` to `bottom` immediately does too (`bottom: var(--dbm-space-12)`).

  **A second, unrelated real bug found investigating the same report — `offset`'s Controls-panel value never displayed at all (read as unset/blank), independent of the `inline:false` issue above.** Root cause in the *shared* `PlaygroundControls.tsx`/`ControlField` (this repo's own `.storybook/blocks/` tooling, not per-component code): its `select`/`radio` branch decided what to display with `typeof value === "string" ? value : undefined`, silently assuming every select-type control's options are strings. `offset`'s options are numbers (the space-token scale), so its default arg (`0`, itself a doubly-unlucky falsy value) always evaluated to `undefined` — the dropdown showed the "Choose option…" placeholder instead of `0`, matching exactly what the user reported. Selecting an option manually made it "work" by accident afterward (the channel then held a *string* `"12"` instead of the real `number` `12`, which happened to still interpolate correctly into the CSS template literal — masking the type mismatch rather than fixing it). Confirmed this isn't Affix-specific: `Heading`'s `level` (`options: [1, 2, 3, 4, 5, 6]`) has the identical shape, and `Avatar`'s `loading`/`status` options include a literal `undefined` entry (a different edge of the same assumption). Fixed once at the shared source rather than per-component: `ControlField` now stringifies for display/comparison (`value === undefined ? undefined : String(value)`) and, on selection, maps the chosen string back to the *original* option via `options.findIndex`, preserving its real type (number, `undefined`, whatever the option list actually contains) instead of always handing back a string. Live-reconfirmed: `offset` now shows `0` on load (previously blank), and after picking `12` it stays correctly displayed as `12` rather than only working after-the-fact.

  **A third, unrelated real bug found investigating the same "side doesn't work" report — direct user push-back ("is it either or?") on the assumption that this was purely the `.docs-story` trade-off correctly identified a second, independent, genuinely fixable cause.** `Playground`'s `render` always placed the 30-line content `<div>` *after* the `Affix`, unconditionally — correct for `side="top"` (needs trailing content) but structurally wrong for `side="bottom"`, which is the mirror case and needs substantial *leading* content instead (the same geometry already established while building `BottomScrollInteraction`). With content only ever trailing, `side="bottom"` had zero room to ever stick — the header just scrolled away immediately, no matter where it was rendered (Docs page or standalone story alike), which is what actually produced the "entire content behaves as one sticky-together block instead of just the title row" symptom: header and content moved in perfect 1:1 lockstep with scroll, confirmed instrumentally (`affixRect.top` tracking `initialTop − scrollY` exactly, i.e. genuinely un-stuck, not merely visually similar to stuck). Fixed by reordering conditionally on `args.side` (content-then-header for `bottom`, header-then-content for `top`) and dropping the outer wrapper's explicit `height: 150vh` in favor of letting it auto-size to whichever ordering is active — a fixed height that doesn't match the active content arrangement is the exact containing-block mismatch already fixed once on `Affix.tsx` itself, so hardcoding one would have silently reintroduced it for one of the two `side` values. Live-verified on the standalone story (outside `.docs-story`, so genuine CSS engagement is observable): `side="bottom"` now holds `bottom` at the viewport height consistently across scroll positions 0→600 (previously tracked scroll linearly and vanished); `side="top"` reconfirmed unaffected (`top: 0` held across 0→900). This fix is orthogonal to fix (2)/its correction above — it makes `side="bottom"` genuininely stickable at all (previously broken everywhere), independent of whether the embedding context is Docs-page-inlined (still blocked by `.docs-story`, per the correction above) or standalone (works now for both `side` values).

  **The open trade-off above (Controls interactivity vs. real Docs-page sticking) is resolved — built a genuine both/and, rather than picking a side.** After the structural fix (3) made `side="bottom"` stickable at all, the user reported the Docs page's embedded Playground still didn't visually separate header from content on scroll — the `.docs-story` limitation from fix (2)'s correction, still present since `Playground` stayed inlined. Confirmed by reading Storybook 10.5.7's own `IFrameStory` block source directly (`@storybook/addon-docs/dist/blocks.js`): `docs.story.inline: false` builds its iframe's `src` exactly once via `getStoryHref(story.id, { viewMode: "story" })`, with no args embedded and no subscription to any args-change event — live Controls-driven syncing for an `inline: false` story isn't a partially-broken feature, it's a capability Storybook's own implementation simply never built. Built a new shared doc block, `PlaygroundCanvas` (`.storybook/blocks/PlaygroundCanvas.tsx`, exported from `.storybook/blocks/index.ts`), as a drop-in swap for `<Canvas of={X} />` on `Affix.mdx`'s Playground section only: it reads the same live args `PlaygroundControls` already exposes (via the same `usePlaygroundArgs` hook — both independently subscribe to the same story's `STORY_ARGS_UPDATED` channel event, so they stay in sync with each other automatically) and rebuilds the iframe's `src` from them on every change, using Storybook's own args-in-URL format (`key:value;key2:value2`, the same format a story already picks back up from its own URL on load) and keying the `<iframe>` by that same string so React fully remounts (a real reload, not a live patch) whenever a control changes. Only JSON-primitive args are embedded — functions (the `fn()` spy substituted for `onStickyChange`) and objects/refs are silently dropped and fall back to the reloaded story's own `meta.args` default, observably identical for a spy. Trade-off of this approach: a brief reload flash and scroll-position reset on every Controls change, instead of an instant in-place patch — an acceptable, standard cost for getting a genuinely separate document (the only way real page-scroll CSS behavior can work) that still reflects Controls changes at all. Live-verified: the iframe's own `src` now reads `args=children:Header+content;side:bottom;offset:0` after selecting `bottom` in the Controls panel, and scrolling within it holds `bottom` at the viewport height consistently across scroll positions 0→600 — both real sticking and live interactivity confirmed together, the combination neither prior fix could achieve alone. `StickyHeader` keeps its own simpler `docs.story.inline: false` (unchanged) since it never had Controls to preserve in the first place — `PlaygroundCanvas` is deliberately scoped to the one story that actually needs both properties at once, not a blanket replacement for `<Canvas>`.

  **Two final copy/scope fixes, same day, at direct user report — with real sticking finally working everywhere, a genuine mismatched-direction copy bug (and its consistent fix across other stories) surfaced.** The user reported: on `side="bottom"`, the title read "Stuck!" on initial load, flipped to "Scroll Down" once scrolled (which should say "Scroll Up"), and appeared not to flip back to "Stuck!" — plus the same issue on other stories. Investigated the "doesn't flip back" claim rigorously first, since it's a state-correctness claim, not just wording: scrolled `side="bottom"`'s Playground through its full range and back (stuck at 0 → unstuck near the end → **re-confirmed stuck again** scrolling back up), with real screenshots forcing genuine paint at each step (plain `scrollTo` + immediate DOM reads gave stale/incorrect results here too, `document.visibilityState` reading `"hidden"` exactly like the earlier known throttling artifact) — the underlying `data-stuck`/`onStickyChange` mechanism re-engages correctly; concluded **not a real bug**, most likely a fast-scroll perception artifact on the user's own side, not reproduced here despite deliberate effort to catch it. **That conclusion was wrong — see the correction immediately below.** The direction-wording issue **was** real and reproducible: `stuck ? "Stuck!" : "Scroll down"` was hardcoded in `Playground` regardless of `side`, but is backwards for `bottom` — given this demo's leading-only content (per fix (3) above), `side="bottom"` starts *already* stuck and only un-sticks once scrolled past the very end of the page, so the direction that re-engages it is *up*, not down. Fixed in `Playground` with a `side`-aware label (`args.side === "bottom" ? "Scroll up" : "Scroll down"`). For the "other stories" half of the report: `StickyHeader` and `WithinScrollContainer` both still had `side`/`offset` left controllable (inherited from `meta.argTypes`, never explicitly disabled), which meant switching either to `side="bottom"` inherited the exact same "content always trailing the Affix" structural bug `Playground` had before fix (3) — not just wrong copy, genuinely non-functional sticking, since neither story's `render` ever got the conditional-reordering treatment. Rather than replicate that reordering logic a third and fourth time for two stories that were never meant to be side-exploration demos in the first place (`StickyHeader` is the dedicated *static* top-side variant; `WithinScrollContainer` exists to demonstrate `scrollContainerRef`, not `side`), disabled `side`/`offset` controls on both (`argTypes: { side: { control: false }, offset: { control: false } }`, the same pattern `ScrollInteraction`/`BottomScrollInteraction` already established for the identical reason) — `Playground` remains the one place intended for exploring `side`/`offset` combinations.

  **Correction, same day, at direct user follow-up ("it doesn't change back to 'Stuck!' when the title row is sticking to the bottom") — the "doesn't flip back" claim above was real after all, and is a severe, previously-undetected bug in `Affix.tsx`'s own stuck-detection logic, not a Storybook/copy issue.** Re-investigated with the exact repro the user gave (scroll to the very end, confirm unstuck, scroll back up partway, check whether the label re-engages) instead of accepting the earlier round-trip test as sufficient — this time watching the transition zone in fine-grained ~100px steps with a real screenshot forced before every single read (not just at the start and end). Found it immediately: at a scroll position where `getBoundingClientRect()` showed the header genuinely pinned at the viewport's exact bottom edge (`rect.bottom` equal to `window.innerHeight`, confirmed visually too — the header held its on-screen position while content scrolled beneath it across three separate screenshots), `data-stuck` stayed `false` and the label stayed "Scroll up." Root cause, found by instrumenting a second, independent `IntersectionObserver` directly on the same sentinel and logging its raw entries: **`entry.rootBounds` was reported in a different coordinate frame than `entry.boundingClientRect` whenever `Affix` renders inside a nested browsing context** — confirmed with a real captured entry: `rootBounds: { top: 0, bottom: 912 }` (the actual outer browser tab's height, i.e. `window.innerHeight` on the *top-level* window) while the sentinel's own document (Storybook's preview iframe) was genuinely only `572px` tall (`iframe.contentWindow.innerHeight`). Per the IntersectionObserver spec, `root: null` resolves to the *implicit root* — the outermost top-level browsing context — and `rootBounds` is reported in that root's own coordinate frame, while `boundingClientRect` stays in the target's local document frame; the two are simply not comparable whenever there's an intervening iframe boundary (any embedding context — Storybook's own preview iframe here, but the identical shape for a real consuming app that renders `Affix` inside any iframe, e.g. an embedded widget or micro-frontend). This exact bug was in the code from fix (1) above (the original directional-stuck-detection fix) — it just never surfaced for `side="top"`, because a viewport's own *top* edge is `0` in literally any coordinate frame (so the mismatched `rootBounds.top` coincidentally still compared correctly), while `bottom` genuinely differs by frame and is the only edge `side="bottom"` ever checks. Fixed by dropping `entry.rootBounds` entirely and computing the reference edge locally instead: `scrollContainerRef.current?.getBoundingClientRect()` when a real container is provided (a same-document element, so inherently frame-consistent), or `{ top: 0, bottom: sentinel.ownerDocument.defaultView.innerHeight }` for the default viewport case — both guaranteed to share `entry.boundingClientRect`'s own coordinate frame, since neither crosses an iframe boundary to get there. Two new regression tests added (`Affix.test.tsx`, `unit` project: 16→18): one reproducing the exact cross-frame mismatch (a fake entry whose `boundingClientRect` is genuinely past jsdom's real `768px` viewport, paired with a deliberately wrong, much larger `rootBounds` a broken implementation would trust instead), one covering the `scrollContainerRef.getBoundingClientRect()` path directly (previously untested on its own) — both verified to actually fail against the reverted `entry.rootBounds`-trusting code before being trusted, matching this file's own established discipline. Live-reconfirmed the exact original repro: `side="bottom"`, scrolled to the very end (unstuck, "Scroll up"), scrolled back up to the same position that previously stayed stuck-on-"Scroll up" — now correctly reads "Stuck!" **Lesson, worth restating alongside the similar one already recorded for fix (1): a plausible-sounding root cause backed by a passing round-trip test is not the same as having actually traced the failure — the first investigation here stopped at "the mechanism re-engages in *a* test I ran," not "the mechanism re-engages in *the exact scenario the user described*." When a user gives a specific, reproducible repro after being told "not a bug," run that exact repro, at fine granularity, before concluding they're mistaken.**

  **Follow-up, same day, at direct user request ("make sure the Interaction side bottom story is fixed as well") — the fix above lives in `Affix.tsx` itself, so it applies to every consumer automatically, but `BottomScrollInteraction`'s own real-browser test never actually exercised the transition that was broken.** Its `play` function tested "stuck from the start" and "still stuck after scrolling deeper" — never the unstuck-then-re-stuck round trip, which is exactly the shape of the cross-frame `rootBounds` bug (only surfaces once the sentinel has crossed the intersection boundary at least twice — once to un-stick, once to re-stick). Live-reconfirmed manually first (`side="bottom"`, standalone story: scrolled to the very end — un-stuck, "Keep scrolling" — then back up partway — correctly "Stuck!" again), confirming the component-level fix does apply here automatically. Then strengthened the test itself so this exact regression stays caught going forward: added assertions for scrolling to `document.documentElement.scrollHeight - window.innerHeight` (un-stuck, `data-stuck` removed, `onStickyChange` last called with `false`) and then back up into range (`data-stuck="true"` again, `onStickyChange` last called with `true`, position re-confirmed via `getBoundingClientRect()` within the same `<3px` tolerance already established elsewhere in this file). Verified the addition actually fails against the reverted `entry.rootBounds`-trusting code first (per this file's own established discipline) — it does, though in this real-browser test environment the failure surfaced one assertion earlier than in manual testing (the initial un-stick check itself, not only the re-stick one), plausibly because Vitest's own browser-mode test harness nests the story in additional iframe layers beyond what manual Storybook browsing does — irrelevant to the fix's correctness, since the assertion's job is only to fail against the bug and pass against the fix, both confirmed.

  Full self-verification after all fixes this round, whole package: `tsc` (package + `.storybook`), `eslint --max-warnings 0`, `tsup` build, both Vitest projects (`unit`: 820/820, `storybook`: 295/295 — the two new cross-frame/scrollContainerRef regression tests bring `unit` up from 818; `BottomScrollInteraction`'s strengthened assertions add no new test count, same story; `PlaygroundControls`/`PlaygroundCanvas` and the copy/argTypes fixes remain untested Storybook-only tooling/config, matching this folder's existing convention), the component bundle-size check, and the Foundations token-coverage check (79/79, unaffected) all clean. Live-verified: the Docs page's own Playground/Properties/Variants/Accessibility/Related-Components sections, both brand/mode combinations, and mobile-width reflow. **Review pass complete — not marked Finalized here**, per the standing rule that only the user declares a component Finalized.

  **Follow-up feature, same day, at explicit direction after a design discussion — `Affix` now supports a horizontal axis (a sticky lead column in a comparison table, a sticky lead card in a swiper), not just vertical.** Discussed first rather than built straight away: confirmed it was technically possible (the sentinel/`IntersectionObserver` architecture is axis-agnostic), then walked through the API shape as two explicit options — extending `side` to four flat values (`"top"|"bottom"|"left"|"right"`) vs. splitting into `axis: "vertical"|"horizontal"` + `edge: "start"|"end"` (logical, RTL-safe naming) — before building anything, and again confirmed scope (full implementation vs. just recording the decision) before starting, given the size of the change. **This is a breaking rename, not an addition** — `side` (`AffixSide`) is gone entirely, replaced by `axis`/`edge` (`AffixAxis`/`AffixEdge`); acceptable because `Affix` isn't Finalized yet and has no consumers outside this monorepo (checked: no other references to `AffixSide` or `side=` anywhere in `packages`/`apps`), and this codebase's own standing convention (`CLAUDE.md`) is to change the code directly rather than carry a compatibility shim.

  Implementation: `axis="vertical"` keeps physical `top`/`bottom` (unchanged from before, since writing-mode changes — the only thing that would ever need to flip those — are a far rarer concern than RTL, and every other logical-property choice already in this codebase, e.g. `marginBlockEnd`, `borderBlock`, only varies the *block* axis's naming, never its physical direction). `axis="horizontal"` uses the CSS logical properties `insetInlineStart`/`insetInlineEnd` specifically so `edge="start"` means "left in LTR, right in RTL" automatically, the browser resolving it against the element's own computed `direction` — using physical `left`/`right` instead would have silently stuck to the wrong side under RTL. The sentinel gained an axis-appropriate dimension (`sentinelVertical`/`sentinelHorizontal` CSS classes — 1px-tall vs. 1px-wide) plus `flex-shrink: 0` (harmless outside a flex context, needed so it survives inside a horizontally-scrolling flex row instead of being squeezed toward 0 alongside its siblings). Sentinel *ordering* needed no axis-specific logic at all — `edge === "start"` places it before the root, `edge === "end"` after, the same rule for both axes, since DOM order (not axis) is what determines which edge a sibling approaches first.

  The harder half: the stuck-detection math (`entry.boundingClientRect[edge] vs. rootRect[edge]`, from the cross-frame fix above) only worked with *physical* edges, but `getBoundingClientRect()` is always physical regardless of `direction` — so a genuine RTL bug was possible here even though the CSS itself (via logical properties) doesn't have this problem: comparing against the wrong physical edge under RTL would silently detect stuck-ness on the mirror side from the one actually rendered. Added `resolvePhysicalEdge(axis, edge, direction)`, reading `getComputedStyle(sentinel).direction` at detection time and resolving `edge="start"` to physical `right` (not `left`) under RTL before doing the comparison. Verified this specific behavior, not just assumed it: confirmed empirically first that jsdom's `getComputedStyle` *does* correctly resolve `direction` inherited from an ancestor's inline style (a real, checkable jsdom limitation risk, not something to assume) — then added a unit test wrapping `Affix` in a `direction: rtl` ancestor and confirming a sentinel that scrolled past the physical *left* edge (the correct trigger for `edge="start"` under LTR) is correctly **not** reported as stuck, while one that scrolled past the physical *right* edge *is* — verified to actually fail against a naive non-RTL-aware version of `resolvePhysicalEdge` before being trusted.

  New story: `HorizontalScrollInteraction` (`Interaction: axis=horizontal sticks to the leading edge while scrolling`) — a horizontally-scrolling comparison-table-style row via `scrollContainerRef` (page-level horizontal scroll is rare, so this models the realistic case directly rather than the less common one), with the same `data-stuck`-then-`getBoundingClientRect()` two-step verification pattern already established for the vertical stories. `Playground` deliberately does **not** expose `axis` as a live control — its own demo content (stacked vertical text lines) only makes sense for `axis="vertical"`; picking "horizontal" there would leave the Controls panel claiming a horizontal demo while nothing on screen actually scrolled sideways. `StickyHeader`/`WithinScrollContainer`/`ScrollInteraction`/`BottomScrollInteraction` all got `axis`/`edge` added to their existing `argTypes: { control: false }` disabling (same reasoning as the existing `side` disabling before the rename). Not done in this pass, deliberately scoped out: no Docs-page Variants-gallery entry for the horizontal case (matches the existing precedent that `ScrollInteraction`/`BottomScrollInteraction` are sidebar-only interaction-test stories, never surfaced via `<Canvas>` in `Affix.mdx` either) and no dedicated horizontal `edge="end"` interaction story (the unit tests do cover `axis="horizontal" edge="end"` directly; only the real-browser story coverage is `edge="start"`-only, matching the concrete real-world cases actually discussed — a leftmost sticky column, a leading sticky card). Also flagged, not fixed: `Affix` always renders a plain `<div>`, so it can't wrap a real `<table>`'s `<td>`/`<th>` directly for a literal HTML comparison table — noted in both the component's own JSDoc and `Affix.mdx`'s "Don't" callout, since there's no `asChild`/polymorphism support yet.

  Full self-verification: `tsc` (package + `.storybook`), `eslint --max-warnings 0`, `tsup` build, both Vitest projects (`unit`: 826/826, `storybook`: 296/296 — `Affix.test.tsx` alone: 18→24, six new tests: horizontal-axis stuck/unstuck detection in both directions, axis-awareness against a vertical-only crossing, the RTL resolution test, plus two new logical-property style tests; `Affix.stories.tsx`: 5→6 stories, the new `HorizontalScrollInteraction`), the component bundle-size check (Affix: 0.80KB JS / 0.11KB CSS, within budget — grew from 0.52KB/0.08KB with the new axis logic, still comfortably under), and the Foundations token-coverage check (79/79, unaffected) all clean. Confirmed no other package/app in the monorepo referenced the old `side`/`AffixSide` API before removing it. Live-verified: the Docs page's Playground correctly hides `axis` while keeping `edge` live and reloading `PlaygroundCanvas` with the new args on change; the standalone `HorizontalScrollInteraction` story genuinely sticks and un-sticks on real panel scroll, confirmed both by the automated `play` function and by manual scrolling with a real forced repaint (not just a JS read, given this session's own repeated finding about stale reads on a backgrounded pane).

  **Correction, same day, at direct user report — disabling `axis` in `Playground` (above) was more restrictive than necessary; rebuilt it to genuinely support both axes instead.** The user noticed `axis` showing `-` (disabled) in the Docs page's Controls panel and asked directly why it wasn't interactive. The original reasoning (this demo's content — 30 stacked vertical text lines — "only makes sense" for `axis="vertical"`) was true as *stated*, but the fix should have been building the *other* shape too, not disabling the axis that needed it — exactly the kind of "explore the prop combinations" job this section exists for. Rebuilt `Playground`'s `render` to branch on `args.axis`: the vertical case is unchanged (stacked lines, page scroll); the horizontal case renders a horizontally-scrolling row of 10 flex columns inside its own internal `overflow-x: auto` container (`role="region"`/`tabIndex={0}`/`aria-label`, the same WCAG 2.1.1 treatment as `WithinScrollContainer`/`HorizontalScrollInteraction`), with `scrollContainerRef` wired internally (not exposed as a control — a ref has no meaningful Controls-panel representation, matching `WithinScrollContainer`'s own precedent). The `edge`-based leading/trailing content reordering already built for the vertical case needed no new logic — `edge === "end"` still means "content before header," now inside whichever container shape is active. The unstuck label gained the horizontal pair ("Scroll right"/"Scroll left", mirroring "Scroll down"/"Scroll up"'s own logic: the label always names the direction that *re-engages* stickiness, not simply "forward"). Live-verified on the Docs page: switching `axis` to `horizontal` swaps `Playground`'s demo canvas to the column row, `edge`/`offset` remain live on top of that, and scrolling the panel genuinely shows `"Stuck!"`. Full re-verification after this fix: both Vitest projects unchanged at 826/296 (no new automated assertions added — `Playground` has never carried its own `play` function, consistent with it always being the exploratory/manual-verification demo rather than a regression-test story, mirrored by the dedicated `ScrollInteraction`/`BottomScrollInteraction`/`HorizontalScrollInteraction` stories instead), `tsc`, `eslint`, `tsup` build, and the bundle-size check all clean.

  **Full Docs-page pass, same day, at direct user request — reviewed every section of `Affix.mdx` against the newly-added horizontal axis, section by section, rather than assuming the earlier updates (Intro/Playground/Properties/Code examples/Design tokens/Related components) covered everything.** Two real gaps found: (1) the Usage guidelines "Don't" callout was missing the `transform`-driven-swiper caveat — identified verbally when the feature was first discussed (`position: sticky` and `IntersectionObserver` both need a genuine scroll offset; a `transform: translateX()`-animated carousel never produces one) but never actually written into the docs, so a reader building the swiper use case this feature was explicitly motivated by had no way to know about it; added as its own bullet. (2) Best practices' "keep the wrapped content's own height stable across the stuck/unstuck transition" was now wrong for `axis="horizontal"` (the relevant dimension there is width, not height) — generalized to "size along the sticking axis." Also asked directly whether the Intro section should carry the table/swiper caveats itself or stay general — recommended keeping it general (matches this doc's own established pattern: Intro is the elevator pitch, caveats live in Usage guidelines) and left it as already written, rather than changing something that wasn't actually broken. Added a new Variants entry, `### Sticky lead column (axis="horizontal")`, reusing `HorizontalScrollInteraction` directly via `<Canvas>` rather than building a fourth story — confirmed safe first, not assumed: since that story already uses its own internal `scrollContainerRef` (a real, nearer scrolling ancestor than `.docs-story`), it sidesteps the exact `.docs-story` `overflow: auto` containing-block issue that required the whole `PlaygroundCanvas`/`inline:false` treatment for the *page-scroll* stories earlier — the same reasoning `WithinScrollContainer` already relies on for its own existing Variants entry. Live-confirmed the new Variant genuinely sticks when embedded inline in the Docs page (not just standalone). Strengthened the Accessibility section's `scrollContainerRef` bullet to note explicitly that `axis="horizontal"` makes that pattern the common case rather than the exception, pointing at the `role="region"`/`tabIndex={0}`/`aria-label` pattern already used throughout this file's own horizontal stories. Full re-verification: both Vitest projects unchanged at 826/296 (docs-only changes, no new test surface), `tsc` (`.storybook/tsconfig.json`, which typechecks the `.mdx` file), all clean.

  **Final review pass, same day, at direct user request ("let's do a final review of Affix before finalizing it") — worked the full `06-engineering-standards.md` §9 checklist fresh, end to end, rather than trusting the extensive history already recorded above.** Two more real, previously-undetected bugs found and fixed, plus two smaller gaps closed:

  (1) **Theming bug, `PlaygroundCanvas`.** Live-checked Brand/Mode toggling on the Docs page (checklist's own "verified live via the toolbar toggles, not inferred from the code" requirement) and found `Playground`'s embedded demo stayed stuck on Purple/Light regardless of the Docs page's actual selection — a fresh iframe load only ever picks up Storybook's *default* globals, the same "a separate document doesn't inherit the parent's live state" gap `args` already had before its own fix. First attempted fix (`useThemeGlobals`, this repo's own hook for reading live Brand/Mode) read back *stale* defaults even right after toggling — `useThemeGlobals`'s own doc comment already explains why: on any docs page with an attached story, the embedded `<Canvas>` machinery tears down and rebuilds that hook's whole module on every globals change, wiping its cache, which is exactly why `DbmDocsContainer` doesn't rely on it here either. Fixed with the same pattern `DbmDocsContainer` already uses: a synchronous `context.getStoryContext(story).globals` read (no subscription, nothing to lose across a module reload) on the same `story` object `PlaygroundCanvas` already resolves — the same read `usePlaygroundArgs` already does for `.args`, just for `.globals`. Serialized onto the iframe URL as `globals=brand:x;mode:y` and folded into the remount key alongside `argsParam`.

  (2) **The identical bug, `StickyHeader`, but in Storybook's own standard mechanism, not ours.** Checking every Variants entry (not just the one just fixed) found `StickyHeader` — using `docs.story.inline: false`, Storybook's own `<Canvas>` parameter, not `PlaygroundCanvas` — visibly stuck at Purple/Light right next to a correctly-dark `WithinScrollContainer` in the same section. Confirmed via the same `IFrameStory` source read from fix (2) earlier in this file: `getStoryHref(story.id, { viewMode: "story" })` never embeds `globals` either. Rather than build a second, parallel fix for the same root cause, swapped `StickyHeader`'s own Docs-page embedding from `<Canvas>` to `PlaygroundCanvas` (which already handles this correctly) and dropped its now-redundant `inline: false` parameter — `PlaygroundCanvas` needs no live Controls wired up to still fix the theming bug, since `StickyHeader` has none anyway. **This generalizes beyond Affix**: any other component anywhere in this codebase using `docs.story.inline: false` has the identical gap, since it's Storybook's own mechanism at fault, not anything Affix-specific — worth checking for when reviewing any component that uses this parameter.

  (3) **Dev-mode warning gap, closed.** `06-engineering-standards.md` §3's "invalid prop combinations fail loud in development" standard had no enforcement for `axis="horizontal"` without `scrollContainerRef` — a silently-wrong-in-practice combination already documented as a "Don't" but never actually warned about at runtime. Added, matching the established `hasWarnedXRef` + `process.env.NODE_ENV !== "production"` pattern (`Avatar.tsx`'s own precedent) exactly — warns once per instance, not once per render. Two new tests confirm it fires for the gap case and stays silent for both `scrollContainerRef`-provided and default-vertical-axis usage; verified to actually fail without the warning code first.

  (4) **Missing story-level coverage for `axis="horizontal" edge="end"`, closed.** The checklist's "Storybook story covering every variant/size/state combination" line — `axis="horizontal" edge="end"` was the one of four axis/edge combinations covered only by a unit test (a fake `IntersectionObserver` entry), never a real-browser story, an asymmetry easy to miss without checking all four combinations explicitly against each other. Added `HorizontalEndScrollInteraction`, mirroring `BottomScrollInteraction`'s own shape (genuinely leading content, not trailing) on the inline axis via `scrollContainerRef` instead of page scroll, including the same unstuck→re-stuck round-trip assertion the cross-frame `rootBounds` fix above already established as necessary coverage. Live-verified: pins to the right, "Stuck!" shown correctly with leading columns visible scrolled-past on the left.

  **One item verified as far as this session's own tooling allows, not fully confirmed live:** real keyboard-driven scrolling of a focused horizontal scroll panel (arrow keys / mouse wheel) could not be exercised directly — this session's own browser-automation tooling has repeatedly shown unreliable native-input delivery into deeply nested iframes throughout this whole review (the same "Browser pane is currently hidden" timeout already hit several times earlier). Structural correctness confirmed instead: `role="region"`/`tabIndex={0}`/`aria-label` (the exact pattern already established by `WithinScrollContainer`, matching axe's own `scrollable-region-focusable` rule), automated `jest-axe` passing with zero violations, and nothing in `Affix.tsx`'s own source intercepts scroll or keyboard events in any way that could interfere with the browser's native default behavior.

  **Open finding, not resolved — needs the user's call before Finalizing:** `Affix` has no `asChild`/polymorphism support, meaning it can't wrap a real HTML `<table>`'s `<td>`/`<th>` directly for the comparison-table use case this session's own horizontal-axis work was partly motivated by — already documented as a known limitation (component JSDoc, `Affix.mdx`'s "Don't" callout), but `06-engineering-standards.md` §9 lists "`asChild` support where composability matters" under *Baseline correctness*, not the judgment-gated Feature-completeness section, and the already-acknowledged `<table>` gap is a concrete, named case where it plausibly does matter. Not implemented in this pass — flagged instead per the standing "flag gaps instead of improvising" rule, since it's a real scope/architecture decision (Radix `Slot` integration, sentinel-vs-slotted-child interaction, its own mini-pass of JSDoc/tests/a11y-with-`asChild`/Storybook coverage matching the rigor every other prop in this review got), not an obvious final-polish fix.

  Full self-verification after all fixes this round, whole package: `tsc` (package + `.storybook`), `eslint --max-warnings 0`, `tsup` build, both Vitest projects (`unit`: 829/829, `storybook`: 297/297 — `Affix.test.tsx`: 24→27 for the three new warning tests; `Affix.stories.tsx`: 6→7 stories for `HorizontalEndScrollInteraction`), the component bundle-size check (Affix: 1.06KB JS / 0.11KB CSS, within budget), and the Foundations token-coverage check (79/79, unaffected) all clean. Live-verified: Brand/Mode theming across every Variants entry (not just the one first checked), mobile-width reflow for both the vertical Playground and the horizontal Variants entry (contained scroll, no page-level horizontal overflow), and the new `HorizontalEndScrollInteraction` story's real stick/un-stick/re-stick behavior. **Review pass complete — still not marked Finalized**, per the standing rule that only the user declares a component Finalized, and given the open `asChild` question above is exactly the kind of thing worth resolving first.

  **`asChild` support added (2026-08-31), resolving the open finding above at direct user request.** Followed `Button.tsx`'s own established Radix `Slot` pattern exactly (`const Component = asChild ? Slot : "div"`, ref/`{...props}`/computed-attributes ordering preserved so `Slot`'s own prop-merging inherits the same "never let a same-named consumer prop silently override a computed attribute" guarantee). A second prop, `sentinelAs?: "div" | "td" | "th"` (default `"div"`), was added alongside it — not originally scoped, but became necessary once building the concrete motivating use case (a real `<table>`) surfaced a second, independent problem the first prop alone didn't solve (see below). Both kept loosely typed as `ElementType` only at the internal JSX-use point (`sentinelAs as ElementType`, `Box.tsx`'s own precedent for the same narrow-public-type/widened-internal-use split — a plain type *annotation* here still failed with a real `TS2322` ref-type conflict; only an `as` *cast* resolved it, worth remembering if this pattern is reused). Three real, confirmed bugs found and fixed while building the concrete `<table>` proof (`WithinTable` story), each verified against real behavior before trusting the fix, not assumed: (1) **The hidden sentinel's own CSS silently depended on ambient flex-row `align-items: stretch`** for its cross-axis size (`.sentinelHorizontal` only ever set `width`, relying on a flex-row context to give it a real `height` "for free") — this broke the moment the sentinel landed inside a real `<tr>` (`display: table-row`, where a bare element sizes to `display: block` with no implicit height), confirmed directly via `getBoundingClientRect()` reporting `height: 0` — a genuinely zero-area `IntersectionObserver` target, which read `data-stuck="true"` from mount regardless of real scroll position and never changed. Fixed by consolidating the old axis-conditional `.sentinelVertical`/`.sentinelHorizontal` split into one unconditional `.sentinel` class with both `height` and `width` explicitly set to `space.px` — the sentinel's cross-axis size was never actually meaningful to the stuck-detection math either way, so there was no reason to leave it to an ambient layout assumption. (2) **A real, confirmed React HTML-validity/hydration hazard** — even after fix (1), the console showed a genuine dev warning: "In HTML, `<div>` cannot be a child of `<tr>`. This will cause a hydration error," since the sentinel (always a plain DOM sibling of the sticky element, never nested inside it — see `Affix.tsx`'s own longstanding comment on why) still defaulted to a `<div>` even in `asChild` mode. This is what motivated `sentinelAs` as a second prop rather than treating `asChild` alone as sufficient — set to `"td"`/`"th"` to match the row, the sentinel becomes a real, validly-nested cell instead (still genuinely invisible: `aria-hidden`, sized to `space.px`). (3) **A test-content-sizing bug, not an Affix defect** — after fixes (1)/(2), `WithinTable`'s own `play`-function test still failed at the `data-stuck` assertion; a temporary diagnostic `console.log` (removed once done, per this file's own established debugging discipline) revealed the automated Chromium test viewport was wide enough (`panelRect.width: 1104`) that the demo table's original 6 body columns didn't actually overflow its scroll panel, so `panel.scrollLeft = 300` silently clamped back to `0` — genuinely nothing to scroll to. Fixed by widening the demo to 12 columns, matching `HorizontalScrollInteraction`'s own already-proven overflow margin. New unit tests added (`Affix.test.tsx`, `describe("asChild")`, 8 tests): slotted-element-type rendering, `data-stuck`/className/style merging via `Slot`, ref forwarding to the real slotted element (not a wrapper), `sentinelAs` rendering the sentinel as the given tag (and defaulting to `div` when omitted), and a dedicated `jest-axe` check against `asChild` rendered onto a non-default element inside a real `<table>` — per this project's own polymorphic-component checklist item, not just the default-element case (`Avatar`'s `role="img"` + `as="button"` finding is the cited precedent for why this matters). `Affix.mdx` updated: the now-factually-wrong "Don't expect `axis="horizontal"` to work inside a real `<table>`'s `<td>`/`<th>` directly" callout replaced with the correct `asChild`+`sentinelAs` guidance and its own two new "Don't" caveats (`sentinelAs` still needed even with `asChild`; `children` must be a single valid element since `Slot` requires exactly one); a new `## Variants` entry ("asChild (sticky lead column in a real `<table>`)") embeds `WithinTable` directly (safe to inline the same way `HorizontalScrollInteraction` already is, since it drives its own local `scrollContainerRef` rather than page-level scroll); a new Code example demonstrates the pattern; the `space.px` token-usage row corrected to reflect the fix-(1) CSS change (no longer axis-conditional). Full self-verification, whole package: `tsc` (package + `.storybook`), `eslint --max-warnings 0`, `tsup` build, both Vitest projects combined (100 test files, 1133/1133 — `Affix.test.tsx`: 25→33 unit tests), the component bundle-size check (Affix: 1.06→1.08KB JS / 0.11→0.10KB CSS, still comfortably within budget), and the Foundations token-coverage check (79/79, unaffected) all clean. Live-verified in a real Chromium tab, not just the automated suite: the `WithinTable` story scrolled to a real `<th>` correctly reporting `data-stuck="true"` with `cellLeft≈panelLeft` geometry (screenshot-confirmed), zero hydration/HTML-validity console warnings post-fix (confirmed absent both on the standalone story and embedded via the Docs page's own inline `<Canvas>`), and the pre-existing "Accessing element.ref was removed in React 19" warning confirmed as a known, pre-existing `@radix-ui/react-slot`/React 19 compat warning already present on `Button`'s own (finalized) `asChild` Docs page — not something this change introduced. **Review pass complete — still not marked Finalized**, per the standing rule that only the user declares a component Finalized.

  **Real, confirmed bug found and fixed in `WithinTable` itself, same day, via direct user question ("in what scenario would only the `th` becoming sticky?").** The story's own name ("Sticky lead column") promised more than it delivered — only the header `<th>` was ever wrapped in `Affix`; the five body `<td>`s in that same column had no sticky positioning of their own at all, so scrolling right left the header pinned while each row's own "Row N" label scrolled away underneath it, breaking the header-to-data correspondence the whole pattern exists to preserve. There is no legitimate scenario for this in a genuine multi-row data table — a header-only-sticky lead column is actively confusing, not a valid variant. Root cause: `position: sticky` is a per-*element* property, not a per-*column* one — there's no single declaration that freezes an entire `<col>`, and `Affix` itself only ever wraps one element. Fixed by keeping only the header `<th>` wrapped in `Affix` (the column's one real `IntersectionObserver`/sentinel, still the correct place for stuck *detection*) and applying the identical `position: sticky` + `insetInlineStart: 0` + `var(--dbm-z-index-sticky)` directly to every body `<td>` in that column via a shared inline style object, driven by the same `stuck` boolean the header already tracks — deliberately not a second `Affix`/sentinel per row, since every cell in the column crosses the sticky edge at the exact same scroll position as the header, making a per-row observer purely redundant work re-deriving an already-known answer. `z-index` matters here and wasn't optional: without it, the non-sticky columns scrolling past would paint over the sticky column instead of under it once their content visually reached the same on-screen position (confirmed by the header cell's own already-passing z-index, inherited for free from `Affix`'s own `.root` class via `Slot`, which the manually-styled body cells don't get automatically). New test coverage: the `play` function now also asserts the first body row's own lead cell (`data-testid="table-lead-body-cell"`) stays pinned in lockstep with the panel's left edge while scrolled — not just the header — specifically because the header's own existing assertions couldn't have caught this class of regression (a broken body-cell `insetInlineStart` would still leave the header behaving perfectly correctly on its own). `Affix.mdx`'s Best practices section gained a new bullet generalizing this pattern (one `Affix`-wrapped detector per sticky column, plain CSS for the rest of that column's cells) so a future consumer doesn't have to rediscover it by trial and error the way this story originally shipped it. Live-verified in a real Chromium tab: scrolled 300px, all five "Row N" labels visibly stayed pinned alongside "Stuck!" instead of scrolling away. Full self-verification: `tsc`, `eslint --max-warnings 0`, and the `storybook` Vitest project scoped to Affix (8/8, including the new body-cell assertion) all clean.

  **A second, unrelated real structural bug found in the same story, same day, via a follow-up direct user report with a screenshot.** The header row and body rows had different numbers of "real" columns before their first `Metric N` cell: the header put one Affix-wrapped cell (labeled "Stuck!"/"Scroll right", not a metric) at column 0 and `Metric 1..12` at columns 1–12, while the fix above had already made the body's own column 0 the "Row N" label — column *positions* lined up (13 cells each), but what occupied column 0 read as a state indicator on the header side and a row label on the body side, and `Metric 1`'s header (column 1) sat over what was actually the row's *second* data point, not its first — confirmed exactly matching the user's own screenshot ("Metric 1" visually adjacent to "1-2"/"2-2" data, one column off from where its label implied). Restructured to match what the user asked for directly: column 0 is now a plain, non-sticky, fixed-width "Row" column (new `rowLabelCellStyle` — explicit `width: "6rem"` plus `whiteSpace: "nowrap"`, since without it "Row 1" wrapped onto two lines, confirmed in the reported screenshot); column 1 ("Metric 1") is now the sticky column instead of column 0, carrying both the `Affix`-wrapped header `<th>` and the fix-above's plain-CSS sticky treatment on its own body `<td>`s (renamed `leadCellStyle` → `stickyMetricCellStyle` to reflect what it now actually targets); columns 2–12 render `Metric 2..12` unchanged in shape, just shifted one index. The header `<th>` text is now always the literal `Metric 1` (never conditionally "Stuck!"/"Scroll right") — the stuck/unstuck visual distinction is carried entirely by the existing border-color swap (already how every body cell in the column indicates it), so the column's own label stays accurate and stable regardless of scroll state, matching how a reader would actually expect a data table's header to behave. Confirmed live post-fix in a real Chromium tab: at `scrollLeft: 0`, `Metric 1`'s header sits directly above `1-1`/`2-1`/.../`5-1`, `Row 1..5` render on one line each; scrolled to 300px, the `Row` column scrolls fully out of view while `Metric 1` (header text unchanged, `data-stuck="true"`) pins flush to the panel's left edge with its own column's data still directly beneath it. `data-testid="table-affix-cell"`/`"table-lead-body-cell"` kept their existing names (still accurate — "the sticky cell", regardless of which column now holds it), so the `play` function's existing assertions needed no logic changes, only the surrounding markup they target. Full self-verification: `tsc`, `eslint --max-warnings 0` (one new `react/no-unescaped-entities` catch on straight `"` quotes added to the intro copy, fixed with `&quot;` matching this file's own existing `&apos;` pattern), and both Vitest projects combined (100 test files, 1133/1133, including the unchanged-but-reverified `storybook` project's Affix suite at 8/8) all clean.

  **A third, unrelated real structural bug found in the same story, same day, via a follow-up direct user report with a screenshot showing a visible blank cell.** Even after the fix above correctly reassigned the sticky column to `Metric 1`, the header row still had one MORE real `<th>` than every body row had `<td>`s, throwing every column after it off by one again — a different root cause from either fix before it. `sentinelAs="th"` makes Affix's own hidden stuck-detection sentinel a genuine sibling `<th>` inside the header `<tr>` (`Affix.tsx` always renders `{sentinel}{root}` as fragment siblings, `edge="start"`'s default putting the sentinel *before* the slotted content) — a real table column of its own, since browsers align table columns purely by cell *index* per row, with no concept of a "skipped" or annotation-only cell regardless of how it's styled (`aria-hidden`, 1px wide). The header row was therefore genuinely 14 cells (`Rows`, sentinel, `Metric 1..12`) against every body row's 13 (`Row N`, `N-1..N-12`) — confirmed exactly matching the reported screenshot: a visible blank gap between "Row"/"Row N" and "Metric 1"/"1-1", with "Metric 1" sitting above what was actually `Metric 2`'s own data one column over. This is a genuinely non-obvious, easy-to-reproduce gotcha for *any* consumer combining `sentinelAs="td"`/`"th"` with a multi-row table, not specific to this story's own prior mistakes — worth real documentation, not just a silent story fix. Fixed by adding a plain `aria-hidden` placeholder `<td>` (matching the sentinel's own `space.px` footprint, not itself sticky) to every body row at the same column index the header's sentinel occupies — column counts now match (14/14), confirmed directly via DOM inspection post-fix, not just visually. Also renamed the row-label header from `"Row"` to `"Rows"` (direct user request) and lightly revised the intro copy's phrasing for accuracy given the now-corrected structure. **`Affix.mdx`'s "Don't" callout gained a new bullet for this specific gotcha** (the sentinel occupies a real column; every other row needs a matching placeholder cell at that index) — this is exactly the kind of structural trap a future consumer would otherwise have to rediscover from scratch, the same reasoning that already justified the two "Do"/"Don't" additions from the two fixes before it. Confirmed live post-fix in a real Chromium tab (`querySelectorAll('thead th')`/`querySelectorAll('tbody tr')[0] td` both returning exactly 14 cells, in the correct `Rows, [sentinel], Metric 1, Metric 2, …` / `Row 1, [placeholder], 1-1, 1-2, …` order) and via screenshot both at `scrollLeft: 0` and scrolled to 300px (`Metric 1` pinned flush left with its own `1-1`/`2-1`/… data directly beneath it, `Rows` column fully scrolled out of view). Full self-verification: `tsc`, `eslint --max-warnings 0`, both Vitest projects combined (100 test files, 1133/1133, `storybook` project's Affix suite reconfirmed at 8/8) all clean.

  **Intro copy extended, same day, at direct user request following a clarifying conversational Q&A about the demo's own two sticking mechanisms.** The user asked directly whether the `Metric 1` header and its own body cells become sticky "in a different way" — confirmed yes (header: real `Affix`/`asChild`, CSS class + computed inset + genuine `IntersectionObserver`; body cells: hand-written `position: sticky` reusing the header's already-detected `stuck` state, no observer of their own) — then asked whether this distinction belongs in the story's own intro paragraph, since the story exists specifically to demonstrate `asChild`'s composability mechanics for a developer audience, not just to look at a sticky column. Agreed and added one sentence to the intro `<Text>` stating only the header cell is `Affix`-wrapped and why the body cells get the same CSS by hand instead of their own redundant sentinel — kept to one sentence, matching this paragraph's existing length rather than growing it into a wall of text. No code/behavior change, prose-only. Full self-verification: `tsc`, `eslint --max-warnings 0` (no new unescaped-entity issues — used `&apos;`/`<code>` consistently with the rest of the paragraph), both Vitest projects combined (100 test files, 1133/1133) all clean; live-confirmed the added sentence renders correctly with `Affix`/`position: sticky` in `<code>` formatting, no layout regression to the table demo below it.

  **Finalized 2026-08-31**, at direct user request — per the standing rule established throughout this whole file, don't make further changes to Affix (code, stories, docs, or its tokens) without asking first. All open items from the review are resolved: the `asChild`/`sentinelAs` gap that blocked Finalizing before is built, tested, and documented; the three real structural bugs found building and refining the `WithinTable` demo (sentinel zero-height, `<div>`-in-`<tr>` hydration hazard, sentinel-as-extra-table-column) are fixed with regression coverage; `Affix.mdx` reflects the current, correct behavior throughout (Properties, Variants, Best practices, Don't callouts).
