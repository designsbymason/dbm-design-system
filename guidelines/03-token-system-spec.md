# DBM Design System — Token Spec

Companion to `packages/tokens/src/` (the actual generated token files in W3C Design Tokens Community Group format — ready to feed into Style Dictionary and, later, the Tokens Studio Figma plugin for design-file sync).

## Architecture: 3 layers

1. **Primitive** (`primitive/*.json`) — raw values, no meaning. Color scales, spacing steps, radii, shadows, etc.
2. **Semantic** (`semantic/*.json`) — contextual meaning, references primitives via `{color.purple.600}`-style aliases. **One file per theme** (brand × mode). This is where multi-brand and light/dark actually live — the token *names* (`bg.brand`, `text.primary`) stay identical across every theme file; only what they resolve to changes.
3. **Component** (`component/*.json`, populated starting 2026-08-12) — component-scoped tokens for values a specific component needs that don't belong in the shared primitive/semantic scales. See "Component-layer tokens" below.

Status/neutral tokens (red, amber, green, blue, gray) are **shared across both brand themes** — success is always green, danger is always red, regardless of which brand is active. Only the "brand" slots (bg.brand, text.link, border.focus, icon.brand) swap per theme. This is a deliberate consistency principle, not an oversight — status meaning shouldn't be reinterpreted by theme.

## What's in each primitive file

| File | Contents |
|---|---|
| `color.json` | 11-step (50–950) scales for `purple`, `emerald`, `gray`, `red`, `amber`, `green`, `blue`, plus `neutral.white`/`neutral.black` |
| `typography.json` | Font families (Nunito primary, Lora secondary, mono system stack for Code/Kbd — added Phase 4.5 continuation, no extra font loading), weights, fluid font-size scale, line-heights, letter-spacing |
| `spacing.json` | 4px-base numeric scale, `0` through `32` (=128px) |
| `radius.json` | **Redefined 2026-07-27** to increment in step with `spacing.json` rather than its own curve: `none`(0) → `xs`(2) → `sm`(4) → `md`(8) → `lg`(12) → `xl`(16) → `2xl`(24) → `3xl`(32) → `full`(9999, pills/circles, deliberately outside the linear ladder) |
| `shadow.json` | Layered elevation shadows, **separate light/dark values** — dark-mode shadows are higher-opacity black, not a naive inversion, and should pair with a subtle border on raised surfaces |
| `breakpoint.json` | `sm`(640) → `3xl`(1920, for enterprise dashboard monitors) |
| `motion.json` | Duration scale (120ms–600ms) + 5 easing curves — `standard`/`decelerate`/`accelerate`, an "emphasized" expo-out curve for premium micro-interactions, and `linear` (added 2026-08-16, Skeleton's wave shimmer — a cubicBezier token equivalent to the CSS `linear` keyword, added so a constant-rate curve goes through the same build pipeline as the others instead of being hardcoded) |
| `other.json` | Icon sizes (paired with Phosphor, 12–48px), border widths, opacity scale, z-index layering scale |

## Color scale generation

**Re-derived in OKLCH in Phase 4 (2026-07-18)**, replacing the original HSL-based generation. Scales are generated by `packages/tokens/scripts/generate-color-scales.mjs` using `culori` (MIT-licensed, devDependency scoped to `packages/tokens` — never shipped to consumers): an 11-step OKLCH lightness curve, shared across all 7 scales, gives perceptually even steps (unlike HSL lightness, which isn't perceptually uniform — this is what caused the pre-Phase-4 `text.success` AA failure below). Chroma tapers toward both ends of each scale, gamut-clamped via `culori.clampChroma` so every generated value is a valid, displayable sRGB hex.

- **`purple-600` (`#5548A4`) and `emerald-600` (`#2E8A7D`) stay exact** — the two brand-mandated anchors (`01-vision-and-goals.md`'s decisions log). Every other step in those two scales is the shared canonical L curve shifted by a constant offset so it passes exactly through the anchor's own OKLCH `L` at 600, preserving the curve's spacing while still hitting the required color.
- **`gray`, `red`, `amber`, `green`, `blue` regenerate fully** — no anchor requirement, so every step (including 600) comes from the canonical curve directly, using each scale's previous 600 value only to pick a matching hue so the scale keeps its color identity (red stays red, etc.).
- Re-run the script whenever a scale needs re-deriving — e.g. adding a 3rd brand theme reuses the same anchored-scale logic with a new brand hex.

**Every contrast pairing changed as a result** (75 of 77 primitive values shifted) and was re-verified from scratch — see the running log below.

## Fluid typography

Built with the standard "Utopia" clamp() formula: two type scales (ratio 1.2 at 320px viewport, 1.25 at 1440px viewport) interpolated with `clamp(min, preferred, max)`. One deliberate deviation from a textbook fluid scale: **sizes at/below the base step (`xs`, `sm`, `base`) are fixed, not fluid** — scaling small UI text down further on large screens adds nothing and risks going under comfortable reading size. Fluid scaling kicks in from `md` upward, where it actually creates a meaningfully improved heading hierarchy across screen sizes.

Note: `font-size-xs` originally computed to ~11px, below the common 12px practical floor for body-adjacent text. **Fixed in Phase 2** — floored at `0.75rem` (12px exactly).

## Contrast verification (done, not assumed)

I ran actual WCAG contrast checks on the semantic pairings rather than eyeballing them. Running log, in the order each was found:

**Phase 1 planning:**
- **White text on `emerald-600` (light mode):** 4.16:1 — fails normal-text AA (needs 4.5:1). **Fixed** by moving the light-mode `bg.brand` solid-fill token to `emerald-700` (4.55:1, passes).
- **White text on `emerald-500` (dark mode):** 2.39:1 — fails badly. **Fixed** by using dark text (`gray-950`) instead of white for `text.on-brand` in the emerald dark theme (6.92:1, passes).

This is why `text.on-brand` is defined per-theme rather than as a single global "white" constant — the right foreground color for a solid brand fill depends on how light that specific brand color is, and emerald sits at a very different perceptual lightness than purple even at the same numeric step.

**Phase 3 (building Button/Badge/Avatar surfaced more gaps — the token set wasn't as complete as the Phase 1 pass assumed):**
- **No `text.on-danger` existed** (only `text.on-brand`, calibrated for brand colors specifically). Computed directly: white on `red.600` (light themes) = 6.12:1, white on `red.500` (dark themes) = 4.74:1 — both pass AA. Added to all 4 themes.
- **No `bg.danger-hover` existed.** Mirroring `bg.brand-hover`'s dark-mode-lightens pattern (`red.400`) only gives white text 3.24:1, failing AA. Used `red.600` instead (darker, breaking the brand-hover directionality convention specifically because `red.400` isn't safe here) — 6.12:1. Light-theme hover (`red.700`) mirrors the brand pattern fine at 8.1:1.
- **Real pre-existing bug:** `text.success` on `bg.success-subtle` was **4.08:1, failing AA** — shipped in Phase 2 undetected, a symptom of green's steps not being as evenly spaced as red's under the HSL-derived scales (see the OKLCH re-derivation item below). Fixed by moving `text.success` from `green.700` to `green.800` (6.01:1) in both light themes; dark themes were already passing.
- **No neutral/offline status-tone fill existed** (Avatar's offline status dot was reusing `border.neutral`, a border-purposed token, as a fill — and that reuse also failed WCAG 1.4.11 non-text contrast: 2.74:1 light / 2.36:1 dark against `bg.surface`, needs 3:1). Added `bg.neutral` = `gray.500`, which uniquely passes both light (4.24:1) and dark (3.12:1) against `bg.surface` — a single shared value across all 4 themes.

**Post-Phase-3 review pass (auditing for workarounds before Phase 4 turned up two more real gaps):**
- **`text.tertiary` failed AA as text:** it's used both as a non-text icon color (Input's affix icons, 3:1 floor — fine at the original `gray.500`) and as literal text color (`Text` component's `color="tertiary"`, `Divider`'s label, `Input`'s placeholder — 4.5:1 floor). The original `gray.500` was 4.24:1 light / 3.12:1 dark against `bg.surface`, failing the text bar despite passing the icon bar. Fixed: `gray.600` for light themes (5.60:1), `gray.400` for dark themes (4.82:1) — both still correctly more muted than `text.secondary`.
- **Badge's solid variant needed a fuller token set than `text.on-danger` alone:** computed `text.on-info` (white, 6.01:1/4.56:1, passes as one shared value), `text.on-success` and `text.on-warning` (white fails badly on both — 2.33–3.30:1 — used `gray.950` instead, 5.02–7.99:1, matching the dark-text precedent from `text.on-brand`'s emerald-dark override), and a dedicated `bg.neutral-strong` (`gray.600`) + `text.on-neutral` (white, 5.60:1) since `bg.neutral` (verified only for the 3:1 non-text case) fell short of 4.5:1 with either white (4.24:1) or dark (3.9:1) text — the least accessible possible host for text, sitting near the perceptual midpoint of the gray scale.

**Note (`bg.neutral`/`bg.neutral-strong` superseded, 2026-08-21 — see the dedicated phase below):** the two tokens introduced in these two bullets were later merged into one — the original `bg.neutral` (`gray.500`, this bullet above) was retired, and `bg.neutral-strong` (`gray.600`, this bullet) was renamed to `bg.neutral`, taking over both roles. Left as accurate history above rather than rewritten in place (unlike this document's usual pure-rename convention, e.g. Phase 21/32) specifically because the *new* `bg.neutral` and the *old* `bg.neutral` named here are two different values — rewriting this bullet to say "`bg.neutral`" throughout would make it describe the wrong color entirely, not just use an outdated name for the same one.

**Phase 4 (OKLCH re-derivation, 2026-07-18) — full re-verification, since 75 of 77 primitive values changed:**
- Re-ran every pairing above plus a systematic sweep (25 pairings × 4 themes = 100 checks: all `text.*`/`bg.*` combinations, `bg.neutral`, `border.focus`, `icon.default`, `icon.brand`). 6 categories of regression found and fixed:
  - `text.on-success` / `text.on-warning`: white/gray.950 no longer cleanly passed against the regenerated `green.600`/`amber.600`. Light themes: `bg.success` moved to `green.700` (white text, 6.50:1); `text.on-warning` moved to white (4.78:1 against unchanged `amber.600`). Both tokens are now **theme-specific** (light = white, dark = gray.950) rather than shared.
  - `text.on-danger` / `text.on-info` (dark themes): white against the regenerated `red.500`/`blue.500` dropped to 3.48:1/3.26:1. Fixed by making both **theme-specific** (dark = gray.950: 5.22:1/5.56:1; light keeps white).
  - `bg.danger-hover` (dark themes): now `red.400` (lighter than base, restoring the normal dark-mode "brighten on hover" direction) instead of the Phase 3 `red.600` — that darker value was only needed because dark-mode `text.on-danger` used to be white; now that it's gray.950, red.400 works and the more intuitive hover direction is restored.
  - `bg.brand-hover` (purple-dark only): `purple.400` dropped to 3.41:1 with white text. Moved to `purple.600` (darker, 7.37:1) — deliberately breaks the "lighten on hover in dark mode" convention here, same class of exception as `bg.danger-hover`'s original Phase 3 fix.
  - `text.link`: purple-dark's `purple.400` (4.13:1) → `purple.300` (5.80:1); emerald-light's `emerald.600` (4.16:1, the brand anchor) → `emerald.700` (6.08:1). The emerald one is a **pre-existing gap unrelated to the regen** — same anchor hex before and after Phase 4, it just hadn't been checked as literal text color until this sweep.
  - `border.focus` (emerald-light only): `emerald.500` fell to 2.92:1 against the 3:1 non-text floor. Moved to `emerald.600` (4.16:1).
  - `icon.secondary` (light themes): found failing 3:1 (`gray.400` = 2.32:1) while auditing the remaining icon tokens — not a regression (this token isn't referenced by any shipped Phase 3 component yet) but a real gap, fixed pre-emptively. Moved to `gray.500` (3.25:1).
- All fixes verified against the full 100-check sweep plus a Storybook visual pass (both brands × both modes) before merging. See individual `$description` fields in each `semantic/*.json` file for exact per-token numbers.

**Phase 4.75 (comprehensive atom completion, 2026-07-26):**
- **New token, deliberately exempt from contrast checking:** `bg.overlay` (added for the `Backdrop` atom — the dimming scrim behind Dialog/Drawer/overlays), shared across all 4 themes as `{color.neutral.black}`. No text is ever rendered directly on it, so it isn't a WCAG contrast pairing in the way every other `bg.*`/`text.*` combination above is — opacity is applied compositionally at the component layer via the existing `opacity.*` scale rather than baked into the token, keeping color and opacity in their own separate token categories everywhere.

**Phase 5 (Foundations Color page review, 2026-07-27) — anchored-scale light end read as gray, not a light tint:**
- **Root cause:** `purple-600`'s OKLCH `L` (0.46) sits well below the canonical curve's own 600 midpoint (0.56), so the constant `lOffset` used to shift the rest of purple's/emerald's steps dragged purple's light end down toward mid-gray territory too. Confirmed empirically — user-flagged (screenshot comparison against `red`/`green`/`blue`'s 50–200, which are unshifted free scales and read as clean light tints).
- **First fix:** `purple`/`emerald` steps 50/100/200 made fully unshifted (use the canonical `L` directly, like the free scales). Fixed 50–200, but left a real discontinuity: the 200→300 `L` gap became 0.171, nearly double every other gap in the scale (~0.09) — a hard on/off cutoff rather than a smooth curve.
- **Follow-up (same day):** user separately flagged `purple.300` as still reading gray. Root cause was that discontinuity, not a new issue — 300 sat perceptibly darker than an evenly-spaced curve would put it. **Real fix:** replaced the on/off cutoff with `OFFSET_BLEND`, ramping the anchor's `lOffset` linearly from 0 at step 200 to 1 (full offset) at step 600, instead of snapping it on at 300. Restores an even `L` curve (gaps now 0.088–0.114 throughout, no cliff) while keeping 50–200 unshifted and 600 exact.
- Fixed at the generation-script level (`packages/tokens/scripts/generate-color-scales.mjs`), not by hand-editing `color.json`, so the fix survives regeneration.
- New purple 50–500: `#F9F9FF`, `#EFEFFC`, `#DBDAFC`, `#B9B7EF`, `#9691E0`, `#746BC6` (600 anchor `#5548A4` unchanged; 700–950 unchanged). Emerald 50–500 shifted by the same negligible-to-small amount as a side effect of the same code path.
- Re-verified every real consumer of the changed steps: `text.link` (purple-dark, `purple.300`) improved 5.80:1 → 7.45:1; `border.focus` (purple-light, `purple.500`) still comfortably passes at 4.98:1 non-text; `text.link` (emerald-dark, `emerald.400`) still passes at 6.49:1. No regressions, all still AA+.

**Phase 6 (`bg.canvas`/`bg.neutral-subtle` swap + new tokens, 2026-07-27):**
- `bg.canvas`/`bg.neutral-subtle` swapped in both light themes (`gray.100`/`gray.50`, was `gray.50`/`gray.100`) at the user's explicit request: canvas should be the more visibly distinct page-level gray so white surface cards pop off it; subtle should be the barely-there tint closest to white, used for hover fills/zebra striping on those cards. Dark mode intentionally left unchanged (`canvas` = `gray.950`, `subtle` = `gray.800`) — tried mirroring with `subtle` at `gray.700` to also fix a pre-existing `subtle`/`surface-raised` collision (both `gray.800`), but that failed AA for the real `text.secondary`-on-`bg.neutral-subtle` pairing (Tag/Badge `subtleNeutral`): 4.00:1, needs 4.5:1. `gray.600` is worse (2.73:1). No primitive step exists between 800 and 900 to fix the collision without breaking contrast, so dark `subtle` stays at `gray.800` — a real but lower-priority visual nit, not a WCAG failure.
- **`bg.warning-hover` / `bg.success-hover` / `bg.info-hover` added** (previously only `brand`/`danger` had hover states) — same one-step-darker (light) / one-step-lighter (dark) pattern as the existing hover tokens. All verified AA-safe: light themes 6.93–9.56:1 (white text), dark themes 7.76–8.23:1 (gray.950 text).
- **`bg.overlay` re-verified, not changed:** user asked whether it should carry opacity. It's correct as-is — opacity is composited separately at the component layer (`Backdrop.tsx`'s `opacity` prop, defaulting to `60` i.e. `var(--dbm-opacity-60)` = 0.6), not baked into the color token, matching the token's own `$description`. Confirmed via the component source and its tests (`Backdrop.test.tsx`); the Foundations page's raw-swatch rendering (opaque black) doesn't reflect the composited runtime result, which is what caused the question.
- **`text.brand` and `border.brand-subtle` added.** Originally requested as literal `brand.600` / `brand.100` in every theme; both failed real contrast checks in most theme/mode combos (`text.brand`=600: 1.92:1 dark-purple, 4.16:1 emerald-light, 3.39:1 dark-emerald, all failing the 4.5:1 floor; `border.brand-subtle`=100: 1.14–1.13:1 in both light themes, failing the 3:1 non-text floor badly). Flagged to the user, who initially chose to reuse the already-verified precedents instead: `text.brand` took the same resolved values as `text.link` per theme/mode; `border.brand-subtle` took the same resolved values as `border.focus`.
- **`border.brand-subtle` revised same day** — user overrode the contrast-driven value and asked for the literal `brand.100` in every theme after all, on the basis that this token is for decorative UI accents rather than a border that identifies a component/state boundary, so WCAG 1.4.11 (which governs borders/graphics required to identify UI components and states) doesn't apply the same way here. Now `purple.100`/`emerald.100` literally in all 4 themes (light: 1.13–1.14:1, deliberately below 3:1; dark: 12.38–12.46:1, far above it as a side effect of primitive.100 sitting near-white against a near-black surface). `border.focus` and `border.danger` remain contrast-verified per-theme, since those genuinely are state-identifying borders under 1.4.11. `text.brand` was not revisited and still uses the `text.link`-matched, contrast-verified values.

**Phase 7 (`bg.canvas` moved lighter in light mode only, 2026-08-08):**
- `bg.canvas` moved from `gray.100` to `gray.50` in both light themes, at explicit user direction, reopening the Phase 6 canvas/subtle swap: `bg.neutral-subtle` is already `gray.50` in light mode, so this makes `bg.canvas` and `bg.neutral-subtle` byte-identical — an accepted tradeoff, confirmed with the user before applying (flagged that it undoes Phase 6's stated rationale for the swap), not an oversight or a missed collision.
- Dark mode's `bg.canvas` was briefly moved from `gray.950` to `gray.900` (mirroring the light-mode move, landing on the same value as `bg.surface`) the same day, then reverted back to `gray.950` at the user's explicit follow-up instruction. Dark mode's `bg.canvas` therefore nets out **unchanged** — still the scale's terminal/most-extreme dark step, still distinct from `bg.surface`. Only light mode actually changed.
- Contrast re-verified for the real pairing this token has (Storybook manager chrome's sidebar text, via `theme.ts`'s `appBg`/`textColor`/`textMutedColor` — `bg.canvas` itself is a page-background-only token, not referenced by any published component): light mode `text.primary`/`text.secondary` on the new `gray.50` measure 13.53:1 / 6.59:1 (were 12.41:1 / 6.05:1 on `gray.100` — improved, since a lighter background only helps contrast against dark text). Dark mode's pairing is unaffected, reverting to its already-verified `gray.950` values (17.38:1 / 10.55:1).

**Phase 8 (`bg.code`/`text.code`/`border.code` added, 2026-08-08):** Found while auditing the Foundations pages after the Phase 7 `bg.canvas` change: `docs.css`'s `.dbm-tokentable code` pill (the "Token" column in the Breakpoints/Miscellaneous reference tables) used `bg.neutral-subtle`/`text.primary`, which visually blended into the table's own zebra stripes once `bg.canvas` moved to equal `bg.neutral-subtle` in light mode — the pill lost its distinctness. User asked for a blue-tinted pill instead (`blue.50` background, `blue.100` border, `text.secondary`) — the *original* treatment this exact pill had before an earlier same-day fix moved it away from blue, because a *fixed* `blue.50` paired with *reactive* `text.secondary` measures only 1.64:1 in dark mode.
- Rather than reintroduce that fixed/reactive mismatch, added three new **reactive** semantic tokens — `bg.code`, `text.code`, `border.code` — instead of pairing a fixed primitive with a reactive token again. Values mirror `bg.info-subtle`/`text.info`'s exact, already-verified lightness-curve pairing (`blue.50`/`blue.700` light, `blue.950`/`blue.300` dark) under new names, since a plain code/token reference isn't a status and reusing the `info` tokens directly would misleadingly imply one (same reasoning `docs.css` already documented for why `blue-50` was never promoted to `bg.info-subtle` for this purpose in the first place). `border.code` added as a fourth companion token (`blue.100` light / `blue.900` dark).
- Shared across all 4 themes (brand-independent), matching every other blue/gray-family token in this system.
- Contrast: `text.code` on `bg.code` measures **6.63:1 light / 10.51:1 dark** (identical to the already-verified `text.info`/`bg.info-subtle` numbers, since the values are the same). `border.code` on `bg.code` measures 1.09:1 light / 1.28:1 dark — well under the 3:1 WCAG 1.4.11 floor, but accepted since this is a purely decorative pill border, not one that identifies a component/state boundary (same accepted class as `border.brand-subtle`).
- Not currently consumed by any published component — the real `Code.tsx` atom uses `bg.neutral-subtle`/`text.primary` (see `Code.module.css`), unaffected by this change. `bg.code`/`text.code`/`border.code` are Storybook-docs-only for now (`docs.css`'s `.dbm-tokentable code`), though nothing prevents `Code.tsx` from adopting them later if the two are meant to look the same.

**Note (`bg.code` briefly superseded, then restored):** retired 2026-08-20 in favor of `bg.info-subtle`, at explicit direction — then restored the same day, reversing that retirement, also at explicit direction. See Phase 29 and the dedicated reversal phase after it below for both halves of this. `text.code` and `border.code` were unaffected throughout, still their own separate tokens.

**Phase 9 (`bg.surface`/`bg.surface-raised`/`bg.neutral-subtle`/`bg.canvas` usages redefined and re-valued, 2026-08-08):** All four tokens' usage descriptions were rewritten at explicit direction, and three of the four values changed with them:
- `bg.surface`: usage → "page, card, sheet and drawer background" (broader than the prior "card/panel surface"). Value: light unchanged (`white`); dark reverted `neutral.black` → `gray.800` (see below for why `neutral.black` didn't stick). Contrast re-verified dark: `text.primary` 9.66:1, `text.secondary` 5.86:1, `text.tertiary` 4.34:1 (all still clear AA at the `gray.300`/`gray.400` values live at the time — see Phase 10 for their own later move).
- `bg.surface-raised`: usage description added ("an elevated surface, paired with a shadow"). Value unchanged both modes (`white` light, `gray.800` dark) — now byte-identical to `bg.surface` in dark mode specifically, so elevation there relies on the paired shadow rather than a background difference. **Superseded 2026-08-20** — this byte-identical-in-every-mode relationship turned out to hold in light mode too (both `white`), so the token was retired entirely; see the dedicated phase below.
- `bg.neutral-subtle`: usage → "hover fill for tertiary/ghost controls, dashboard panel, neutral accent" (broader than hover-fill/zebra-striping alone). Value: light unchanged (`gray.50`); dark moved `gray.800` → `gray.900`, which also resolves the long-standing `bg.neutral-subtle`/`bg.surface-raised` collision noted in that token's own history (both were `gray.800`). Contrast re-verified: `text.secondary`-on-`bg.neutral-subtle` (Tag/Badge subtleNeutral) 8.21:1, `text.primary` 13.53:1.
- `bg.canvas`: usage narrowed to "canvas and playground container" — a specific Storybook live-preview/demo surface, not the general page background it meant before. Value: light `neutral.white` → `gray.100`; dark `neutral.black` → `gray.700`. In dark mode this puts `bg.canvas` (`gray.700`) one step *lighter* than the reverted `bg.surface` (`gray.800`) — an intentional inversion of the usual dark-mode elevation cue (raised content is normally lighter than the canvas behind it), confirmed at explicit direction given the narrower Storybook-specific usage. This is also what reopened the AA gap Phase 10 exists to close: `text.secondary`/`text.tertiary` (at their pre-Phase-10 values) measured 4.00:1/2.96:1 against the new `bg.canvas`, both failing.
- Side effect: reverting `bg.surface` off `neutral.black` back to `gray.800` recreated a `border.default`/`bg.surface` collision (both `gray.800`, 1.00:1) that the `neutral.black` value had incidentally fixed a few edits earlier — `docs.css`'s table-border and section-divider rules were reverted from `border.default` back to `border.neutral-subtle` (`gray.900`, 1.40:1 against the new `bg.surface`) to compensate.
- Two more collisions flagged but not fixed, since nothing currently renders either pairing: `border.neutral-subtle`/`bg.canvas` in light mode (both now `gray.100`) and `border.neutral-subtle`/`bg.neutral-subtle` in dark mode (both now `gray.900`).

**Phase 10 (`text.secondary`/`text.tertiary` dark-mode values raised, 2026-08-08):** Direct follow-up to Phase 9's `bg.canvas` move, which left `text.secondary`/`text.tertiary` failing AA against it.
- `text.secondary` dark: `gray.300` → `gray.200`, at explicit direction. Was 4.00:1 against `bg.canvas` (fails the 4.5:1 text floor); now 5.10:1 (passes). Improves against every other `bg.*` pairing too: 7.47:1 on `bg.surface` (was 5.86:1), 10.47:1 on `bg.neutral-subtle` (was 8.21:1).
- `text.tertiary` dark: `gray.400` → `gray.300`, at explicit direction, same motivation. Still doesn't clear the 4.5:1 text floor against `bg.canvas` (4.00:1) — it does clear the 3:1 non-text floor there, but this token's own literal-text uses (`Text` `color="tertiary"`, Divider label, Input placeholder) need 4.5:1, the same standard that ruled out `gray.500` originally. No primitive step between `gray.300` and `gray.400` exists to close that last gap without also moving past `text.secondary`'s own new `gray.200`. Against this token's actual intended surfaces it's comfortably clear: 5.86:1 on `bg.surface`, 8.21:1 on `bg.neutral-subtle`.
- **Rule: `text.tertiary` must never be used as literal text directly on `bg.canvas`** (any mode/brand) — it's below the AA text floor there and no available primitive step closes the gap without disturbing other, currently-passing pairings. Enforced by convention/review, not by a build-time check.

**Phase 11 (`text.link` on `bg.brand-subtle` logged, 2026-08-14):** Found undocumented while reviewing Avatar (`.content`'s default background/text pairing). Not a new value or a regression — both tokens are unchanged from their existing, already-established values; this is closing a gap in the verification log itself, re-computed directly from the built theme output rather than assumed: **purple-light 7.03:1, purple-dark 7.45:1, emerald-light 5.83:1, emerald-dark 6.49:1** — all comfortably clear of the 4.5:1 AA text floor. The two dark-theme numbers match the `$description` already recorded on each theme's own `bg.brand-subtle` token (Phase 9's `purple-dark`/`emerald-dark` move to `gray.900`); the two light-theme numbers hadn't been stated anywhere before this entry.

**Note (superseded by the text.link de-branding phase, below):** `.content` moved from `text.link` to `text.brand` (2026-08-20) once `text.link` stopped being brand-colored. The pairing this phase verified — a brand-colored text on `bg.brand-subtle` — still exists and is still correct today, just under `text.brand`'s name instead; the numbers above are unchanged (`text.brand` kept the exact value `text.link` used to hold).

**Phase 12 (`bg.{brand,danger,warning,success,info}-subtle-hover` added, 2026-08-15):** New tokens for Avatar's hover state (`as="button"`-style interactive mode) — a subtle-background fill needed its own hover step, one primitive scale step past `-subtle` in this system's standard hover direction (darken in light mode, lighten in dark). All 5 reuse an already-generated scale step (each family's `-subtle` value from *before* its own 2026-07-27 one-step move), rather than needing a new primitive — light themes get each family's `.100` step back, dark themes get `.900` (or `gray.800` for `brand-subtle-hover` specifically, since dark `brand-subtle` itself is `gray.900`, not a brand hue — see Phase 9). Every text token that already pairs with each `-subtle` background was re-checked against the new `-subtle-hover` value directly from the built theme output:

| Pairing | Light | Dark |
|---|---|---|
| `text.danger` / `bg.danger-subtle-hover` | 6.43:1 | 8.22:1 |
| `text.warning` / `bg.warning-subtle-hover` | 8.90:1 | 8.19:1 |
| `text.success` / `bg.success-subtle-hover` | 8.42:1 | 8.19:1 |
| `text.info` / `bg.info-subtle-hover` | 6.10:1 | 8.20:1 |
| `text.link` / `bg.brand-subtle-hover` (purple) | 6.47:1 | 5.32:1 |
| `text.link` / `bg.brand-subtle-hover` (emerald) | 5.37:1 | 4.63:1 |

All 12 clear the 4.5:1 AA text floor — the tightest is emerald-dark's brand pairing at 4.63:1. `danger`/`warning`/`success`/`info` are shared across both brand themes, matching their own `-subtle` tokens' existing brand-independence; `brand-subtle-hover` is brand-specific in light mode (distinct purple/emerald tints) and shared in dark mode (both resolve to `gray.800`, mirroring `brand-subtle` itself being gray-based in dark mode per Phase 9). **The `text.link` rows are superseded the same way as Phase 11's own pairing above** — `.content`'s hover state now pairs `text.brand` with `bg.brand-subtle-hover`, same unchanged values, just under `text.brand`'s name since 2026-08-20.

**Phase 13 (`border.brand-subtle` dark-mode value diverged from light, 2026-08-16):** Dark-mode `border.brand-subtle` moved off the literal `purple.100`/`emerald.100` it shared with light mode (set in Phase 6) to `purple.900`/`emerald.900`. The literal value measured 12.38:1 (purple) / 12.46:1 (emerald) against `bg.brand-subtle` in dark mode — a stark high-contrast ring, nothing like light mode's own intentionally low-key ~1.09:1 pairing. New dark values: `purple.900` on `gray.900` = 1.32:1; `emerald.900` on `gray.900` = 1.08:1 — both now similarly subtle to their own light-mode counterpart, restoring the "decorative accent, not a state boundary" intent Phase 6 established (WCAG 1.4.11's 3:1 floor still doesn't apply here, same reasoning as before).

**Phase 14 (`border.{danger,warning,success,info}-subtle` added, 2026-08-16):** Added for Avatar's `colorful` variant border-matching — until now every colorful avatar kept a fixed `border.brand-subtle` (purple) ring regardless of which status family it hashed to. Four new tokens, one per family, following `border.brand-subtle`'s exact scale-step pattern (family.100 light / family.900 dark) rather than reusing the existing `border.{danger,warning,success,info}` tokens (those are `.500`/`.400`, the stronger state-identifying borders used on Input/Select/etc. validation — a different, already-verified class of token, not decorative; see Phase 15 below). Brand-agnostic, like their matching `bg.*-subtle` tokens. Contrast against each family's own `bg.*-subtle` background:

| Token | Light | Dark |
|---|---|---|
| `border.danger-subtle` | red.100 on red.50 = 1.10:1 | red.900 on red.950 = 1.26:1 |
| `border.warning-subtle` | amber.100 on amber.50 = 1.09:1 | amber.900 on amber.950 = 1.27:1 |
| `border.success-subtle` | green.100 on green.50 = 1.09:1 | green.900 on green.950 = 1.30:1 |
| `border.info-subtle` | blue.100 on blue.50 = 1.09:1 | blue.900 on blue.950 = 1.28:1 |

All comparably subtle to `border.brand-subtle`'s own pairings — deliberately, not a gap, same accepted decorative-only class as `border.brand-subtle`/`border.code`. Dark mode here does *not* neutralize to gray the way `bg.brand-subtle` does (Phase 9): each family's own `bg.*-subtle` keeps its own tint in dark mode (`red.950`, not `gray.900`), so the border pairs against that tinted background rather than gray.

**Phase 15 (`border.{warning,success,info}` added, completing the state-family border set, 2026-08-16):** `border.danger` was the only status-family "strong" border — a real state-identifying border (Input/Select/Checkbox/Textarea validation), correctly subject to WCAG 1.4.11's 3:1 non-text floor unlike the decorative `-subtle` tokens above. Added matching `warning`/`success`/`info` at the same scale step (`.500` light / `.400` dark) for symmetry, verified against each token's real hosting surface (`bg.surface` — white light, `gray.800` dark, what Input/Select/etc. actually sit on) rather than assumed from the pattern match alone:

| Token | Light (vs white) | Dark (vs gray.800) |
|---|---|---|
| `border.danger` (existing) | red.500 = 3.48:1 | red.400 = 4.13:1 |
| `border.warning` | amber.500 = 3.31:1 | amber.400 = 4.31:1 |
| `border.success` | green.500 = 3.07:1 | green.400 = 4.57:1 |
| `border.info` | blue.500 = 3.26:1 | blue.400 = 4.35:1 |

All clear the 3:1 floor — `border.success`'s light value is the tightest at 3.07:1. Not yet consumed by any shipped component; added to complete the token set ahead of a future warning/success/info-state form control, same "add the token layer ahead of its specific consumer" precedent as `bg.code`/`text.code`/`border.code` (Phase 8). Also reordered every semantic `border.*` block (all 4 theme JSON files) and `Color.mdx`'s matching array to group each family's own base token with its own `-subtle` variant (`danger`, `danger-subtle`, `warning`, `warning-subtle`, ...) rather than by when each was added — see `07-storybook-and-documentation-standards.md` §7's order-check note for the full incident.

**Phase 16 (`bg.{danger,warning,success,info}-indicator` added, 2026-08-16 — found during Badge's atom review):** Badge's `dot` mode (a small solid graphic with no text) was using `variant="subtle"`'s pastel background regardless of tone — measured directly against `bg.surface`, those `-subtle` tints come out around 1.0–1.8:1, nowhere near WCAG 1.4.11's 3:1 non-text floor, since they were only ever verified as a background *for text* sitting on top of them. Switching the dot to reuse the existing solid `bg.danger`/`bg.warning`/`bg.success`/`bg.info` fills directly (the obvious next choice) turned out not to be safe either once actually measured against `bg.surface` as a standalone graphic rather than as a text-host:

| Token | Light (vs white) | Dark (vs gray.800) |
|---|---|---|
| `bg.danger` (existing) | red.600 = 5.10:1 | red.500 = **2.90:1 — fails** |
| `bg.warning` (existing) | amber.600 = 4.78:1 | amber.500 = 3.05:1 — barely clears it |
| `bg.success` (existing) | green.700 = 6.50:1 | green.500 = 3.28:1 — thin |
| `bg.info` (existing) | blue.600 = 4.71:1 | blue.500 = 3.09:1 — barely clears it |

Light mode was fine across the board; dark mode wasn't — `bg.danger` fails the floor outright, the rest pass with almost no margin. Rather than mutate those existing, already-verified (for their real purpose) tokens and risk the shared consumers that depend on their current value, added four new tokens at the same scale step `border.{warning,success,info}` already established in Phase 15 (`.500` light / `.400` dark — one step lighter in dark, this system's standard "brighten for the harder-to-see case" direction):

| Token | Light (vs white) | Dark (vs gray.800) |
|---|---|---|
| `bg.danger-indicator` | red.500 = 3.48:1 | red.400 = 4.13:1 |
| `bg.warning-indicator` | amber.500 = 3.31:1 | amber.400 = 4.31:1 |
| `bg.success-indicator` | green.500 = 3.07:1 | green.400 = 4.57:1 |
| `bg.info-indicator` | blue.500 = 3.26:1 | blue.400 = 4.35:1 |

All clear the 3:1 floor — `bg.success-indicator`'s light value is the tightest at 3.07:1 (the same primitive step `border.success` already uses for the identical reason). No new neutral token was needed: `bg.neutral` (Phase 3) already solves this exact problem for the neutral/offline case, and Badge's neutral dot reuses it directly. `Color.mdx` updated with all four, positioned immediately after each family's own existing entries (matching the semantic JSON's own key order, per Phase 15's ordering rule).

Same day, applying this same lens to Avatar's already-finalized status dot (`online`/`busy`/`away`) found it had the identical latent bug — it was reusing `bg.success`/`bg.danger`/`bg.warning` directly, the exact tokens shown failing/barely-passing above (`offline` already used `bg.neutral`, unaffected). Fixed by switching those three to the new `-indicator` tokens — a narrow, user-authorized fix to a finalized component, not a full review reopening.

Also corrected while re-deriving these numbers: `bg.neutral`'s own `$description` had cited "4.34:1 dark" for years — that figure was actually `gray.400`'s contrast against `bg.surface`, not `gray.500`'s (the token's real value, 3.10:1 dark). The token itself was never wrong, only the number recorded in its description. Corrected across all 4 theme files.

**Note (`bg.danger-indicator`/`bg.warning-indicator`/`bg.success-indicator`/`bg.info-indicator` superseded):** the 2026-08-20 token consolidations (see the dedicated phases below) moved each tone's own dark value to a lighter step — `bg.danger` to red.300 (5.67:1, up from an outright-failing 2.90:1), `bg.warning` to amber.300 (5.80:1, up from a barely-passing 3.05:1), `bg.success` to green.300 (6.01:1, up from an already-fine-but-thin 3.28:1), `bg.info` to blue.300 (5.85:1, up from a barely-passing 3.09:1) — all four clear the 3:1 floor these tokens existed to work around. All four `-indicator` tokens were retired; every consumer moved to the base `bg.{tone}` token directly. `bg.brand-indicator` is unaffected, still in use — `brand` is the only tone not yet part of this consolidation series. **Superseded 2026-08-21 — see the dedicated phase below:** `brand` went through the exact same consolidation once `bg.brand`'s own dark value moved to a lighter step, and `bg.brand-indicator` was retired the same way as its four siblings here.

**Phase 17 (disabled-state contrast, verified 2026-08-16 — closes what was previously logged as "not yet checked"):** confirmed real, computed numbers for every component with a disabled state, rather than leaving the WCAG exemption asserted but unmeasured. Two distinct mechanisms exist in this codebase:

- **Opacity-composited (9 components — `Button`, `IconButton`, `CloseButton`, `Avatar`, `Checkbox`, `Switch`, `Input`, `Textarea`, `Select`):** `opacity: var(--dbm-opacity-40)` applied to the whole element. Modeled as a real alpha composite (`0.4×foreground + 0.6×page-background`, `bg.surface` as the representative page background every prior phase's own checks already use) rather than assumed. One representative pairing per component's own default appearance, computed for all 4 themes — full range across every theme was **1.02:1 to 2.96:1** (`Switch`'s track fill was tightest, `Input`/`Textarea`/`Select`'s value text was closest to clearing a floor). `CloseButton` excluded — its icon color is `color: inherit`, context-dependent by design, no single number applies.
- **Direct tokens, not opacity-derived (`text.disabled`, `icon.disabled` — consumed by `FieldLabel`, `FieldHelperText`, `Heading`, `Text`, `Icon`, `Spinner`):** used as literal resolved colors, no compositing involved. `text.disabled` measured **2.14–2.32:1** against `bg.surface` across all 4 themes; `icon.disabled` measured **1.46–1.72:1**.

Every number sits below both the 4.5:1 text floor and the 3:1 non-text floor, in every theme, with no surprises (nothing measured near-zero/fully-invisible either — `Switch`'s 1.02:1 is the closest, still visually present via its own shape and the paired `cursor: not-allowed`, not relying on color alone). This confirms the existing exemption is correctly applied, not just asserted: WCAG 2.1 success criteria 1.4.3 (Contrast Minimum) and 1.4.11 (Non-text Contrast) both explicitly exclude **inactive/disabled user interface components** from their contrast requirements — a disabled control reading as visibly muted is the correct, spec-sanctioned behavior, not a gap to close by raising these numbers. Nothing was changed as a result of this phase; it's a verification-only entry, matching this log's own standing rule of confirming rather than assuming.

**Phase 18 (`bg.skeleton` added, 2026-08-16 — found during Skeleton's atom review):** `Skeleton.tsx` used `bg.neutral-subtle` for its loading-placeholder fill. Measured directly against `bg.surface` (the white/near-white card it's designed to sit on) rather than assumed: **`bg.neutral-subtle` (gray.50) vs `bg.surface` (white) is 1.04:1 in light mode** — confirmed live in Storybook (Purple and Emerald, both barely perceptible) as well as computed. Not a WCAG-text-contrast failure — Skeleton is decorative/`aria-hidden`, exempt the same way disabled controls are (Phase 17) — but a real functional gap: a loading placeholder that's functionally invisible against the surface it's meant to sit on defeats its own purpose. Same root cause as Phase 16's `bg.*-indicator` gap for Badge's dot mode — `bg.neutral-subtle`'s existing verification (`"hover fill for tertiary/ghost controls, dashboard panel, neutral accent"`) never covered "must read as a distinct shape against `bg.surface`," and reusing it for that unverified purpose didn't hold up once actually measured.

Added a dedicated `bg.skeleton` token rather than moving `bg.neutral-subtle` itself (which would risk regressing its other real consumers — hover fills, dashboard panels, Tag/Badge's `subtleNeutral` text pairing) or reusing `bg.neutral` (already-verified, but noticeably darker/more prominent than a typical loading placeholder, and semantically meant for status-dot fills, not loading state). Initial value (both brands, shared): **light `gray.200`** (1.35:1 vs `bg.surface`, matching the real-world contrast MUI's own default Skeleton fill produces against white) / **dark `gray.900`, unchanged** (already 1.40:1, confirmed visible before this fix — a rename onto a dedicated token, not a value change).

**Revised same day, at explicit direction: light mode moved off gray to a brand-tinted value.**
- **Light: `purple.200` (Purple theme) / `emerald.200` (Emerald theme) — no longer brand-agnostic.** Chosen specifically to preserve the just-established contrast target rather than picking an arbitrary brand step: `purple.200` vs `bg.surface` = **1.36:1**, `emerald.200` vs `bg.surface` = **1.34:1** — both deliberately matched to `gray.200`'s own 1.35:1 so the visibility fix carries over unchanged; only the hue changed, not the visual weight.
- **Dark: stays `gray.900`, unchanged, deliberately not following light mode's brand tint.** Mirrors `bg.brand-subtle`'s own established precedent — that token drops its brand tint entirely in dark mode too (`purple.950` was tried and rejected there for failing `text.link`/`icon.brand` contrast and colliding with `bg.canvas`; see that token's own description above). `bg.skeleton` hosts no text, so the original contrast reason doesn't directly transfer, but the underlying tension likely does: a dark, low-chroma brand tint reads as nearly indistinguishable from plain gray at this end of the OKLCH lightness curve, so there's no real visual payoff for a fourth brand-specific value. Revisit if a specific dark brand-tinted value is ever explicitly requested.

Brand-agnostic in dark mode only now (both brands share `gray.900`), brand-specific in light mode — an asymmetry, not an inconsistency, matching `bg.brand-subtle`'s own precedent for the same split. Added to `Color.mdx`'s `bg.*` array immediately after `bg.neutral-strong`, per §7's ordering rule, and verified against the Foundations token-coverage script (`07-storybook-and-documentation-standards.md` §10) before considering this done.

**Phase 19 (`bg.neutral-subtle-hover` / `bg.neutral-strong-hover` added, 2026-08-17 — found adding Tag's whole-tag clickable/selectable mode):** Tag's `subtleNeutral`/`solidNeutral` classes (`bg.neutral-subtle`/`bg.neutral-strong`) had no matching `-hover` token, unlike every other tone (`brand`/`danger`/`warning`/`success`/`info` all already had both a solid `-hover` and a `-subtle-hover` from Phases 3/12). Both computed and verified before adding, not assumed from the existing pattern:

- **`bg.neutral-subtle-hover`: `gray.100` (light) / `gray.800` (dark)** — the standard darken-in-light/lighten-in-dark direction every other `-subtle-hover` token already uses. `text.secondary` (the text Tag/Badge's `subtleNeutral` actually pairs with `bg.neutral-subtle`) on `gray.100` = 6.05:1, on `gray.800` = 7.47:1 — both clear AA comfortably. The dark value is byte-identical to `bg.surface-raised` (also `gray.800`) — the same already-flagged, harmless collision `bg.neutral-subtle` itself carries in dark mode (nothing pairs the two directly).
- **`bg.neutral-strong-hover`: `gray.700`, the same value in all 4 themes** — a deliberate exception to the mode-based direction rule, computed rather than pattern-matched. `bg.neutral-strong` is already brand-and-mode-agnostic (`gray.600` in every theme, see its own entry above), and the "lighten in dark mode" direction that works for every other solid-fill hover fails outright here: white `text.on-neutral` on the candidate `gray.500` measures **3.25:1**, below the 4.5:1 AA floor. `gray.700` (the same "darken" direction light mode uses) gives white text 6.88:1 in both modes — same class of exception as `bg.danger-hover`/`bg.brand-hover`'s own original Phase 3 dark-mode direction overrides, just discovered later since neutral had no hover token to compute until now.

**Note (`bg.neutral`/`bg.neutral-strong`/`bg.neutral-strong-hover` superseded, 2026-08-21 — see the dedicated phase below):** the original `bg.neutral` (`gray.500`) was retired, `bg.neutral-strong` renamed to `bg.neutral`, and `bg.neutral-strong-hover` renamed to `bg.neutral-hover` — the reasoning and numbers in this Phase 19 entry and the two bullets above it in Phase 3 (`bg.neutral` = `gray.500`, `bg.neutral-strong` = `gray.600`) still describe real, accurate history, just under names that have since moved. Left in place rather than rewritten, the same reasoning as the note after Phase 3's own bullets.

Both added to `Color.mdx`'s `bg.*` array immediately after their base token, per §7's ordering rule.

**Phase 20 (`border.{danger,warning,success,info}-strong` and `border.strong` — the neutral tone's own step, originally named `border.neutral-strong` — added, 2026-08-19 — found reviewing Tag's selected solid-variant ring):** Tag's selected state on a solid-fill tag was implemented as an offset halo (a `bg.surface`-colored gap ring, then an accent ring further out — see the component's own `Tag.module.css` comment for why a flat ring directly against the fill doesn't work: every existing `border.{tone}` token measures only ~1.45–2.1:1 there, `border.focus` only ~1.1–1.2:1, since ring and fill share similar luminance regardless of hue). The accent ring initially reused `text.{tone}` directly, which worked (6.9–10.1:1 light, 5.7–7.5:1 dark, against `bg.surface`) but was flagged in review as reaching across token categories — `border.*` and `text.*` are kept separate specifically so each can be retuned independently, and borrowing `text.danger` for a border-ish use means a future text-legibility tweak could silently move this ring too.

Fixed by giving `border.*` its own "strong" step per tone, at the *same* primitive alias `text.{tone}` currently happens to use — visually identical today, independently tracked going forward:

| Token | Light | Dark | vs `bg.surface` (light / dark) |
|---|---|---|---|
| `border.danger-strong` | `red.700` | `red.300` | 7.39:1 / 5.67:1 |
| `border.warning-strong` | `amber.800` | `amber.300` | 10.14:1 / 5.80:1 |
| `border.success-strong` | `green.800` | `green.300` | 9.56:1 / 6.01:1 |
| `border.info-strong` | `blue.700` | `blue.300` | 6.93:1 / 5.85:1 |
| `border.strong` | `gray.700` | `gray.200` | 6.88:1 / 7.47:1 |

`neutral` has no existing `border.danger`-style "state" tier to sit alongside (there's no tone-neutral input-validation border), so `border.strong` is a standalone addition rather than completing an existing `-strong`/base pair — placed next to `border.neutral` in `Color.mdx` rather than in the danger/warning/success/info block. All 5 added to `Color.mdx`'s `border.*` array immediately after their base token, per §7's ordering rule.

**Note (`border.danger-strong`/`border.warning-strong`/`border.success-strong`/`border.info-strong` superseded):** the 2026-08-20 token consolidations (see the dedicated phases below) folded these directly into `border.danger`/`border.warning`/`border.success`/`border.info`, and retired all four — the role survives, but not always the exact value: dark mode carries over unchanged for all four (`red.300`/`amber.300`/`green.300`/`blue.300`, since each `-strong` token's own dark value happened to already match the tone's new consolidated dark step), while light mode moves to the shared step (`red.700`→`red.600`, `amber.800`→`amber.600`, `green.800`→`green.700`, `blue.700`→`blue.600`). `border.strong` is unaffected, still its own separate token — `neutral` is the only tone not yet part of this consolidation series.

**Phase 21 (`border.brand` renamed to `border.brand-subtle`, 2026-08-20):** Naming-consistency fix, no value change — every sibling family token added since Phase 6 (`border.danger-subtle`/`warning-subtle`/`success-subtle`/`info-subtle`, Phase 14) carries the `-subtle` suffix for this exact decorative-only role (explicitly exempt from WCAG 1.4.11's 3:1 non-text floor, unlike the stronger state-identifying `border.{tone}` tokens), but the original brand token never picked up the suffix when that pattern formed around it. Renamed rather than aliased — every real consumer (`Avatar.module.css`'s default-fill ring) and every doc reference (`Avatar.mdx`, `Color.mdx`, `docs.css`) updated in the same pass, so there's exactly one live name for this token, not two. Historical phase entries above (6, 9, 13, 14) have been updated in place to the new name — the *decisions* and *numbers* they describe didn't change, only the identifier used to talk about them, so rewriting them to the current name keeps this document internally consistent rather than preserving a name that no longer resolves to anything.

**Phase 22 (`border.brand` re-added, 2026-08-20 — a new, distinct token from the one renamed away in Phase 21):** Speculative addition, no component consumes it yet — a general-purpose default brand-colored border, for future use wherever a real (WCAG-1.4.11-verified, not decorative-only) brand-accent border is needed, distinct from `border.focus`'s specific keyboard-focus role. Requested as "maps to `color.purple.600`"; confirmed with the user that this should be brand-adaptive (a per-theme step, like every other `border.*`/`bg.*`/`text.*` brand token) rather than the literal purple value shared across the Emerald theme files too.

Values, computed rather than copied from an existing token wholesale:
- **Light mode reuses `bg.brand`'s own light step** (`purple.600` / `emerald.700`) — `purple.600` is exactly what was requested; `emerald.700` (not the numerically-parallel `.600`) matches the established pattern of emerald needing one step further than purple for equivalent perceptual/contrast weight (same reason `border.focus`'s own emerald value sits at `.600` while purple's sits at `.500`). Both verified as real border pairings against `bg.surface`, not assumed from `bg.brand`'s own (different) text-hosting contrast case: `purple.600` = 7.37:1, `emerald.700` = 6.08:1.
- **Dark mode does *not* reuse `bg.brand`'s own dark step** — `purple.500`/`emerald.500` measure only 2.24:1 / 3.41:1 against dark `bg.surface` (the first outright fails 3:1, the second clears it with almost no margin), since `bg.brand`'s dark calibration is for hosting text on a fill, not for a thin border line resolving against the surrounding surface. Reused `border.focus`'s own already-proven dark values instead (`purple.400` / `emerald.400`, 3.56:1 / 4.63:1) — `border.brand` and `border.focus` end up visually identical in dark mode despite differing in light mode (`.600`/`.700` vs `.500`/`.600`), a real constraint on how much contrast headroom a saturated brand border has against a dark surface, not an oversight.

**Note (superseded 2026-08-21 — see the dedicated phase below):** the constraint this bullet describes no longer holds once `bg.brand`'s own dark step moved to a lighter `.300`. `border.brand` was repointed to reuse `bg.brand`'s new dark value directly, diverging from `border.focus` in dark mode for the first time — a real improvement (5.32:1/6.07:1, up from 3.56:1/4.63:1), not a regression of the reasoning above, which was correct for the dark values that existed at the time.

Added to `Color.mdx`'s `border.*` array immediately before `border.brand-subtle` (mirroring the danger/warning/success/info block's own "base tone, then `-strong`, then `-subtle`" ordering), per §7's ordering rule.

**Phase 23 (`border.neutral-strong` added, 2026-08-20 — found adding Tag's outline variant):** Every other tone (`danger`/`warning`/`success`/`info`/`brand`) already had a real, state-identifying `border.{tone}` token, contrast-verified against `bg.surface`. Neutral didn't — the only existing generic gray border, `border.neutral`, is documented as decorative "hover-state emphasis," and measuring it (rather than assuming it would do) confirmed it genuinely fails as a real border: **2.32:1 light / 2.14:1 dark against `bg.surface`**, both well under the 3:1 WCAG 1.4.11 floor.

Values, same "comfortable margin over bare-minimum" standard as `border.brand` (Phase 22) rather than the family's usual `.500`/`.400` step (which for gray specifically only clears 3.25:1 light / 3.10:1 dark — technically passing but thin):
- **Light: `gray.600`** — 4.70:1 against `bg.surface`.
- **Dark: `gray.400`** — 4.34:1 against `bg.surface`.

Same value in both brand themes (brand-agnostic, matching `border.neutral`/`border.default`'s own precedent). Added to `Color.mdx`'s `border.*` array immediately after `border.neutral`, before `border.strong`, per §7's ordering rule.

**Note (superseded by Phase 24 below):** Tag's outline variant, the motivating use case named above, ended up consuming `border.strong` for its own default border, not this token — see Phase 24. `border.neutral-strong` still stands as a real, contrast-verified token completing the family, available for a future default-emphasis use.

**Pitfall hit while adding this:** the first draft of this token's own `$description` used literal `border.{tone}` placeholder notation to mean "each per-tone border token." Style Dictionary scans *every* string field for `{...}` reference syntax, including `$description` — `{tone}` was parsed as an attempted token reference to a token named `tone`, which doesn't exist, and broke the build with "Reference Errors: Some token references (1) could not be found." Fixed by spelling out the tone names in prose (`border.danger/warning/success/info/brand`) instead of using bracketed placeholder notation anywhere in a token file, including descriptions — worth remembering for any future token description that wants to reference "the whole family" generically.

**Phase 24 (Tag `outline` variant revised, 2026-08-19 — no new tokens, existing-token reassignment only):** Two changes to `outline`'s own CSS, requested after Phase 23 landed:

1. **Default (unselected) border upgraded one tier**, from `border.{tone}` to `border.{tone}-strong` (the same Phase 20 tokens `solid`'s own selected-state halo uses) — `brand` unchanged at `border.brand` (Phase 22), since it has no `-strong` sibling and was already judged confident enough on its own. Reasoning: `outline`'s rim is the *only* visual signal it has (no fill), so it reads better one step richer than the decorative-tier default it launched with in Phase 23's companion work.
2. **Selected state converges toward `solid`'s own look** — background switches from the `-subtle` tint to the tone's full fill (`bg.{tone}`, the exact token `solid{Tone}` uses) with matching `text.on-{tone}` text, rather than staying a light tint. The border is unchanged from the unselected state — same `border.{tone}-strong`/`border.brand` token, just redeclared to win the cascade over the generic `.selected` rule's own `currentColor` ring.

**Why the same border tier survives being placed against the new fill, when it didn't for `solid`'s own ring (Phase 20):** measured `border.{tone}-strong` directly against the new `bg.{tone}` selected fill before deciding this was acceptable (this system's standing practice). Every tone measures poorly there: **danger 1.45:1, warning 2.12:1, success 1.47:1, info 1.47:1, neutral (`border.strong`, formerly `border.neutral-strong`) vs `bg.neutral` (formerly `bg.neutral-strong`) 1.46:1, brand vs brand 1.00:1** (the exact same primitive value on both sides). Kept anyway, deliberately, unlike Phase 20's offset-halo fix: once `outline` is selected, the background swap to a full saturated fill is *already* an obvious, high-contrast state signal on its own — the border at that point is decorative continuity with the unselected look, not the sole functional state boundary WCAG 1.4.11 governs, the same reasoning that already lets the `-subtle` decorative border tokens elsewhere in this system sit below 3:1. The *unselected* border, where it genuinely is the only signal `outline` has, stays fully contrast-verified via `border.{tone}-strong`/`border.brand` (point 1 above) — this reasoning applies only once a second, higher-contrast signal (the fill) is also present.

No token additions — `border.{tone}-strong` already existed (Phase 20). Border drawn via `box-shadow: inset`, not the `border` property, in both states — box-shadow doesn't participate in the box model, keeping `outline` pixel-identical in size to `subtle`/`solid` at the same `size` step. Confirmed via live `getComputedStyle` checks in both brand themes and both color modes before considering this done, matching this project's self-verification standard.

**Note (`danger`/`warning`/`success`/`info` superseded):** the 2026-08-20 token consolidations (below) folded `border.danger-strong`/`border.warning-strong`/`border.success-strong`/`border.info-strong` into `border.danger`/`border.warning`/`border.success`/`border.info`, so `outlineDanger`/`selected.outlineDanger`, `outlineWarning`/`selected.outlineWarning`, `outlineSuccess`/`selected.outlineSuccess`, and `outlineInfo`/`selected.outlineInfo` now reference the unified tokens — same reasoning above, just under the consolidated token name (dark-mode value unchanged for all four; light-mode value shifted to the shared step). `neutral` is unaffected — the only tone left on the original `-strong` pattern once this ships.

**Note (`neutral`'s own border renamed, 2026-08-21):** the border token this whole phase calls `border.{tone}-strong` for the `neutral` case (`border.neutral-strong`, gray.700/gray.200) was later renamed to `border.strong` as part of a broader `border.*` identifier rotation — see the dedicated phase below. `neutral` no longer literally fits the `border.{tone}-strong` template by name, though every decision/value described above still applies unchanged; wherever this phase's prose says `border.{tone}-strong` and means the neutral case specifically, read it as `border.strong`.

**Phase 25 (danger token consolidation, 2026-08-20 — first pass of a planned per-tone cleanup):** The token layer had been growing need-by-need, one narrow single-purpose token at a time (Phases 16, 20 above are two examples) — some of those tokens ended up with only one or two real consumers. At explicit direction, started consolidating around one shared, contrast-verified primitive step per mode, reused across `bg`/`text`/`border`/`icon` for a given tone, rather than a distinct value per narrow use case — `danger` first, as a trial run before repeating this for `warning`/`success`/`info`/`neutral`.

Full contrast review (computed, not assumed) confirmed every new pairing clears AA, and two are strict improvements:

| Token | Light | Dark | Light Δ | Dark Δ |
|---|---|---|---|---|
| `bg.danger` | `red.600` (unchanged) | `red.300` ← `red.500` | — | 5.10:1 → 5.67:1 vs `bg.surface` |
| `bg.danger-hover` | `red.700` (unchanged) | `red.200` ← `red.400` | — | (follows `bg.danger`'s dark move) |
| `text.on-danger` | `neutral.white` (unchanged) | `red.900` ← `gray.950` | — | 5.22:1 → 8.22:1 vs new `bg.danger` |
| `text.danger` | `red.600` ← `red.700` | `red.300` (unchanged) | 7.39:1 → 5.10:1 vs `bg.surface` | — |
| `border.danger` | `red.600` ← `red.500` | `red.300` ← `red.400` | 3.48:1 → 5.10:1 | 4.13:1 → 5.67:1 |
| `icon.danger` (new) | `red.600` | `red.300` | 5.10:1 vs `bg.surface` | 5.67:1 vs `bg.surface` |
| `icon.on-danger` (new) | `neutral.white` | `red.900` | 5.10:1 vs new `bg.danger` | 8.22:1 vs new `bg.danger` |

`bg.danger-subtle`, `bg.danger-subtle-hover`, and `border.danger-subtle` are unchanged — those stay their own dedicated decorative-tint tiers, not part of the shared-step consolidation.

**Two tokens retired, both because the value change above resolved the exact problem that made them necessary:**
- **`bg.danger-indicator`** (Phase 16) existed only because `bg.danger`'s old dark value (`red.500`) measured 2.90:1 against dark `bg.surface`, an outright WCAG 1.4.11 failure for a small standalone graphic (a status dot). The new dark value (`red.300`) clears it at 5.67:1, so every former consumer — Avatar's `.statusBusy`, Badge's `.dotDanger` — now uses `bg.danger` directly.
- **`border.danger-strong`** (Phase 20) existed as a separate richer tier because `border.danger`'s old value was too weak to serve both roles (a real input-validation border *and* a confident accent ring/frame). The new `border.danger` value (`red.600`/`red.300`) absorbs that role directly — every former consumer (Tag's `outlineDanger`/`selected.outlineDanger` border, `selected.solidDanger`'s offset-halo accent) now uses `border.danger`.

**Two explicit user confirmations, since these aren't contrast questions but visual-identity ones:**
1. Tag's outline/selected-halo border recolors from `red.700` to `red.600` in light mode (still comfortably passes, 7.39:1 → 5.10:1, but a real visible change to a just-shipped component).
2. Dark-mode solid-danger surfaces (Button, IconButton, ProgressBar, ProgressCircle, Badge solid, Tag solid/selected-outline) shift from a `red.500` fill + near-black `gray.950` text to a lighter `red.300` fill + dark-maroon `red.900` text — a monochromatic-red look instead of red-fill-plus-neutral-text, system-wide.

`icon.danger`/`icon.on-danger` are new, purely additive — no existing component referenced a dedicated icon-danger token before this (icons inside e.g. `FieldError` inherit `text.danger` via `currentColor`), so nothing had to migrate onto them; they exist now for future consumers that want an icon-specific token rather than relying on inheritance.

`Color.mdx`, `Avatar.mdx`, and `Badge.mdx` updated to match — the two retired tokens' rows removed (merged into their replacement token's own `usage` text where a doc explained a specific role, e.g. Badge's dot-mode note folded into its existing `bg.danger` row), the two new `icon.*` rows added. Verified via `check-foundations-token-coverage.mjs` (the tripwire script that fails on any documented-but-nonexistent or existing-but-undocumented token) before considering the doc pass complete.

**Standing rule, added same day after review:** `Color.mdx`'s own `usage` text (unlike the token JSON's own `$description`, and unlike per-component docs like `Avatar.mdx`/`Badge.mdx`) must stay general — no named components, no "e.g. Tag's..." examples, no changelog dates/pointers. `Color.mdx` documents what a token *is for*, not which specific component happens to consume it today; component-specific detail belongs in that component's own doc page or the token's own `$description`. Five danger rows were tightened to match this (`bg.danger`, `bg.danger-subtle`, `text.danger`, `border.danger`, `border.danger-subtle`) — applies to every future token added or edited in `Color.mdx`, including this phase's own warning entries.

**Phase 26 (warning token consolidation, 2026-08-20 — second pass of the per-tone cleanup started in Phase 25):** Same approach as danger — one shared, contrast-verified primitive step per mode across `bg`/`text`/`border`/`icon`, replacing the narrow single-purpose tokens.

| Token | Light | Dark | Light Δ | Dark Δ |
|---|---|---|---|---|
| `bg.warning` | `amber.600` (unchanged) | `amber.300` ← `amber.500` | — | 3.05:1 → 5.80:1 vs `bg.surface` |
| `bg.warning-hover` | `amber.700` (unchanged) | `amber.200` ← `amber.400` | — | (follows `bg.warning`'s dark move) |
| `text.on-warning` | `neutral.white` (unchanged) | `amber.900` ← `gray.950` | — | 5.49:1 → 8.19:1 vs new `bg.warning` |
| `text.warning` | `amber.600` ← `amber.800` | `amber.300` (unchanged) | 10.14:1 → 4.78:1 vs `bg.surface` | — |
| `border.warning` | `amber.600` ← `amber.500` | `amber.300` ← `amber.400` | 3.31:1 → 4.78:1 | 4.31:1 → 5.80:1 |
| `icon.warning` (new) | `amber.600` | `amber.300` | 4.78:1 vs `bg.surface` | 5.80:1 vs `bg.surface` |
| `icon.on-warning` (new) | `neutral.white` | `amber.900` | 4.78:1 vs new `bg.warning` | 8.19:1 vs new `bg.warning` |

`bg.warning-subtle`, `bg.warning-subtle-hover`, and `border.warning-subtle` are unchanged — same as danger, those stay their own dedicated decorative-tint tiers.

**Same two retirements as danger, same conclusion, one real difference worth flagging:** unlike danger's old dark `bg` value (which failed WCAG 1.4.11 outright at 2.90:1), warning's old dark value already cleared the 3:1 floor at 3.05:1 — thin, not broken. The new value (5.80:1) is a clean improvement either way, so `bg.warning-indicator` (Phase 16) was retired the same as `bg.danger-indicator`, every former consumer (Avatar's `.statusAway`, Badge's `.dotWarning`) now using `bg.warning` directly. `border.warning-strong` (Phase 20) is retired the same way, absorbed into `border.warning` — every former consumer (Tag's `outlineWarning`/`selected.outlineWarning` border, `selected.solidWarning`'s offset-halo accent) now uses `border.warning`.

**The margins here are noticeably thinner than danger's own pass, for a structural reason, not an error:** `text.warning`'s old value (`amber.800`) sat unusually high on the amber scale, so unifying it costs far more headroom than danger's equivalent move did — `text.warning` vs `bg.surface` drops from 10.14:1 to 4.78:1, and `text.warning` vs `bg.warning-subtle` drops from 9.74:1 to **4.59:1**, the single tightest margin found across either consolidation pass (still a real, deliberate AA pass — 0.09 above the 4.5:1 floor — not a violation, just the thinnest headroom of any token reviewed so far).

**One structural difference from danger:** warning has no validation-border consumer anywhere in the component set (`border.danger` is used by `Input`/`Textarea`/`Checkbox`/`Select`; `border.warning` isn't used by any of them) — its real-world role is narrower, effectively just the accent-ring role absorbed from `border.warning-strong`. `Color.mdx`'s `border.warning` row reflects this ("state-identifying border; also the richer accent-ring role" rather than danger's "error-state input border" framing).

**Two explicit user confirmations, same pattern as danger:**
1. Tag's outline/selected-halo border recolors from `amber.800` to `amber.600` in light mode — a more visually distinct shift than danger's own `red.700`→`red.600` move (10.14:1 → 4.78:1, versus danger's 7.39:1 → 5.10:1).
2. Dark-mode solid-warning surfaces (ProgressBar, ProgressCircle, Badge solid, Tag solid/selected-outline — no Button/IconButton consumer exists for warning) shift from an `amber.500` fill + near-black `gray.950` text to a lighter `amber.300` fill + dark-brown `amber.900` text.

A side effect worth naming: `border.warning` and `bg.warning` now resolve to the exact same primitive value in both modes (both `amber.600` light / `amber.300` dark), same as danger's own `border.danger`/`bg.danger` pair — so Tag's `.selected.outlineWarning` border-vs-fill contrast is now a literal 1.00:1 (border and fill are byte-identical), same as danger and brand. See that component's own `Tag.module.css` comment (`.selected.outlineBrand` block) for why this is fine: once selected, the background swap itself is the high-contrast state signal, and the border becomes decorative continuity rather than the functional boundary WCAG 1.4.11 governs.

`icon.warning`/`icon.on-warning` are new, purely additive, same as danger's icon tokens. `Color.mdx`, `Avatar.mdx`, and `Badge.mdx` updated to match, following the general-usage-text standing rule established after Phase 25 (no component examples in `Color.mdx`'s own rows). Verified via `check-foundations-token-coverage.mjs` before considering the doc pass complete.

**Phase 27 (success token consolidation, 2026-08-20 — third pass of the per-tone cleanup started in Phase 25):** Same approach as danger and warning — one shared, contrast-verified primitive step per mode across `bg`/`text`/`border`/`icon`.

| Token | Light | Dark | Light Δ | Dark Δ |
|---|---|---|---|---|
| `bg.success` | `green.700` (unchanged) | `green.300` ← `green.500` | — | 3.28:1 → 6.01:1 vs `bg.surface` |
| `bg.success-hover` | `green.800` (unchanged) | `green.200` ← `green.400` | — | (follows `bg.success`'s dark move) |
| `text.on-success` | `neutral.white` (unchanged) | `green.900` ← `gray.950` | — | 5.91:1 → 8.19:1 vs new `bg.success` |
| `text.success` | `green.700` ← `green.800` | `green.300` (unchanged) | 9.56:1 → 6.50:1 vs `bg.surface` | — |
| `border.success` | `green.700` ← `green.500` | `green.300` ← `green.400` | 3.07:1 → 6.50:1 | 4.57:1 → 6.01:1 |
| `icon.success` (new) | `green.700` | `green.300` | 6.50:1 vs `bg.surface` | 6.01:1 vs `bg.surface` |
| `icon.on-success` (new) | `neutral.white` | `green.900` | 6.50:1 vs new `bg.success` | 8.19:1 vs new `bg.success` |

`bg.success-subtle`, `bg.success-subtle-hover`, and `border.success-subtle` are unchanged, same as the previous two passes.

**The healthiest of the three passes so far — no tight margins anywhere.** Unlike warning, success's own numbers already sat close to the eventual shared step before this: `bg.success` light was *already* `green.700` (zero change needed), and `text.success`/`border.success-strong` were only one step off (`green.800`) rather than warning's two-step outlier (`amber.800`). The tightest margin anywhere in this pass is **6.01:1** — nowhere near warning's 4.59:1 near-miss.

**Same two retirements, and this is the safest of the three:** `bg.success-indicator`'s old dark value (`green.400`) already had real margin at 4.57:1 — not a thin pass like warning's 3.05:1, let alone danger's outright 2.90:1 failure — so retiring it is a clean improvement over an already-fine baseline, not a fix for a real gap. `border.success-strong`'s only consumer is Tag's own `Tag.module.css` (outline default/selected border, solid's selected-halo accent) — same 3 spots as danger and warning; the light-mode shift (`green.800→green.700`) is modest, closer to danger's own subtle recolor than warning's more visible jump.

**Same structural note as warning:** no validation-border consumer exists for `border.success` either.

**Same two confirmations:**
1. Tag's success outline/selected-halo border recolors from `green.800` to `green.700` in light mode — a low-key shift (9.56:1→6.50:1).
2. Dark-mode solid-success surfaces (ProgressBar, ProgressCircle, Badge solid, Tag solid/selected-outline — no Button/IconButton consumer) shift from `green.500` fill + near-black `gray.950` text to `green.300` fill + dark-green `green.900` text.
3. `border.success` and `bg.success` now resolve to the identical value in both modes, same as danger/warning — Tag's `.selected.outlineSuccess` border-vs-fill contrast is a literal 1.00:1, joining brand/danger/warning in that group (`info`/`neutral` remain the only two tones not yet consolidated, still on their own `-strong` tier).

`icon.success`/`icon.on-success` are new, purely additive. `Color.mdx`, `Avatar.mdx`, and `Badge.mdx` updated to match, following the general-usage-text standing rule from Phase 25. Verified via `check-foundations-token-coverage.mjs` before considering the doc pass complete.

**Phase 28 (info token consolidation, 2026-08-20 — fourth pass of the per-tone cleanup started in Phase 25; `neutral` is the only tone left after this one):** Same approach as the previous three passes — one shared, contrast-verified primitive step per mode across `bg`/`text`/`border`/`icon`.

| Token | Light | Dark | Light Δ | Dark Δ |
|---|---|---|---|---|
| `bg.info` | `blue.600` (unchanged) | `blue.300` ← `blue.500` | — | 3.09:1 → 5.85:1 vs `bg.surface` |
| `bg.info-hover` | `blue.700` (unchanged) | `blue.200` ← `blue.400` | — | (follows `bg.info`'s dark move) |
| `text.on-info` | `neutral.white` (unchanged) | `blue.900` ← `gray.950` | — | 5.56:1 → 8.20:1 vs new `bg.info` |
| `text.info` | `blue.600` ← `blue.700` | `blue.300` (unchanged) | 6.93:1 → 4.71:1 vs `bg.surface` | — |
| `border.info` | `blue.600` ← `blue.500` | `blue.300` ← `blue.400` | 3.26:1 → 4.71:1 | 4.35:1 → 5.85:1 |
| `icon.info` (new) | `blue.600` | `blue.300` | 4.71:1 vs `bg.surface` | 5.85:1 vs `bg.surface` |
| `icon.on-info` (new) | `neutral.white` | `blue.900` | 4.71:1 vs new `bg.info` | 8.20:1 vs new `bg.info` |

`bg.info-subtle`, `bg.info-subtle-hover`, and `border.info-subtle` are unchanged, same as the previous three passes. One difference in consumer footprint: Avatar has no "info" status dot, so `bg.info-indicator`'s only real consumer was Badge's `.dotInfo` — one fewer file touched than the danger/warning/success passes, which each also updated an Avatar status.

**The one pairing worth calling out explicitly — flagged before implementing, proceeded at explicit direction:** `text.info` against `bg.info-subtle` in light mode computes to **4.5068:1** — 0.0068 above the 4.5:1 AA floor for normal text. This is the tightest margin found across all four consolidations (danger/warning/success/info), tighter even than warning's own 4.59:1 near-miss. Every other pairing in this pass has real margin (next-tightest is 4.71:1). Accepted as-is rather than special-cased — affects Avatar's `colorInfo` avatar family, Badge's subtle-info text, and Highlight, anywhere `text.info` sits on the light info tint rather than on plain `bg.surface`. Worth re-verifying if this primitive scale is ever regenerated, since there's essentially no headroom left to absorb drift.

**Same retirement pattern, safety in between danger's and success's:** `bg.info-indicator`'s old dark value (`blue.400`) already had real margin at 4.35:1 — same class of "already fine, not broken" retirement as success's, not a fix for a real gap like danger's. `border.info-strong`'s only consumer is Tag (outline default/selected border, solid's selected-halo accent) — same 3 spots as every prior pass; light-mode shift (`blue.700→blue.600`) is modest.

**Same structural note:** no validation-border consumer exists for `border.info`.

**Same two confirmations:**
1. Tag's info outline/selected-halo border recolors from `blue.700` to `blue.600` in light mode (6.93:1→4.71:1).
2. Dark-mode solid-info surfaces (ProgressBar, ProgressCircle, Badge solid, Tag solid/selected-outline — no Button/IconButton consumer) shift from `blue.500` fill + near-black `gray.950` text to `blue.300` fill + dark-navy `blue.900` text.
3. `border.info` and `bg.info` now resolve to the identical value in both modes — Tag's `.selected.outlineInfo` border-vs-fill contrast is a literal 1.00:1, joining brand/danger/warning/success. `neutral` is now the *only* tone left where this ring measures meaningfully above 1.00:1 (1.46:1 light / 3.49:1 dark, still on its own `-strong` tier) — see Tag's own `Tag.module.css` comments, rewritten this phase to reflect neutral being the sole remaining exception rather than one of several.

`icon.info`/`icon.on-info` are new, purely additive. `Color.mdx` and `Badge.mdx` updated to match (no `Avatar.mdx` change needed this pass, per the consumer-footprint note above), following the general-usage-text standing rule from Phase 25. Verified via `check-foundations-token-coverage.mjs` before considering the doc pass complete.

**Phase 29 (`bg.*` cleanup — retire `bg.surface-raised`/`bg.code`, rename `bg.subtle`→`bg.neutral-subtle`, reorder, 2026-08-20 — a separate request from the danger→info tone consolidations above, but landed the same day):** Four independent changes to the `bg.*` layer, each at explicit direction:

1. **`bg.surface-raised` retired**, every consumer moved to `bg.surface` directly. The two had been byte-identical in every theme/mode since Phase 9 (see that phase's own note) — elevation always came from the paired shadow, never a background difference, so the second name added a lookup with no payoff. No real component consumer existed (grep confirmed); the only touch point was `Shadows.mdx`'s own usage guidance, reworded to recommend `bg.surface` directly.

2. **`bg.subtle`/`bg.subtle-hover` renamed to `bg.neutral-subtle`/`bg.neutral-subtle-hover`**, no value change, and regrouped in both the token JSON files and `Color.mdx` to sit with `bg.neutral`/`bg.neutral-strong`/`bg.neutral-strong-hover` instead of up near `bg.canvas`/`bg.surface` — the name never made clear it was part of the neutral family, unlike every status tone's own `-subtle` token (`bg.danger-subtle`, etc.), which already reads as obviously related to its base tone. Real consumers were extensive — 15 component `.module.css`/`.stories.tsx` files, 2 Storybook chrome files (`manager.ts`, `theme.ts`), 2 `.mdx` docs — all updated to the new name. `manager.ts`/`theme.ts` needed a real code change, not just a find-replace: the built token object's keys stay literal (`bg["danger-hover"]`, not `bg.dangerHover`), so a hyphenated key isn't valid as `.subtle` dot-notation once renamed — every `t.bg.subtle`/`tokens.bg.subtle` access became bracket notation (`t.bg["neutral-subtle"]`), matching how this codebase already accesses every other hyphenated token key (e.g. the pre-existing `t.bg["brand-hover"]`).

3. **`bg.code` retired**, every consumer (`docs.css`'s two inline-code-pill rules) moved to `bg.info-subtle`. Light mode: zero visual change — `bg.code`'s own light value was already `blue.50`, byte-identical to `bg.info-subtle`, by design since Phase 8 (`bg.code` was explicitly built to mirror `bg.info-subtle`'s lightness curve under a status-neutral name). Dark mode: a real, deliberate visual change — `bg.code`'s dark value had been moved off blue entirely to a flat `gray.700` at an earlier explicit direction (dropping the blue tint in dark mode specifically); `bg.info-subtle`'s own dark value is `blue.950`, so the docs code-pill regains its blue tint in dark mode, reversing that earlier decision. Flagged rather than silently carried through, since it undoes a prior explicit choice, not just a rename. Knock-on effect on `border.code` (unaffected in value, `blue.100`/`gray.700`, but now paired with a different background): light mode unaffected (1.09:1, same as before); dark mode changes from 1.00:1 (byte-identical to the old flat `bg.code`, an intentional invisible-border look) to 2.63:1 against the new `bg.info-subtle` — a real, visible thin border where dark mode used to render flat/borderless, still comfortably within this decorative-tier border's own accepted range (WCAG 1.4.11 doesn't apply here) but a genuine style change. `text.code`/`text.secondary` (the pill's own text) remain comfortably clear of AA against the new background in both modes (6.57:1 light / 13.43:1 dark, freshly verified, not assumed). **Reversed the same day — see the dedicated phase right after this one.**

4. **`bg.overlay` moved to immediately after `bg.canvas`, `bg.skeleton` moved to immediately after `bg.overlay`** — pure reordering, no value or consumer change, applied identically across all 4 token JSON files and `Color.mdx`'s matching array, per this project's standing "`Color.mdx` mirrors the semantic JSON's own key order" rule (established Phase 15, applied throughout this whole log).

Final `bg.*` order after items 1/2/4 above (item 3, `bg.code`'s retirement, was reversed the same day — see the next phase for its own final position): `canvas`, `overlay`, `skeleton`, `surface`, `brand` family, `danger` family, `warning` family, `success` family, `info` family, `neutral`, `neutral-strong`, `neutral-strong-hover`, `neutral-subtle`, `neutral-subtle-hover`, `code`.

Every touched token's own `$description` was rewritten in place to explain these changes (not just a changelog entry here) — including fixing a few unrelated staleness issues found along the way: `border.code`'s own description still cited numbers from before an earlier `bg.code` dark-mode value change that was never propagated into it, corrected as part of this same pass since the description was being rewritten anyway. Rebuilt and re-verified via `check-foundations-token-coverage.mjs`, `tsc`, `eslint`, and the full unit/Storybook test suites before considering this done.

**Phase 30 (`bg.code` restored, reversing Phase 29 item 3, 2026-08-20 — same day):** At explicit direction, `bg.code`'s retirement was reversed: the token is back, at its original values (`blue.50` light / `gray.700` dark, unchanged from Phase 8/Phase 8-follow-up), positioned as the last key in the `bg.*` block (its original relative position — last, after the neutral family — carried over even though `bg.overlay`/`bg.skeleton` moved earlier in the block during Phase 29's own item 4; `bg.code` wasn't part of that reordering request, so it returns to sitting after whatever the neutral family's own last member now is, `bg.neutral-subtle-hover`).

Both `docs.css` consumers (`.sbdocs-content code` and `.dbm-proptable code`) moved back from `bg.info-subtle` to `bg.code` — restoring the exact pre-Phase-29 look: flat, borderless dark-mode code pills (`border.code`'s own gray.700 dark value is byte-identical to `bg.code`'s again, `1.00:1`, the intentional invisible-border treatment), rather than the blue-tinted, visibly-bordered look Phase 29 introduced. `border.code`'s own description reverted to describing this restored pairing rather than the brief `bg.info-subtle` interlude. `Color.mdx`'s `bg.code`/`border.code` rows restored to their pre-Phase-29 wording.

No other part of Phase 29 was touched — `bg.surface-raised`'s retirement, the `bg.subtle`→`bg.neutral-subtle` rename, and the `bg.overlay`/`bg.skeleton` reorder all stand as shipped. This is a narrow, single-token reversal, not a broader rollback.

Rebuilt and re-verified via `check-foundations-token-coverage.mjs` (76 tokens, up from 75), `tsc` (including `.storybook/tsconfig.json`, since `manager.ts`/`theme.ts` were untouched by this reversal and needed no further change), `eslint`, and the full unit/Storybook test suites before considering this done.

**Phase 31 (`text.link` de-branded to a shared `blue.600`/`blue.300`, 2026-08-20 — at explicit direction):** `text.link` moved off the brand anchor it had held since this token's introduction (`purple.600`/`purple.300` in the purple theme, `emerald.700`/`emerald.400` in the emerald theme) to a single shared value across all 4 theme files: `blue.600` light, `blue.300` dark. Links no longer visually match the active brand — they use a fixed, universal link-blue regardless of theme, closer to the broader web convention this token's own name implies than a brand-matching accent was.

| | Old (per theme) | New (shared) | Contrast vs `bg.surface` |
|---|---|---|---|
| Light | `purple.600` / `emerald.700` | `blue.600` | 4.71:1 (was 7.37:1 purple / 6.08:1 emerald) |
| Dark | `purple.300` / `emerald.400` | `blue.300` | 5.85:1 (was 5.32:1 purple / 4.63:1 emerald — both freshly re-verified against the correct `gray.800` `bg.surface`, see below) |

Both new values comfortably clear the 4.5:1 AA text floor — light mode's margin drops the most (a real, deliberate reduction, not a near-miss). `blue.600`/`blue.300` are byte-identical to `text.info`'s own light/dark values — not a problem (independently-tracked tokens that happen to share a step, same as several other pairs across this log), but worth knowing since it means `text.link` and `text.info` will always move together unless one is deliberately re-pointed later.

**Found and fixed while re-verifying `purple-dark.json`/`emerald-dark.json`'s own numbers:** both files' `text.link` and `text.brand` `$description`s had cited contrast figures computed against `gray.900`, a `bg.surface` dark value that hasn't been current since Phase 9's revert to `gray.800` — the descriptions were simply never updated when that revert happened. Recomputed against the actual current `bg.surface` (`gray.800`) rather than propagating the stale numbers: `purple.600`-on-`gray.800` is `1.37:1` (previously cited as `1.92:1`), `purple.300`-on-`gray.800` is `5.32:1` (previously cited as `5.80:1`), `emerald.600`-on-`gray.800` is `2.42:1` (previously cited as `3.39:1`). None of these change any actual token value or any pass/fail outcome — the failing pairings were already correctly identified as failing, and the passing ones as passing — only the specific ratio cited was off. Corrected in both dark files' `text.brand` descriptions.

**`text.brand` is unaffected in value** — it no longer says it "reuses `text.link`'s own resolved value" (that claim would now be false), but its own actual color is unchanged per theme (`purple.600`/`purple.300`, `emerald.700`/`emerald.400`). This split is a real, deliberate consequence, not a loose end: before this phase, `text.brand` and `text.link` were two names for the same value; after it, they diverge for the first time, which is arguably what should have been true all along — `text.brand` is for "brand-colored emphasis," `text.link` is for "link," and now only one of those is actually brand-colored.

**Two real consumers relied on `text.link`'s old brand-matching behavior and needed to move to `text.brand` to keep their existing, previously-verified appearance** (found by checking every consumer, not just updating the token in isolation):
- **Avatar's default `.content` fill** (the fallback-initials badge, paired with `bg.brand-subtle`/`border.brand-subtle`) — this is Avatar's own brand-colored default look, explicitly verified twice already (Phase 11: the base pairing; Phase 12: its hover state) — not a link. Left on `text.link`, it would have silently turned Avatar's initials text blue while its background/border stayed brand-purple or brand-emerald, a visibly broken mismatch. Moved to `text.brand`; both phases' own verified numbers carry over unchanged, since `text.brand` kept the exact value `text.link` used to hold.
- **Storybook's own `theme.ts`** — `colorSecondary` drives the toolbar's active Brand/Mode label color and the Docs page's active-TOC-item text, both of which specifically need to match whichever brand is selected (a "Purple" label should read in purple, not a fixed blue). Moved from `t.text.link` to `t.text.brand`, preserving the exact contrast numbers and reasoning already documented in that file's own comment (only the token name changed).

Every other real consumer (`Heading`'s `colorLink`, `Text`'s `colorLink`, `Link` itself) is a genuine link/link-styled use and picks up the new shared blue as intended — no changes needed there beyond the token's own value.

**A third case, caught by the automated a11y test suite rather than manual review: `Portal.stories.tsx`'s two demo badges** paired `text.link` directly with `bg.brand-subtle` (a coincidental pairing that worked before only because `text.link` used to resolve to the same brand color `bg.brand-subtle` is tinted from) — `blue.600` on `purple.50` measures **4.49:1**, failing the 4.5:1 AA text floor by a hair. Caught by `@storybook/addon-a11y`'s axe check during the Storybook test-suite re-run, not found manually — a good example of why that suite runs as part of self-verification rather than being treated as redundant with manual contrast math. These demo badges aren't links at all (generic "portaled content" markers), so moved to `text.brand` for the same reason as Avatar and the Storybook toolbar above — it's what they actually meant. Worth the general reminder: any other hand-authored `.stories.tsx`/`.mdx` file pairing `text.link` with a non-`bg.surface` background should be checked the same way if one turns up later; this pass only found the one.

**`Link`'s own WCAG 1.4.1 justification re-verified, not assumed to still hold:** `Link.module.css`'s `underlineAlways`-must-not-be-hover-only rationale depends on `text.link` staying under the 3:1 color-only-distinction floor against `text.primary`. Recomputed against the new shared value: **2.99:1 light / 1.65:1 dark** — both still comfortably under 3:1, so the existing design (underline can't default to hover-only, since `:hover` never fires for keyboard/touch users) remains correct without any component-level change. Both `Link.module.css` and `Link.types.ts` had this exact number hardcoded in a comment (previously a four-theme range, `1.92–2.33:1`, now collapsing to two numbers since both `text.link` and `text.primary` are brand-agnostic) — updated in place. Light mode's `2.99:1` is close enough to the `3:1` floor that this is flagged as worth re-checking explicitly again if the primitive scale ever regenerates, rather than assumed to hold forever.

Rebuilt and re-verified via `check-foundations-token-coverage.mjs`, `tsc` (including `.storybook/tsconfig.json`), `eslint`, and the full unit/Storybook test suites before considering this done.

**Phase 32 (`border.subtle`/`border.strong`/`border.neutral`/`border.neutral-strong` identifier rotation, 2026-08-21 — at explicit direction):** A four-way rename across all 4 semantic JSON files — no values changed anywhere, only names:

| Old name | New name | Value (light / dark) |
|---|---|---|
| `border.subtle` | `border.neutral-subtle` | `gray.100` / `gray.900` |
| `border.strong` | `border.neutral` | `gray.400` / `gray.600` |
| `border.neutral` | `border.neutral-strong` | `gray.600` / `gray.400` |
| `border.neutral-strong` | `border.strong` | `gray.700` / `gray.200` |

This is a genuine rotation, not a pairwise swap: `border.strong` moves to `border.neutral`, `border.neutral` moves to `border.neutral-strong`, and `border.neutral-strong` moves to `border.strong` — each token keeps its own original color, only three of the four identifiers cycle through each other. Requested, clarified, and confirmed explicitly after an initial ambiguous phrasing (a first draft of this request would have renamed `border.strong`, the default/hover border on 9 real components, directly to `border.neutral-subtle-hover` — flagged before implementing, since that name would have misdescribed a default-state token as a hover-only variant of the faint `subtle` tier; the corrected four-way rotation above is what actually shipped).

Kept in place structurally — same position in the JSON key order (`default`, then these four, then `focus`) and the same position in `Color.mdx`'s array — only the labels rotated. Read in the new order this produces a genuinely clean ascending-weight grouping right after `border.default`: `border.neutral-subtle` (100) → `border.neutral` (400) → `border.neutral-strong` (600) → `border.strong` (700, sitting just outside the `neutral-` prefix as the family's single richest step, mirroring how `border.brand`/`border.danger`/etc. each have their own unprefixed base name).

Real consumers were extensive — updated in the same pass, no value regressions since nothing about how any of them render actually changed:
- `border.strong`→`border.neutral`: 9 real component `.module.css` files (`Select`, `Input`, `Checkbox`, `Textarea`, `Button`, `IconButton`, `Kbd`, `Blockquote`, `Indicators`), plus 2 `.stories.tsx` demo files (`Portal`, `Affix`) and 2 `.mdx` docs (`Box.mdx`, `Overview.mdx`) using it as an inline demo/example border, plus `Button.mdx`'s own `TokenRow`.
- `border.subtle`→`border.neutral-subtle`: 7 Storybook-chrome-only files (`docs.css` and 6 `.storybook/blocks/**/*.tsx` Foundations-page components) — no published component consumes this token.
- `border.neutral-strong`→`border.strong`: `Tag.module.css`'s 3 real usages (`outlineNeutral`'s rim, both `selected.solidNeutral`/`selected.outlineNeutral`'s offset-halo rings) plus its own explanatory comment.
- `border.neutral`→`border.neutral-strong`: no real consumers today (per Phase 23's own note, Tag ended up using the token now named `border.strong` instead) — rename only, nothing to update.

**Found while updating `border.neutral`'s (formerly `border.strong`'s) real consumers, not fixed:** `Color.mdx`'s usage text for this token — "Hover-state border emphasis" — only describes 6 of its 9 real consumers (`Select`/`Input`/`Textarea`/`Button`'s secondary variant/`IconButton`/`Indicators` genuinely use it on `:hover`); `Checkbox`, `Blockquote`, and `Kbd` all use it as a permanent default-state border/accent, not hover-gated. Pre-existing gap, not introduced by this rename — flagged rather than silently carried forward unremarked, but left as-is since fixing stale usage text wasn't part of this request (a pure rename, done surgically).

Every historical phase entry above that named one of these four tokens (5, 9, 20, 21's own precedent, 23, 24) was rewritten in place to use the current identifier, per the same reasoning Phase 21 established: the decisions and numbers those entries describe didn't change, only which name is used to talk about them, so keeping the document internally consistent with the live token names beats preserving an identifier that no longer resolves to anything. Two spots needed more than a straight swap: Phase 20's own title used shorthand family notation (`border.{danger,warning,success,info,neutral}-strong`) that no longer holds literally for the neutral case now that its token is bare `border.strong`, not `border.{something}-strong` — clarified inline and with a forward note after Phase 24. Phase 24's own contrast breakdown had one literal composite label (`neutral-strong-vs-neutral-strong 1.46:1`) naming the old token directly — reworded to name both the old and new identifier.

Rebuilt and re-verified via `check-foundations-token-coverage.mjs` (76 tokens, unchanged — a rename, not an addition/removal), `tsc` (including `.storybook/tsconfig.json`), `eslint`, and the full unit/Storybook test suites before considering this done.

**Phase 33 (`bg.neutral` retired into `bg.neutral-strong`, renamed `bg.neutral-strong`→`bg.neutral` and `bg.neutral-strong-hover`→`bg.neutral-hover`, 2026-08-21 — step 1 of 2, at explicit direction):** Unlike `border.*`'s four-way rotation (Phase 32) or any other rename in this log, this one **retires** a token rather than purely renaming it — the original `bg.neutral` (`gray.500`, Phase 3) is gone, and its two real consumers (Avatar's `.statusOffline`, Badge's `.dotNeutral`) now resolve through whatever the token named `bg.neutral` points to going forward, which is `bg.neutral-strong`'s own former identity (`gray.600`), renamed in the same pass. `bg.neutral-strong-hover` renamed to `bg.neutral-hover` alongside it, no value change.

**This step is mechanics only — value is deliberately not yet fixed, and that leaves a real, known WCAG 1.4.11 regression in place on purpose, for a follow-up step to close:**

| | Old `bg.neutral` (`gray.500`) | New `bg.neutral` (`gray.600`, ex-`bg.neutral-strong`) |
|---|---|---|
| vs `bg.surface` light | 3.25:1 ✅ | 4.70:1 ✅ |
| vs `bg.surface` dark | 3.10:1 ✅ | **2.14:1 ❌ (below the 3:1 floor)** |

Avatar's offline dot and Badge's neutral dot — both non-text indicators governed by the 3:1 floor — now fail it in dark mode specifically, a regression introduced deliberately, mid-sequence, at explicit user direction: *"First, let's begin with [the retire/rename] ... and then, we will pick the correct value to map"* — a two-step request, confirmed after two full rounds of contrast review (this token's original creation reasoning in Phase 3, and a further round proposing `gray.300`/`gray.200` for dark mode that was itself found to break the *other* set of real consumers — `Badge.solidNeutral`, `Tag.solidNeutral`, `Tag.selected.outlineNeutral`, `Tooltip`'s `.content`/`.arrow` — which pair this same token with white `text.on-neutral` text, at only 1.72:1/1.35:1 against that light a gray). Step 2, picking a dark-mode value that satisfies both the non-text dot case (≥3:1 vs `bg.surface`) and the text-hosting case (≥4.5:1 for white text) simultaneously, or resolving the conflict some other way, is a separate, immediately-following piece of work — **this phase's own end state is not itself considered complete or shippable**, flagged explicitly in every touched description/doc rather than silently left broken (`bg.neutral`'s own `$description` in all 4 semantic JSON files, `Badge.mdx`, `Avatar.mdx`). **Resolved the same day — see Phase 34 immediately below.**

**Why historical phase entries above were NOT rewritten in place this time, breaking from the Phase 21/32 pure-rename convention:** that convention only holds when "the decisions and numbers didn't change, only the identifier" (Phase 21's own words). Here, the *identifier* `bg.neutral` persists but now means a *different value* (`gray.600` instead of `gray.500`) — Phase 3's original bullets and Phase 19's `bg.neutral-strong-hover` entry both use `bg.neutral`/`bg.neutral-strong` to describe the pre-merge values accurately; rewriting them in place to say "`bg.neutral`" throughout would make them describe the *wrong color*, not just use a stale name for the same one — the exact ambiguity risk Phase 21's convention doesn't anticipate. Handled instead with forward-pointing notes directly after the relevant Phase 3 and Phase 19 passages (see above), leaving the original numbers as accurate, dated history. Two isolated, unambiguous numeric citations (Phase 24's contrast breakdown, this document's own §-external cross-reference) were still safe to rewrite directly, since they didn't sit next to a conflicting old-`bg.neutral` mention.

Real consumers updated in the same pass: `Badge.module.css`'s `.solidNeutral` and `.dotNeutral`, `Tag.module.css`'s `.solidNeutral`/`.selected.outlineNeutral`/`.interactive.solidNeutral:hover`, `Tooltip.module.css`'s `.content`/`.arrow` (its only two color rules), and `Badge.test.tsx`'s one assertion referencing the old `--dbm-bg-neutral-strong` var name. Avatar's and Badge's dot consumers needed **no code change** — both already referenced the bare `--dbm-bg-neutral` custom property by name, so the retirement/rename swaps what that name resolves to without touching either file. `Badge.mdx`'s two separate `TokenRow`s for `bg.neutral-strong` (solid fill) and `bg.neutral` (dot fill) were merged into one, matching this project's existing convention for a token with more than one real use (same pattern already used for `bg.danger`/`bg.warning`/`bg.success`/`bg.info` after their own 2026-08-20 dot-mode consolidation). `Color.mdx`'s `bg.*` array likewise merged two rows into one and renamed the hover row, dropping the documented token count from 76 to 75 (a real removal, not a wash, unlike Phase 32's rename).

Rebuilt and re-verified via `check-foundations-token-coverage.mjs` (75 tokens, down from 76 as expected), `tsc` (including `.storybook/tsconfig.json`), `eslint`, and the full unit/Storybook test suites — **all passing, since none of them check real-world contrast ratios; the dark-mode WCAG failure above is real and open, not caught by any automated gate in this pipeline, only by the manual computation recorded here.** Step 2 (the value fix) is expected to follow immediately, in the same working session.

**Phase 34 (`bg.neutral`/`bg.neutral-hover` dark-mode values fixed, `text.on-neutral` dark value switched to a dark color, 2026-08-21 — step 2 of 2, at explicit direction):** Closes the WCAG gap Phase 33 deliberately left open. Two proposed fixes were reviewed and rejected before this one, each caught by computing the real numbers rather than assuming:

1. *Restore `gray.500`/`gray.600` split values under the merged name* (`bg.neutral` = `gray.500` both modes, `bg.neutral-hover` = `gray.600`/`gray.400`) — fixes the dot case (3.10–3.25:1, passes) but reintroduces the text-hosting failure Phase 3 originally created `bg.neutral-strong` to solve, this time in **both** modes, not just dark: white `text.on-neutral` on `gray.500` = 3.25:1, failing 4.5:1, on `Badge.solidNeutral`/`Tag.solidNeutral`/`Tag.selected.outlineNeutral`/`Tooltip`. Rejected — a bigger regression than the one it fixes.
2. *A new dedicated `bg.neutral-indicator` token*, splitting dot and text-hosting fill back into two tokens (the pre-Phase-33 shape, under a new name) — viable, but reopens a token this project just consolidated and doesn't match how `danger`/`warning`/`success`/`info` solved the identical tension.

**Adopted instead: switch dark mode to dark text on a light fill, the same fix already shipped for `bg.danger`/`bg.warning`/`bg.success`/`bg.info` in the 2026-08-20 consolidation** (e.g. `text.on-danger` dark = `red.900`, not white) — neutral has no hue, so its own "colored dark text" is simply `gray.900`. Light mode is untouched (it already worked): `gray.600` vs `bg.surface`/white text on `gray.600` were both already passing at 4.70:1.

| Token | Light (unchanged) | Dark (changed) |
|---|---|---|
| `bg.neutral` | `gray.600` | `gray.600` → **`gray.300`** |
| `bg.neutral-hover` | `gray.700` | `gray.700` → **`gray.200`** |
| `text.on-neutral` | `white` | `white` → **`gray.900`** |

All three re-verified directly, not assumed from the pattern:

| | dot vs `bg.surface` (≥3:1) | text on fill (≥4.5:1) |
|---|---|---|
| dark, base | `gray.300` vs `gray.800` = **5.86:1** | `gray.900` on `gray.300` = **8.21:1** |
| dark, hover | `gray.200` vs `gray.800` = **7.47:1** | `gray.900` on `gray.200` = **10.47:1** |

Both clear their floors with wide margins — `gray.300`/`gray.200` also match the exact steps every other tone's own dark fill/hover already landed on (`red.300`/`red.200`, `amber.300`/`amber.200`, `green.300`/`green.200`, `blue.300`/`blue.200`), so this isn't a one-off value pick, it's the same convention finally applied to `neutral`, the one tone that hadn't gone through it yet.

**Real, visible consequence, not just a number:** `Badge.solidNeutral`, `Tag.solidNeutral`/`.selected.outlineNeutral`, and `Tooltip`'s entire chrome now render **dark text on a light-gray fill in dark mode**, not white text on a mid-gray fill — flagged explicitly before implementing (not discovered after), confirmed live in Storybook (dark mode, both brand themes) before considering this done. Light mode is visually unchanged.

The temporary flags Phase 33 added — `bg.neutral`'s `$description` in all 4 semantic JSON files, `Badge.mdx`'s dot-mode caveat, `Avatar.mdx`'s `TokenRow` caveat — are removed/rewritten now that the gap they were flagging is closed; `text.on-neutral`'s own `$description` (all 4 files) rewritten to explain the dark-mode text-color switch and cross-reference `bg.neutral`'s own reasoning.

Rebuilt and re-verified via `check-foundations-token-coverage.mjs` (75 tokens, unchanged — value/description edits only), `tsc` (including `.storybook/tsconfig.json`), `eslint`, the full unit/Storybook test suites, and a live Storybook check (both brand themes × both modes) confirming the actual computed dot/fill/text colors before considering this — and the whole `bg.neutral` consolidation across Phases 33–34 — done.

**Phase 35 (`bg.brand`/`bg.brand-hover`/`border.brand` dark values moved to a lighter step, `text.on-brand` dark unified to `gray.900`, `bg.brand-indicator` retired into `bg.brand`, 2026-08-21 — at explicit direction):** Applies the same dark-mode pattern from Phases 34/16–17 (light fill + dark text, instead of a mid-tone fill + white text) to `brand`, the one tone that hadn't gone through it — reviewed and confirmed before implementing, not requested blind.

| Token | Old dark | New dark |
|---|---|---|
| `bg.brand` (purple) | `purple.500` | `purple.300` |
| `bg.brand` (emerald) | `emerald.500` | `emerald.300` |
| `bg.brand-hover` (purple) | `purple.600` | `purple.200` |
| `bg.brand-hover` (emerald) | `emerald.400` | `emerald.200` |
| `text.on-brand` (purple) | `white` | `gray.900` |
| `text.on-brand` (emerald) | `gray.950` | `gray.900` |
| `border.brand` (purple) | `purple.400` | `purple.300` |
| `border.brand` (emerald) | `emerald.400` | `emerald.300` |

**Two real, pre-existing problems this fixes, not just a stylistic pass:**
- `text.on-brand` dark was an *accumulated* inconsistency, not a designed one — purple used `white`, emerald used `gray.950` (`emerald.500` was too light for white text, so it got a workaround purple never needed). Now both share one value.
- `bg.brand-hover`'s dark-mode direction was **inconsistent between brands** — purple went darker on hover (`purple.500`→`purple.600`), emerald went lighter (`emerald.500`→`emerald.400`), opposite directions for the same UI state. Both now lighten, matching every other tone's own dark-mode hover convention.
- White text on the old `bg.brand` dark (`purple.500`) measured **4.51:1** — technically passing, but a near-miss one primitive regeneration away from silently failing.

**Every pairing re-verified directly, not assumed from the pattern:**

| | vs `bg.surface` dark (fill/border distinguishability) | `gray.900` text on it |
|---|---|---|
| `bg.brand`/`border.brand` (purple) | `purple.300` = **5.32:1** | **7.45:1** |
| `bg.brand`/`border.brand` (emerald) | `emerald.300` = **6.07:1** | **8.51:1** |
| `bg.brand-hover` (purple) | — | `purple.200` = **10.38:1** |
| `bg.brand-hover` (emerald) | — | `emerald.200` = **10.55:1** |

All eight clear their floors with wide margins — no near-misses like the 4.51:1 this replaces. `border.brand` also improves over its own prior values (purple `3.56:1`→`5.32:1`, emerald `4.63:1`→`6.07:1`) — see the forward note after Phase 22, which explains why `border.brand` no longer needs to borrow `border.focus`'s dark value now that `bg.brand`'s own dark step clears a real border's floor on its own.

**`bg.brand-indicator` retired, folded into `bg.brand`** — the same consolidation `danger`/`warning`/`success`/`info` went through on 2026-08-20 (see the forward note after that phase), now completing the set for the one tone that hadn't gone through it. `bg.brand-indicator` existed specifically because `bg.brand`'s old dark value didn't clear 3:1 well enough for a standalone non-text dot (`purple.400`/`emerald.400` were used instead, `3.56:1`/`4.63:1`); the new `bg.brand` dark value (`purple.300`/`emerald.300`) clears that same floor *more* comfortably than the old indicator token itself did (`5.32:1`/`6.07:1`), so the separate token is no longer needed. Light mode was already byte-identical between the two tokens (`purple.600`/`emerald.700` both ways) — this is a real, value-changing merge only in dark mode.

Real consumers: `Badge.module.css`'s `.dotBrand` and `Badge.test.tsx`'s one assertion, both moved from `--dbm-bg-brand-indicator` to `--dbm-bg-brand`. `Badge.mdx`'s two separate `TokenRow`s (solid fill, dot fill) merged into one, matching the existing convention for a multi-use token (same pattern already used for the other four tones). `Color.mdx`'s `bg.*` array dropped the `bg.brand-indicator` row and folded its usage into `bg.brand`'s own row text, taking the documented token count from 75 to 74. `border.brand` remains unconsumed by any real component (per Phase 22's own note) — this phase only changes its value, not its usage.

**Real, visible consequence, not just a number:** `Badge.solidBrand` and (once something consumes it) anything hosting `text.on-brand` render **dark text on a light brand-tinted fill in dark mode** for both brand themes now, not white/near-white text on a mid-tone fill — confirmed live in Storybook (dark mode, both brand themes) before considering this done. Light mode is untouched.

Rebuilt and re-verified via `check-foundations-token-coverage.mjs` (74 tokens, down from 75 as expected), `tsc` (including `.storybook/tsconfig.json`), `eslint`, the full unit/Storybook test suites, and a live Storybook check confirming the actual computed colors before considering this done.

## Multi-theme structure going forward

Adding a third brand theme later = one more pair of semantic JSON files (`{brand}-light.json`, `{brand}-dark.json`) referencing a new primitive color scale, following the exact same token names as the existing two. No changes needed to component code, since components should only ever reference semantic tokens, never primitives directly.

## Component-layer tokens

**First populated 2026-08-12 (Avatar's size scale), extended 2026-08-13 (Avatar's status-size scale, same file — `component/avatar.json` now holds two token families, `avatar.size.*` and `avatar.status-size.*`)** — the pattern below is now established, not just planned.

- **File convention:** one file per component, `component/{component-name}.json`, self-namespaced under that component's own top-level key (e.g. `avatar.json` → `{ "avatar": { ... } }`) — same unwrapped convention as `typography.json`/`other.json`, so no entry in `dbm/namespace-primitives`'s `NAMESPACE_BY_FILENAME` map is needed.
- **Theme-independent by default.** Unlike semantic tokens, component tokens aren't split per brand/mode — they're built once into `component-tokens.css`/`component-tokens.ts` and loaded globally (`:root`, no `data-theme` scoping), alongside `primitives.css`. A component token *can* alias a primitive (e.g. a future color override) if it ever needs one — the build includes the primitive source for reference resolution — but plain literal values (dimensions, etc.) are just as valid and don't require an alias.
- **When to reach for this layer, in order of preference:**
  1. Use an existing semantic token if one already fits.
  2. If a component needs a numeric value on an existing primitive scale (spacing, icon-size, etc.) that just isn't the value currently in use, prefer that existing scale over inventing something new.
  3. Only add a component-layer token when the component's design genuinely calls for a value with **no home on any existing primitive scale** — reach for this layer instead of hardcoding the literal value directly in component CSS, which the "tokens are the single source of truth" rule in `CLAUDE.md` forbids regardless of layer.
- **Precedent — Avatar's size scale:** requested pixel values were 36/40/44/48/52px. Only 40 and 48 matched existing tokens (`space-10`, `space-12`); 36/44/52 matched nothing on any scale. Rather than distort the shared spacing scale to fit one component's needs, or silently snap to the nearest existing values (changing the requested design), `component/avatar.json` was created with `avatar.size.{xs,sm,md,lg,xl}` as literal dimension values, scoped only to Avatar.
- **Build wiring:** `style-dictionary.config.js`'s `buildComponents()` runs a dedicated Style Dictionary instance (source: primitives + `component/*.json`, filtered to component-namespaced tokens via `token.filePath` rather than a hardcoded namespace list, so a new `component/*.json` file needs no config change) — same shape as the existing primitives-only and per-theme passes.

## Build pipeline decisions (Phase 2)

Style Dictionary 5's actual behavior diverged from a few of the phase brief's assumptions. Documented here so these don't get "fixed" as bugs in a later session — each was audited post-Phase-3 and confirmed to be the correct, standards-conforming response to a real SD5 constraint, not a shortcut.

- **Built-in transforms cover fontFamily/cubicBezier.** SD5 ships `fontFamily/css` and `cubicBezier/css` out of the box — no custom transform code needed, contrary to the original assumption. Used directly in `style-dictionary.config.js`'s `CSS_TRANSFORMS`.
- **Primitive files aren't self-namespaced.** `radius.json` and `breakpoint.json` both use bare `sm`/`md`/`lg`/`xl` at the top level, which collide on a naive merge. A custom parser (`dbm/namespace-primitives`) wraps each file's content under the namespace its DTCG aliases already expect (`color.json` → `color`, `spacing.json` → `space`, etc.); `typography.json` and `other.json` need no wrapping since their internal groups are already distinct.
- **The built-in size/rem transform can't handle `clamp()`.** Confirmed empirically — it throws `Invalid Number` on fluid typography values. `CSS_TRANSFORMS` deliberately excludes any dimension-unit transform; all dimension `$value`s (spacing, radius, fluid font sizes) are pre-authored as final, correctly-unitted CSS strings (`"0.25rem"`, `"4px"`, `"clamp(1.2rem, 1.186rem + 0.071vw, 1.25rem)"`) at the source, so they pass through the pipeline verbatim instead of being computed by a transform.
- **`shadow.json`'s `_note` key is stripped during parsing.** DTCG parsing expects every leaf to resolve to a `$value`; the human-readable `_note` annotation isn't a token, so the custom parser deletes it, scoped narrowly to that one file/key.
- **Each theme is built as a separate Style Dictionary instance**, not as one config with multiple platforms. The four semantic theme files (`purple-light`, `purple-dark`, `emerald-light`, `emerald-dark`) all use identical top-level token paths (`bg.canvas`, etc.) — loading more than one into a single dictionary at once collides. `style-dictionary.config.js` builds 5 instances total: one primitives-only pass plus one per theme, each sourcing the shared primitives plus exactly one semantic file.
- **Motion easing gets two parallel TS exports, decided 2026-08-16 (previously an open question in `01-vision-and-goals.md` §12).** `motion.easing.*` (unchanged) stays a CSS `cubic-bezier()` string — the form the built-in `cubicBezier/css` transform produces, and what the Foundations Motion page's own interactive demo (`MotionScale.tsx`) already builds a `transition` shorthand string from. A new sibling, `motion.easingArray.*`, additionally exports the same value as the raw `[x1,y1,x2,y2]` array — the form the `motion` library's own `easing` prop wants, and the one no export previously covered. Implemented generically in `dbm/typescript-const`'s `buildNestedObject` (reads `token.original.$value` for any `$type: "cubicBezier"` token, not hardcoded to `motion` specifically) rather than as a one-off — additive only, so the existing string export and its one real consumer are unaffected. No component uses the `motion` package yet (still zero imports of it anywhere in `packages/components`), so this remains unexercised until that integration actually happens — but the export shape no longer blocks it from starting.

## Files delivered
```
packages/tokens/src/
├── primitive/
│   ├── color.json
│   ├── typography.json
│   ├── spacing.json
│   ├── radius.json
│   ├── shadow.json
│   ├── breakpoint.json
│   ├── motion.json
│   └── other.json
├── semantic/
│   ├── purple-light.json
│   ├── purple-dark.json
│   ├── emerald-light.json
│   └── emerald-dark.json
└── component/
    ├── avatar.json
    └── badge.json
```

## Open for next pass
- Component-layer tokens: pattern established (Avatar's size scale); extend to other components only as their own reviews turn up a genuine scale gap, not preemptively
