/**
 * Curated re-export of Phosphor Icons for the DBM Design System. Currently
 * re-exports the full Phosphor set (including the `Icon`/`IconWeight`
 * types every icon component shares) — narrowing to an approved subset is
 * a future curation pass, not a v1 requirement.
 */
export * from "@phosphor-icons/react";
