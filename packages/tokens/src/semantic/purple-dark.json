{
  "bg": {
    "canvas": {
      "$type": "color",
      "$value": "{color.gray.700}",
      "$description": "Usage redefined (2026-08-08) to \"canvas and playground container\" — narrower than the prior \"page/app background\" meaning, now specifically the live-preview/demo container surface. Value moved off neutral.black (was briefly byte-identical to bg.surface) to gray.700, one step lighter than the reverted bg.surface (gray.800) — an intentional inversion of the usual dark-mode elevation cue (raised content is normally lighter than the canvas behind it), confirmed at explicit direction given canvas's narrower Storybook-specific usage. Known accessibility gap, accepted for now and slated for an upcoming text-color pass rather than blocking this background change: text.secondary on this value measures 4.00:1 (fails the 4.5:1 AA text floor) and text.tertiary measures 2.96:1 (fails even the 3:1 non-text floor); text.primary is fine at 6.59:1. Don't place text.secondary/tertiary literal text directly on bg.canvas until that follow-up lands."
    },
    "surface": {
      "$type": "color",
      "$value": "{color.gray.800}",
      "$description": "Reverted (2026-08-08) from neutral.black back to gray.800, paired with bg.canvas moving off neutral.black in the same edit — surface and canvas are no longer byte-identical; canvas (gray.700) now reads slightly lighter than surface (gray.800), see bg.canvas's own description for why. Reintroduces a border.default/bg.surface collision (both gray.800, 1.00:1) that the neutral.black value had incidentally fixed — reverted docs.css's table-border/section-divider rule back to border.subtle (gray.900, 1.40:1 against this value) to compensate. Contrast re-verified: text.primary 9.66:1, text.secondary 5.86:1, text.tertiary 4.34:1 — all still clear AA. Usage redefined in the same edit to \"page, card, sheet and drawer background\"."
    },
    "surface-raised": {
      "$type": "color",
      "$value": "{color.gray.800}",
      "$description": "Usage description added (2026-08-08): \"an elevated surface (paired with a shadow)\". Value unchanged (gray.800) — now byte-identical to bg.surface's own reverted value (see that token's description), removing the surface/raised background distinction in dark mode specifically; elevation here relies on the paired shadow instead. Confirmed at explicit direction alongside the bg.surface revert."
    },
    "subtle": {
      "$type": "color",
      "$value": "{color.gray.900}",
      "$description": "Usage redefined (2026-08-08) to \"hover fill for tertiary/ghost controls, dashboard panel, neutral accent\", value moved from gray.800 to gray.900 in the same edit — also resolves the surface-raised/subtle collision noted in this token's own history below, since gray.900 is a step further from surface-raised (gray.800) than gray.800 was from itself. Contrast re-verified: text.secondary-on-bg.subtle (Tag/Badge subtleNeutral) 8.21:1 (was 4.00:1 on the gray.700 this token's history below rejected, and comfortably clear of gray.800's own value too), text.primary 13.53:1 — both clear AA. Byte-identical to border.subtle (also gray.900) — nothing currently pairs the two, flagged here as a heads-up. History: was byte-identical to surface-raised on gray.800 (2026-07-27 review) — a hover fill invisible against the elevated card it sits on; gray.700 was tried and rejected then for failing the text.secondary-on-bg.subtle pairing at 4.00:1, which is why gray.900 (not gray.700) is the value used to break the collision here."
    },
    "subtle-hover": {
      "$type": "color",
      "$value": "{color.gray.800}",
      "$description": "New token (2026-08-17), found adding Tag's whole-tag clickable/selectable mode — bg.subtle's own hover step, one primitive scale step lighter (the dark-mode hover direction bg.brand-subtle-hover/etc. already use). Byte-identical to bg.surface-raised (also gray.800) — same kind of flagged-not-fixed collision already noted on bg.subtle itself above, and likewise harmless since nothing pairs the two directly. text.secondary (Tag/Badge subtleNeutral's own paired text) on gray.800 = 7.47:1, comfortably passes AA."
    },
    "brand": {
      "$type": "color",
      "$value": "{color.purple.500}"
    },
    "brand-hover": {
      "$type": "color",
      "$value": "{color.purple.600}",
      "$description": "Darker than brand (breaks the usual 'dark mode lightens on hover' direction, same deviation already established for bg.danger-hover below) — purple.400 only gives white text 3.41:1, failing AA; purple.600 gives 7.37:1 (verified 2026-07-18, Phase 4 OKLCH regen). purple.300 (lighter still) would only make it worse (2.43:1)."
    },
    "brand-subtle": {
      "$type": "color",
      "$value": "{color.gray.900}",
      "$description": "Moved from purple.950 to gray.900 (2026-08-09) at explicit direction, after two rejected candidates: gray.700 was tried first and rejected for two reasons — it dropped text.link (purple.300) to 3.63:1 (fails the 4.5:1 AA text floor, down from 10.86:1 on purple.950) and icon.brand (purple.400) to 2.43:1 (fails the 3:1 non-text floor); it also exactly collided with bg.canvas (also gray.700 in dark mode), making a selected/highlighted state visually indistinguishable from the plain canvas behind it. gray.900 avoids both: text.link measures 7.45:1, icon.brand measures 4.99:1, and it reads distinct from bg.canvas (2.05:1). Trade-off accepted: gray.900 is now byte-identical to bg.subtle (also gray.900), so a brand-highlighted state and a plain neutral hover-fill look the same if adjacent — same class of lower-priority overlap already accepted elsewhere in this system (e.g. bg.subtle/bg.surface-raised), not a contrast failure. Drops the purple tint from this token entirely in dark mode — brand-subtle no longer reads as \"purple\" there, only as a distinct-from-canvas-and-surface neutral highlight."
    },
    "brand-subtle-hover": {
      "$type": "color",
      "$value": "{color.gray.800}",
      "$description": "New token (2026-08-15), added for Avatar's hover state — one step lighter than brand-subtle (gray.900), matching this system's standard dark-mode hover direction (lighten, mirroring light mode's darken). text.link (purple.300) on gray.800 = 5.32:1, comfortably passes AA."
    },
    "brand-indicator": {
      "$type": "color",
      "$value": "{color.purple.400}",
      "$description": "New token (2026-08-16), found adding a `brand` tone to Badge — for non-text graphical status indicators. bg.brand's own dark value (purple.500) measures only 2.24:1 against bg.surface (gray.800), failing WCAG 1.4.11's 3:1 non-text floor outright — bg.brand was only ever verified as a background *for text*. One step lighter (purple.400) clears it at 3.56:1. Light theme reuses bg.brand's own light value directly (purple.600, 7.37:1) — see that theme's own description. Brand-specific (unlike the shared danger/warning/success/info-indicator tokens), since brand color itself varies per theme."
    },
    "danger": {
      "$type": "color",
      "$value": "{color.red.500}"
    },
    "danger-hover": {
      "$type": "color",
      "$value": "{color.red.400}",
      "$description": "Lighter than danger, restoring the usual dark-mode 'brighten on hover' direction (Phase 4 OKLCH regen, 2026-07-18) now that text.on-danger is gray.950 in dark mode: gray.950 on red.400 = 7.44:1. The earlier fix used red.600 (darker) specifically because it paired with white text (3.24:1 on red.400 failed then) — that constraint no longer applies now that on-danger is theme-specific."
    },
    "danger-subtle": {
      "$type": "color",
      "$value": "{color.red.950}",
      "$description": "Moved from red.900 to red.950 (2026-07-27), one step darker — the mirror-opposite move to the light theme's red.100 → red.50, preserving the same relative gap from each theme's pole. text.danger (red.300) on red.950 = 10.32:1, still comfortably passes AA (was 8.22:1 on red.900)."
    },
    "danger-subtle-hover": {
      "$type": "color",
      "$value": "{color.red.900}",
      "$description": "New token (2026-08-15), added for Avatar's hover state — reuses danger-subtle's own pre-2026-07-27 value (red.900), one step lighter, matching this system's standard dark-mode hover direction. text.danger (red.300) on red.900 = 8.22:1, comfortably passes AA. Shared across both brand themes, matching danger-subtle's own brand-independence."
    },
    "danger-indicator": {
      "$type": "color",
      "$value": "{color.red.400}",
      "$description": "New token (2026-08-16), found reviewing Badge's dot mode — for non-text graphical status indicators. bg.danger's own dark value (red.500) measures only 2.90:1 against bg.surface (gray.800), failing WCAG 1.4.11's 3:1 non-text floor — bg.danger was only ever verified as a background *for text* (gray.950 on top), a different and less strict pairing. One step lighter (red.400) clears it at 4.13:1. Light theme uses red.500 (3.48:1) — see that theme's own description. Shared across both brand themes, matching every other status-tone token's brand-independence."
    },
    "warning": {
      "$type": "color",
      "$value": "{color.amber.500}"
    },
    "warning-hover": {
      "$type": "color",
      "$value": "{color.amber.400}",
      "$description": "Lighter than warning, restoring the usual dark-mode 'brighten on hover' direction (2026-07-27), mirroring bg.danger-hover: gray.950 text.on-warning on amber.400 = 7.76:1, passes AA."
    },
    "warning-subtle": {
      "$type": "color",
      "$value": "{color.amber.950}",
      "$description": "Moved from amber.900 to amber.950 (2026-07-27), one step darker — the mirror-opposite move to the light theme's amber.100 → amber.50, preserving the same relative gap from each theme's pole. text.warning (amber.300) on amber.950 = 10.44:1, still comfortably passes AA (was 8.19:1 on amber.900)."
    },
    "warning-subtle-hover": {
      "$type": "color",
      "$value": "{color.amber.900}",
      "$description": "New token (2026-08-15), added for Avatar's hover state — reuses warning-subtle's own pre-2026-07-27 value (amber.900), one step lighter, matching this system's standard dark-mode hover direction. text.warning (amber.300) on amber.900 = 8.19:1, comfortably passes AA. Shared across both brand themes, matching warning-subtle's own brand-independence."
    },
    "warning-indicator": {
      "$type": "color",
      "$value": "{color.amber.400}",
      "$description": "New token (2026-08-16), found reviewing Badge's dot mode — for non-text graphical status indicators. bg.warning's own dark value (amber.500) measures only 3.05:1 against bg.surface (gray.800) — technically clearing WCAG 1.4.11's 3:1 non-text floor but with almost no margin, since bg.warning was only ever verified as a background *for text*, a different and less strict pairing. One step lighter (amber.400) clears it comfortably at 4.31:1. Light theme uses amber.500 (3.31:1) — see that theme's own description. Shared across both brand themes, matching every other status-tone token's brand-independence."
    },
    "success": {
      "$type": "color",
      "$value": "{color.green.500}"
    },
    "success-hover": {
      "$type": "color",
      "$value": "{color.green.400}",
      "$description": "Lighter than success, restoring the usual dark-mode 'brighten on hover' direction (2026-07-27), mirroring bg.danger-hover: gray.950 text.on-success on green.400 = 8.23:1, passes AA."
    },
    "success-subtle": {
      "$type": "color",
      "$value": "{color.green.950}",
      "$description": "Moved from green.900 to green.950 (2026-07-27), one step darker — the mirror-opposite move to the light theme's green.100 → green.50, preserving the same relative gap from each theme's pole. text.success (green.300) on green.950 = 10.68:1, still comfortably passes AA (was 8.19:1 on green.900)."
    },
    "success-subtle-hover": {
      "$type": "color",
      "$value": "{color.green.900}",
      "$description": "New token (2026-08-15), added for Avatar's hover state — reuses success-subtle's own pre-2026-07-27 value (green.900), one step lighter, matching this system's standard dark-mode hover direction. text.success (green.300) on green.900 = 8.19:1, comfortably passes AA. Shared across both brand themes, matching success-subtle's own brand-independence."
    },
    "success-indicator": {
      "$type": "color",
      "$value": "{color.green.400}",
      "$description": "New token (2026-08-16), found reviewing Badge's dot mode — for non-text graphical status indicators. bg.success's own dark value (green.500) measures only 3.28:1 against bg.surface (gray.800) — clearing WCAG 1.4.11's 3:1 non-text floor but thinner than desired for a dedicated indicator, since bg.success was only ever verified as a background *for text*, a different and less strict pairing. One step lighter (green.400) clears it more comfortably at 4.57:1. Light theme uses green.500 (3.07:1) — see that theme's own description. Shared across both brand themes, matching every other status-tone token's brand-independence."
    },
    "info": {
      "$type": "color",
      "$value": "{color.blue.500}"
    },
    "info-hover": {
      "$type": "color",
      "$value": "{color.blue.400}",
      "$description": "Lighter than info, restoring the usual dark-mode 'brighten on hover' direction (2026-07-27), mirroring bg.danger-hover: gray.950 text.on-info on blue.400 = 7.83:1, passes AA."
    },
    "info-subtle": {
      "$type": "color",
      "$value": "{color.blue.950}",
      "$description": "Moved from blue.900 to blue.950 (2026-07-27), one step darker — the mirror-opposite move to the light theme's blue.100 → blue.50 (2026-07-27), preserving the same relative gap from each theme's pole. text.info (blue.300) on blue.950 = 10.51:1, still comfortably passes AA (was 8.20:1 on blue.900)."
    },
    "info-subtle-hover": {
      "$type": "color",
      "$value": "{color.blue.900}",
      "$description": "New token (2026-08-15), added for Avatar's hover state — reuses info-subtle's own pre-2026-07-27 value (blue.900), one step lighter, matching this system's standard dark-mode hover direction. text.info (blue.300) on blue.900 = 8.20:1, comfortably passes AA. Shared across both brand themes, matching info-subtle's own brand-independence."
    },
    "info-indicator": {
      "$type": "color",
      "$value": "{color.blue.400}",
      "$description": "New token (2026-08-16), found reviewing Badge's dot mode — for non-text graphical status indicators. bg.info's own dark value (blue.500) measures only 3.09:1 against bg.surface (gray.800) — technically clearing WCAG 1.4.11's 3:1 non-text floor but with almost no margin, since bg.info was only ever verified as a background *for text*, a different and less strict pairing. One step lighter (blue.400) clears it comfortably at 4.35:1. Light theme uses blue.500 (3.26:1) — see that theme's own description. Shared across both brand themes, matching every other status-tone token's brand-independence."
    },
    "neutral": {
      "$type": "color",
      "$value": "{color.gray.500}",
      "$description": "Fifth status-tone fill, for neutral/offline-style indicators (e.g. Avatar's offline status dot). gray.500 on bg.surface: 3.25:1 light / 3.10:1 dark, both pass WCAG 1.4.11 non-text contrast (3:1) — re-verified 2026-07-18 (Phase 4, OKLCH regen), shared across all 4 themes since gray.500 uniquely satisfies both. (Dark figure corrected 2026-08-16 — the previously-recorded 4.34:1 was gray.400's own contrast against this same surface, not gray.500's; the token's actual value was never wrong, only this description's cited number. Found while adding bg.danger/warning/success/info-indicator, which needed the same gray.400-vs-gray.500 distinction checked carefully.)"
    },
    "neutral-strong": {
      "$type": "color",
      "$value": "{color.gray.600}",
      "$description": "Solid neutral fill for hosting text (Badge's solid variant, neutral tone) — bg.neutral (gray.500) is only verified for the 3:1 non-text case. gray.600 with white text gives 4.70:1, passing everywhere — shared across all 4 themes (re-verified 2026-07-18, Phase 4 OKLCH regen)."
    },
    "neutral-strong-hover": {
      "$type": "color",
      "$value": "{color.gray.700}",
      "$description": "New token (2026-08-17), found adding Tag's whole-tag clickable/selectable mode — bg.neutral-strong's own hover step. Unlike every other *-hover token, uses the SAME value (gray.700) in both light and dark themes rather than the usual darken-in-light/lighten-in-dark split: bg.neutral-strong itself is already brand-and-mode-agnostic (gray.600 in all 4 themes, see its own description above), and lightening toward gray.500 for a dark-mode hover fails AA against text.on-neutral (white on gray.500 = 3.25:1, below the 4.5:1 floor) — a documented exception, same class as bg.danger-hover/bg.brand-hover's own original dark-mode direction overrides (see 03-token-system-spec.md's Phase 3 notes). White text.on-neutral on gray.700 = 6.88:1, comfortably passes AA in both modes."
    },
    "skeleton": {
      "$type": "color",
      "$value": "{color.gray.900}",
      "$description": "New token (2026-08-16), added for Skeleton alongside the light-mode fix (see that theme's own description for the full rationale) — dark mode's existing gray.900 value (vs bg.surface/gray.800 = 1.40:1) was already visibly correct, confirmed live in Storybook, so this is a rename onto a dedicated token, not a value change. Light mode was revised same day to a brand-tinted purple.200/emerald.200; dark mode deliberately stays neutral gray rather than following suit, mirroring bg.brand-subtle's own established precedent (that token drops its brand tint entirely in dark mode too — purple.950 was tried and rejected there for failing text.link/icon.brand contrast and colliding with bg.canvas, see that token's own description). bg.skeleton hosts no text, so the contrast reason doesn't directly transfer, but the same underlying tension likely does: a dark, low-chroma brand tint reads as almost the same as plain gray at this end of the lightness curve, so there's no real visual payoff for the added complexity of a fourth brand-specific value. Revisit if a specific dark brand-tinted value is ever requested."
    },
    "overlay": {
      "$type": "color",
      "$value": "{color.neutral.black}",
      "$description": "Dimming scrim behind Dialog/Drawer/overlays (Backdrop atom). No text sits directly on it, so it isn't a WCAG contrast pairing — opacity is applied separately, compositionally, via the opacity.* scale rather than baked into this token, matching how color and opacity stay separate token categories everywhere else. Shared across all 4 themes since dimming what's behind isn't a brand-dependent decision."
    },
    "code": {
      "$type": "color",
      "$value": "{color.gray.700}",
      "$description": "Dark value moved from blue.950 to gray.700 (2026-08-09) at explicit direction — code pill drops the blue tint in dark mode specifically, moving to a neutral gray fill instead. Light mode is unchanged (blue.50). Now byte-identical to border.code's own dark value (also gray.700, see that token) — the pill's border is effectively invisible against its own fill in dark mode, an explicit flat/borderless treatment for this mode, not an oversight. text.secondary on this value still measures 5.10:1, clear of the 4.5:1 AA text floor. Original description: new token (2026-08-08) for inline code/token-reference pills in Storybook documentation (docs.css's .dbm-tokentable code) — mirrored bg.info-subtle's exact blue.50/blue.950 lightness-curve values, but under a name that doesn't imply status/info meaning, since a plain code reference isn't a status. Not currently consumed by any published component (the real Code.tsx atom uses bg.subtle instead) — Storybook-docs-only for now."
    }
  },
  "text": {
    "primary": {
      "$type": "color",
      "$value": "{color.gray.50}"
    },
    "secondary": {
      "$type": "color",
      "$value": "{color.gray.200}",
      "$description": "Moved from gray.300 to gray.200 (2026-08-08) at explicit direction, driven by bg.canvas's move to gray.700 the same day: text.secondary on bg.canvas measured 4.00:1 on gray.300 (fails the 4.5:1 AA text floor), gray.200 clears it at 5.10:1. Also improves against every other bg.* pairing: 7.47:1 on bg.surface (was 5.86:1 on gray.300), 10.47:1 on bg.subtle (was 8.21:1)."
    },
    "tertiary": {
      "$type": "color",
      "$value": "{color.gray.300}",
      "$description": "Moved from gray.400 to gray.300 (2026-08-08) at explicit direction, same motivation as text.secondary's move above. Still doesn't fully clear the 4.5:1 text floor against bg.canvas (gray.300 on gray.700 measures 4.00:1) — it does clear the 3:1 non-text floor there, but this token's own literal-text uses (Text color=\"tertiary\", Divider label, Input placeholder) need 4.5:1, same standard that ruled out gray.500 originally (see history below). No primitive step between 300 and 400 exists to close that last gap without also moving past text.secondary's own gray.200. Given that, `bg.canvas` should never host `text.tertiary` literal text — enforced by guideline note (see guidelines/03-token-system-spec.md) rather than by chasing a lighter value here, since 5.86:1 on bg.surface and 8.21:1 on bg.subtle (this token's actual intended surfaces) are both comfortably clear at gray.300. History: gray.500 originally failed AA (needs 4.5:1) for this token's literal-text uses; gray.400 gave 6.08:1 against the surfaces it was verified on at the time (re-verified 2026-07-18, Phase 4 OKLCH regen)."
    },
    "disabled": {
      "$type": "color",
      "$value": "{color.gray.600}"
    },
    "on-brand": {
      "$type": "color",
      "$value": "{color.neutral.white}"
    },
    "on-danger": {
      "$type": "color",
      "$value": "{color.gray.950}",
      "$description": "Text on a solid bg.danger fill. Post-OKLCH-regen (Phase 4, 2026-07-18): dark-theme bg.danger is red.500, where white only gives 3.48:1. gray.950 gives 5.22:1 (and 7.44:1 against danger-hover's red.400) — this token is now theme-specific; light themes keep white (5.10:1 against red.600)."
    },
    "on-info": {
      "$type": "color",
      "$value": "{color.gray.950}",
      "$description": "Text on a solid bg.info fill (Badge solid variant). Post-OKLCH-regen (Phase 4, 2026-07-18): dark-theme bg.info is blue.500, where white only gives 3.26:1. gray.950 gives 5.56:1 — this token is now theme-specific; light themes keep white (4.71:1 against blue.600)."
    },
    "on-success": {
      "$type": "color",
      "$value": "{color.gray.950}",
      "$description": "Text on a solid bg.success fill (Badge solid variant). Dark-theme bg.success is green.500: white fails (3.07:1), gray.950 passes at 5.91:1 (re-verified 2026-07-18, Phase 4 OKLCH regen). Light themes now use white instead — see purple-light.json / emerald-light.json."
    },
    "on-warning": {
      "$type": "color",
      "$value": "{color.gray.950}",
      "$description": "Text on a solid bg.warning fill (Badge solid variant). Dark-theme bg.warning is amber.500: white fails (3.31:1), gray.950 passes at 5.49:1 (re-verified 2026-07-18, Phase 4 OKLCH regen). Light themes now use white instead — see purple-light.json / emerald-light.json."
    },
    "on-neutral": {
      "$type": "color",
      "$value": "{color.neutral.white}",
      "$description": "Text on a solid bg.neutral-strong fill (Badge solid variant). White on gray.600 = 4.70:1, passes everywhere — shared across all 4 themes (re-verified 2026-07-18, Phase 4 OKLCH regen)."
    },
    "link": {
      "$type": "color",
      "$value": "{color.purple.300}",
      "$description": "purple.400 on bg.surface (gray.900) is 4.13:1, failing AA after the Phase 4 OKLCH regen. purple.300 gives 5.80:1 (verified 2026-07-18)."
    },
    "brand": {
      "$type": "color",
      "$value": "{color.purple.300}",
      "$description": "Accent text drawn from the brand color (2026-07-27) — for emphasis/labels, not links (see text.link). Reuses the same resolved value as text.link: purple.600 (the literal brand anchor) on gray.900 is only 1.92:1, badly failing AA — purple.300 gives 5.80:1."
    },
    "danger": {
      "$type": "color",
      "$value": "{color.red.300}"
    },
    "warning": {
      "$type": "color",
      "$value": "{color.amber.300}"
    },
    "success": {
      "$type": "color",
      "$value": "{color.green.300}"
    },
    "info": {
      "$type": "color",
      "$value": "{color.blue.300}"
    }
  },
  "border": {
    "default": {
      "$type": "color",
      "$value": "{color.gray.950}",
      "$description": "Moved from gray.800 to gray.950 (2026-08-08) at explicit direction. Fixes the exact-collision with bg.surface (also gray.800, 1.00:1) noted in Phase 9 of guidelines/03-token-system-spec.md — this pairing now measures 1.80:1. Also improves against bg.canvas (1.46:1 → 2.64:1). Trade-off: gets slightly worse against bg.subtle (1.40:1 → 1.28:1). Neither the old nor the new value clears the 3:1 WCAG 1.4.11 non-text floor against any of these real component-hosting surfaces (Input/Textarea/Button/Kbd/Divider/IconButton all use this token) — a pre-existing gap this change doesn't create, confirmed and accepted rather than assumed."
    },
    "subtle": {
      "$type": "color",
      "$value": "{color.gray.900}"
    },
    "strong": {
      "$type": "color",
      "$value": "{color.gray.600}"
    },
    "focus": {
      "$type": "color",
      "$value": "{color.purple.400}"
    },
    "brand": {
      "$type": "color",
      "$value": "{color.purple.900}",
      "$description": "Brand-accent border, decorative-only (2026-07-27, revised from purple.400). Dark value moved off the literal brand.100 shared with light mode to purple.900 (2026-08-16) — purple.100 on bg.brand-subtle (gray.900) measured 12.38:1, a stark high-contrast ring that read nothing like light mode's own low-key accent. purple.900 on gray.900 is 1.32:1, mirroring light mode's own 1.09:1 (purple.100 on purple.50) — both intentionally subtle. This token is for decorative UI accents, not a border that identifies a component/state boundary, so WCAG 1.4.11's 3:1 floor doesn't apply here (border.focus/border.danger remain contrast-verified since those ARE state-identifying)."
    },
    "danger": {
      "$type": "color",
      "$value": "{color.red.400}"
    },
    "danger-subtle": {
      "$type": "color",
      "$value": "{color.red.900}",
      "$description": "Family-matched decorative-accent border, dark counterpart to border.danger-subtle (light) — follows the same purple.900-in-dark-mode pattern established for border.brand above. Not neutralized to a shared gray the way border.brand's dark value is: family accents keep their own tint in dark mode (bg.danger-subtle here is red.950, not gray.900 — see that token's own description). Pairs with bg.danger-subtle for any component's danger-tinted ring, not limited to one specific component — first used by Avatar's `colorful` danger variant (2026-08-16), but not scoped to it. red.900 on red.950 is 1.26:1, comparably subtle to border.brand's own 1.32:1 dark pairing. Brand-agnostic — identical value in every brand theme."
    },
    "warning": {
      "$type": "color",
      "$value": "{color.amber.400}",
      "$description": "Added 2026-08-16 to complete the state-family set alongside border.danger — same amber.400 scale step border.danger uses for red, for a warning-toned state-identifying border (e.g. a future warning-state form control)."
    },
    "warning-subtle": {
      "$type": "color",
      "$value": "{color.amber.900}",
      "$description": "Family-matched decorative-accent border, same rationale as border.danger-subtle above — pairs with bg.warning-subtle for any component's warning-tinted ring, not limited to one specific component. amber.900 on amber.950 is 1.27:1. Brand-agnostic."
    },
    "success": {
      "$type": "color",
      "$value": "{color.green.400}",
      "$description": "Added 2026-08-16 to complete the state-family set alongside border.danger — same amber.400-equivalent scale step (green.400) for a success-toned state-identifying border."
    },
    "success-subtle": {
      "$type": "color",
      "$value": "{color.green.900}",
      "$description": "Family-matched decorative-accent border, same rationale as border.danger-subtle above — pairs with bg.success-subtle for any component's success-tinted ring, not limited to one specific component. green.900 on green.950 is 1.30:1. Brand-agnostic."
    },
    "info": {
      "$type": "color",
      "$value": "{color.blue.400}",
      "$description": "Added 2026-08-16 to complete the state-family set alongside border.danger — same scale step (blue.400) for an info-toned state-identifying border."
    },
    "info-subtle": {
      "$type": "color",
      "$value": "{color.blue.900}",
      "$description": "Family-matched decorative-accent border, same rationale as border.danger-subtle above — pairs with bg.info-subtle for any component's info-tinted ring, not limited to one specific component. blue.900 on blue.950 is 1.28:1. Brand-agnostic."
    },
    "code": {
      "$type": "color",
      "$value": "{color.gray.700}",
      "$description": "Dark value moved from blue.900 to gray.700 (2026-08-09) at explicit direction, matching bg.code's own move to gray.700 in dark mode — the two are now byte-identical there, an explicit flat/borderless pill treatment for dark mode specifically (light mode keeps the blue.100/blue.50 pairing and its visible border, unchanged). Original description: paired with bg.code (2026-08-08) — blue.100/blue.900, a purely decorative border, not one that identifies a component/state boundary, so WCAG 1.4.11's 3:1 floor doesn't apply — same accepted class as border.brand. Measured 1.09:1 light / 1.28:1 dark against the old bg.code, an accepted tradeoff, not an oversight."
    }
  },
  "icon": {
    "default": {
      "$type": "color",
      "$value": "{color.gray.300}"
    },
    "secondary": {
      "$type": "color",
      "$value": "{color.gray.500}"
    },
    "brand": {
      "$type": "color",
      "$value": "{color.purple.300}",
      "$description": "Moved from purple.400 to purple.300 (2026-08-09) at explicit direction. purple.400 was a real but tight pass of the 3:1 WCAG 1.4.11 non-text floor (icons are graphical/UI elements, not text, so 3:1 is the correct floor, not 4.5:1) against bg.surface — 3.56:1 — but failed outright against bg.canvas at 2.43:1, a real surface in this system (the live Storybook Canvas/Playground background). purple.300 clears both: 5.32:1 on bg.surface, 3.63:1 on bg.canvas — the latter now also clears the stricter 4.5:1 text floor, for extra headroom given icon.brand can end up adjacent to literal text using the same brand accent."
    },
    "disabled": {
      "$type": "color",
      "$value": "{color.gray.700}"
    }
  }
}
